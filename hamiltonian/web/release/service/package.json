{
  "name": "@release/service",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": "./index.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "prebuild": "bun run typecheck",
    "build": "bun build ./index.ts --target=browser --format=cjs --production --minify --drop console.debug --outfile=dist/index.js"
  },
  "dependencies": {
    "@internal/rpc": "workspace:^0.1.0"
  },
  "artifact": {
    "cache": "release"
  },
  "devDependencies": {
    "@types/serviceworker": "^0.0.200"
  }
}
