{
  "name": "@release/main",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "exports": "./main.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "prebuild": "bun run typecheck",
    "build": "bun build ./main.ts --target=browser --production --minify --drop console.debug --outfile=dist/index.js"
  },
  "dependencies": {
    "@internal/visual": "workspace:^0.1.0"
  },
  "artifact": {
    "cache": "release"
  }
}
