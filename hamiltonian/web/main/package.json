{
  "name": "@web/main",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "build": "bun run typecheck && bun build main.ts --target=browser --outfile=dist/main.js"
  }
}
