{
  "name": "@import/main",
  "private": true,
  "type": "module",
  "exports": "./main.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "prebuild": "bun run typecheck",
    "build": "bun build ./main.ts --target=browser --outfile=dist/index.js"
  },
  "dependencies": {
    "@internal/visual": "workspace:*"
  }
}
