{
  "name": "@import/main",
  "private": true,
  "type": "module",
  "exports": "./main.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "build": "bun run typecheck && bun build @import/main --target=browser --outfile=dist/main.js"
  },
  "dependencies": {
    "@internal/visual": "workspace:*"
  }
}
