{
  "name": "@startup/main",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "build": "bun run typecheck && bun build index.ts --target=browser --external=/import-main.js --outfile=dist/index.js"
  }
}
