{
  "name": "@startup/main",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "exports": "./index.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "prebuild": "bun run typecheck",
    "build": "bun build ./index.ts --target=browser --packages=external --production --minify --drop console.debug --outfile=dist/index.js"
  },
  "artifact": {
    "cache": "startup"
  }
}
