{
  "name": "@startup/main",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./index.ts",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "prebuild": "bun run typecheck",
    "build": "bun build ./index.ts --target=browser --external=/code?module=@release/main --production --minify --drop console.debug --outfile=dist/index.js"
  },
  "artifact": {
    "cache": "startup"
  }
}
