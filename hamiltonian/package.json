{
  "name": "@metafor/hamiltonian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run --parallel --if-present --filter '@startup/*' --filter '@release/*' --filter '@internal/*' build",
    "dev": "NODE_ENV=development bun --port=4444 server",
    "start": "bun --port=4444 server",
    "tls:create": "bun run create-tls.ts",
    "test": "bun test",
    "test:load": "tsc --project tests/tsconfig.json --pretty false && bun test tests/load-001.boundary.spec.ts tests/load-001.browser.spec.ts"
  },
  "dependencies": {
    "@release/main": "workspace:^0.1.5",
    "@release/service": "workspace:^0.1.5",
    "@release/server": "workspace:*",
    "@internal/visual": "workspace:^0.1.3",
    "@hamiltonian/visual": "workspace:*",
    "@metafor/engine": "workspace:*",
    "@metafor/web-push": "workspace:*",
    "@nodes/layout": "workspace:*",
    "@nodes/hud": "workspace:*",
    "@nodes/ui": "workspace:*",
    "@ui/components": "workspace:*",
    "@ui/elements": "workspace:*",
    "nodes": "workspace:*",
    "web-push": "3.6.7",
    "werift": "0.24.3"
  },
  "devDependencies": {
    "@types/web-push": "3.6.4",
    "puppeteer-core": "25.4.0"
  }
}
