/**
 * Канонический адрес хаба в MetaFor.
 *
 * Address — это строковый идентификатор репозитория-источника,
 * из которого загружаются meta-схемы.
 *
 * Формат строго фиксирован:
 *
 * `owner/repo`
 *
 * где:
 * - `owner` — владелец репозитория
 * - `repo` — имя репозитория
 *
 * Примеры:
 * - `zavx0z/metafor`
 * - `zavx0z/hub`
 *
 * В текущей версии адрес **не содержит хеша коммита или версии**.
 * Адрес указывает только на репозиторий-источник.
 */
export type Address = string
