{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MetaFor intentionally exposes no Tauri commands to the remote page.",
  "windows": ["main"],
  "permissions": []
}
