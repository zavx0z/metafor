# MetaFor Electron

Chromium shell for `https://meta.proizvodstvo1.ru/` on macOS.

```bash
bun --filter @app/electron dev
bun --filter @app/electron build
```

`dev` opens Electron with CDP on port `9230` for diagnostics.
