{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MetaFor WebView intentionally exposes no Tauri commands to the remote page.",
  "windows": ["main"],
  "permissions": []
}
