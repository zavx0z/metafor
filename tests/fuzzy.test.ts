import { describe } from "bun:test"

/**
 * Структура тестов для частицы Fuzzy.
 *
 * Fuzzy — это ветвление и суперпозиция.
 * Сюда относятся логические условия, ternary и другие допустимые
 * формы выбора на основе state/value.
 */

describe("Fuzzy — логическое ветвление", () => {
  // должен формироваться из логического условия по state
  // должен формироваться из логического условия по value
  // должен сохранять дочерние частицы ветви
})

describe("Fuzzy — тернарное ветвление", () => {
  // должен формироваться из ternary-условия
  // должен сохранять true-ветвь
  // должен сохранять false-ветвь
})

describe("Fuzzy — ограничения basis", () => {
  // не должен зависеть от mass
  // не должен принимать array как basis ветвления
  // не должен принимать runtime-источники вне state/value
})

describe("Fuzzy — нормализация", () => {
  // должен нормализовать basis из state
  // должен нормализовать basis из value
  // должен сохранять expr
  // должен сохранять branch-семантику дочерних частиц
})
