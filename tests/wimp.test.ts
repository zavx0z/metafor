import { describe } from "bun:test"

/**
 * Структура тестов для частицы Wimp.
 *
 * Wimp — это дочерняя meta-ссылка.
 * Здесь должны проверяться только ограничения и контракт формирования
 * частицы из dsl.gravity, а не весь dark pipeline.
 */

describe("Wimp — допустимый контракт", () => {
  // должен принимать статический src
  // должен принимать динамический src только от value
  // должен принимать fields только от value
  // должен использовать только статический tag meta-for
})

describe("Wimp — ограничения", () => {
  // не должен принимать отсутствующий src
  // не должен принимать src от mass
  // не должен принимать fields от mass
  // не должен принимать mass как topology payload
  // не должен принимать динамический meta-tag
  // не должен принимать невалидный hub-адрес в src
})

describe("Wimp — нормализация", () => {
  // должен нормализовать статический src в StaticBinding
  // должен нормализовать динамический src в DynamicBinding
  // должен нормализовать fields в Binding
  // не должен терять tag при нормализации
})
