/**
 * Типы для matrix.
 *
 * @packageDocumentation
 */

import type { MatrixData } from "./store.t"

/** Плоский подготовленный снимок, который Matrix записывает в канонический store. */
export type PreparedData = MatrixData
