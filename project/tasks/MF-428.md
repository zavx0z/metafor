# MF-428 — Различать несколько Chrome и их Service Worker

## Коротко

Если одновременно запущены несколько Chrome-профилей, Hamiltonian должен
показывать каждый наблюдённый Service Worker внутри того Chrome, которому он
принадлежит. Service Worker не должен становиться корневой нодой, сливаться с
Worker другого профиля или получать страницы чужого Chrome.

## Зачем

Один Chrome-профиль с несколькими вкладками уже использует один общий Service
Worker и один согласованный lifecycle. При одновременном запуске второго
профиля у того же origin появляется другой browser-local scope и другой Service
Worker. Без точной browser identity Hamiltonian показывает причинно неверную
топологию и не позволяет понять, какой Worker обслуживает какие страницы.

## Связь с дорожной картой

Задача относится к первому этапу Hamiltonian — управлению одной Вселенной на
одном устройстве в [`MF-425`](MF-425.md). Она обнаружена при визуальной работе,
но не является подзадачей `MF-424` и не относится к `@nodes/layout`: исправить
нужно Hamiltonian lifecycle projection и её browser ownership.

Постоянный владелец действующего закона —
[`hamiltonian/README.md`](../../hamiltonian/README.md), разделы о причинном
мониторе и странице оркестрации.

## Подтверждённые факты

* При одном Chrome-CDP с двумя вкладками обе page realm находятся внутри одного
  `Chrome` и используют общий Service Worker.
* При одновременной работе обычного Chrome и отдельного Chrome-CDP `@meta/chrome`
  подтвердил два разных browser process/profile.
* В этом двухпрофильном состоянии живая сцена показала наблюдённый `Service
  Worker` отдельной корневой нодой, хотя действующий Hamiltonian contract
  разрешает назначить browser parent после фактического `connect-window` с
  browser identity.
* Это входной lifecycle/ownership defect. Layout получает уже готовый
  `parentId` и не должен угадывать либо исправлять предметную принадлежность.
* Потеря принадлежности локализована в двух независимых местах: Service Worker
  не передавал host ownership-closed browser/profile snapshot, а page projection
  после authoritative replacement безусловно сохраняла отсутствующий remote
  `browser-runtime`. Оба дефекта относятся к Hamiltonian lifecycle/ownership,
  а не к layout.

## Решение владельца

* Вести это отдельной задачей Hamiltonian.
* Поддержать несколько одновременно наблюдаемых Service Worker и не смешивать
  исправление с текущей работой над раскладкой.
* В общей сцене каждый наблюдённый Chrome/profile является отдельной compound-
  нодой. Слева в её шапке остаётся имя runtime `Chrome`, справа показывается
  компактный идентификатор профиля, а полный идентификатор доступен в фактах.
* Наблюдённый Service Worker не показывается отдельно от своего Chrome после
  подтверждённого browser ownership: для второго Worker обязана существовать
  вторая Chrome-нода с его собственными страницами и transport.
* Действующая browser/profile identity — UUID Hamiltonian в origin-local
  storage Chrome-профиля (исторический key `hamiltonian-device`). Это не имя
  Google-профиля, PID, title, user agent или URL. UUID общий для вкладок одного
  storage profile, передаётся Service Worker и host, переживает quit/reopen
  вместе со storage; новый, очищенный либо incognito storage является новым
  owner.
* Логическая identity Service Worker, incarnation его текущего execution и
  версия исполняемого кода являются тремя разными величинами. Версия кода
  задаётся только валидным SemVer и видна в каждой ноде Service Worker.
* В ноде Service Worker компактная logical identity показывается справа в
  шапке, но не повторяется параметром. Описательный текст между шапкой и
  параметрами отсутствует.
* В первом локальном Mac-срезе Hamiltonian build обновляет JSON manifest
  последней browser-сборки. При подключении каждого сейчас запущенного
  Chrome/profile Hamiltonian сравнивает заявленную Worker code version с
  manifest и требует browser-managed update, если версия устарела.
* До совпадения с версией локального manifest Service Worker выполняет только
  техническую проверку и обновление и не начинает прикладную работу. Web Push
  может лишь разбудить Worker для проверки; код через Push не передаётся.
* Решение ограничено этим локальным browser-контуром и не задаёт общий закон
  обновления других воплощений либо механизм публикации за его пределами.

## Подзадачи

### MF-428.1 — Показать каждый Chrome-профиль отдельной нодой

Статус: результат зафиксирован коммитом
`c490c3d652390954e234cbf8aeda2c666b27cab2`; оставшиеся restart/close критерии
позднее выполнены в `MF-428.2`. После этого родитель был передан в `REVIEW`, но
12 августа 2026 года вновь открыт из-за принятых `MF-428.3`–`MF-428.5`.

Материализовать все одновременно наблюдаемые browser/profile owner как
отдельные `Chrome` compound-ноды, вывести profile identifier в правой части
шапки и сохранить полный identifier в фактах. Страницы, Service Worker и их
transport должны находиться только внутри exact profile owner; отсутствие
owner-ноды после подтверждённого `connect-window` является ошибкой lifecycle-
проекции, а не основанием поднять Worker на корень.

Подзадача вводит общий декларативный закон, а не специальную обработку второго
Service Worker: каждый передаваемый между realm retained snapshot обязан быть
ownership-closed. Некорневая entity допустима только вместе с точной живой
owner-chain до объявленного root этого scope. Host агрегирует валидированные
profile-scoped snapshots целиком; presentation не синтезирует Chrome по факту
появления Worker и не исправляет отдельные виды нод вручную.

#### Результат

Потеря принадлежности локализована на границе Service Worker → host retained
aggregation. Worker сообщал свою identity, а host удерживал Service Worker с
`ownerId` браузера, но сам remote `browser-runtime` через эту границу не
передавался. Presentation закономерно не могла поставить `parentId` на
невидимого владельца и материализовала Worker как root.

Исправление передаёт и валидирует ownership-closed snapshot профиля целиком:
Chrome-root, Service Worker и все уже наблюдённые дочерние realm. Общий
валидатор проверяет owner-chain независимо от вида дочерней ноды; host отдельно
подтверждает только exact browser root и exact Service Worker текущего control
endpoint. Поэтому второй профиль не требует специальной ветки, а будущий новый
вид browser-owned realm проходит тот же закон.

Offline proof: `49` lifecycle/projection/browser-build тестов и все `33` host
теста прошли, `bun run typecheck` прошёл. Live proof на двух точно адресованных
browser-local scope одного Chrome-CDP process подтвердил две вкладки и один
Service Worker на профиль, две отдельные Chrome-ноды с разными profile ID и
отсутствие корневого Service Worker; reload exact target в каждом профиле
сохранил его profile и Worker identity. Обе проверенные консоли пусты.

`ServiceWorker.stopAllWorkers` через текущий одно-командный REST CDP surface не
доказал execution restart: `ServiceWorker.enable` не переживает открытие новой
CDP-сессии, поэтому Chrome вернул `ServiceWorker domain not enabled`. Этот
результат не был засчитан на этапе `MF-428.1`; restart execution и закрытие
профиля позднее доказаны в `MF-428.2`.

### MF-428.2 — Доказать restart execution и закрытие профиля

Статус: результат принят как baseline 12 августа 2026 года; новые обязательные
срезы `MF-428.3`–`MF-428.5` его не отменяют.

На двух точно адресованных browser-local scope доказать, что restart execution
одного Service Worker сохраняет его логическую identity и exact Chrome owner,
не создавая дубль либо root. Затем закрыть один scope целиком и доказать, что
его Chrome ownership-поддерево удалено, а второй profile, его страницы, Worker
и transport не изменились. Если live-сценарий обнаружит lifecycle defect,
исправить общий retained ownership/retirement contract без веток по номеру или
типу конкретной ноды.

#### Результат

Настоящий browser-managed restart выполнен через `Stop` exact регистрации на
`chrome://serviceworker-internals/` отдельно в normal и incognito scope.
Логические Worker identity `45d8fde1-…` и `5df2f151-…` не изменились, execution
incarnation сменились соответственно `ed0e4a42-… → 40b71c0f-…` и
`3a1ab9c8-… → 11e38947-…`. Оба Worker остались внутри прежних exact Chrome
owner, root и дубликат не появились.

Живое закрытие локализовало два общих defect. Во-первых, Chrome может закрыть
control socket раньше финального `tabs: []`, поэтому немедленное удаление по
состоянию topology недостаточно. Host теперь ждёт общую heartbeat/authority
grace; reconnect того же profile отменяет expiry, а при отсутствии control,
Web Push и pending wake атомарно забывает всё ownership-поддерево без terminal
fence стабильной identity. Явно полученный пустой window inventory позволяет
тот же переход без ожидания. Во-вторых, page projection сохраняла любую
`browser-runtime` ноду при авторитетной замене snapshot, из-за чего после уже
чистого host snapshot оставался пустой Chrome root. Теперь она сохраняет только
owner текущей page, а отсутствующий remote browser scope забывает целиком и
может позднее принять ту же стабильную identity снова.

Offline proof: `bun run typecheck`, все `35` host тестов, все `23` core
lifecycle теста и все `27` lifecycle projection тестов прошли. Отдельные тесты
доказывают немедленное закрытие по пустому inventory, abrupt close после grace,
отмену удаления при reconnect/rebirth, сохранение причинного closed WebSocket
до завершения endpoint и повторное появление забытой стабильной identity.

Финальный live-сценарий на host version `mf-428-projection-final` использовал
normal profile `b04b5959-…` и incognito profile `90a10c48-…`, по две exact
вкладки и разные Worker. После закрытия exact incognito window host записал
`browser-profile-unreachable`; retained snapshot revision `389` содержит только
normal Chrome, его один Worker, две page realm и два Service Worker API
transport. В готовой сцене пустого remote Chrome, root Worker и cross-profile
edges нет; CDP console normal target за `1500` мс вернула `0` записей.

### MF-428.3 — Ввести SemVer версии кода Service Worker

Статус: результат принят 12 августа 2026 года на checkpoint-коммитах
`002570bb010d3ad5fb777db8dd28fa8124e2d4a2` и
`79e64bd3f31187d780acc4d0fd6700b4520e52a0`; родительская MF-428 остаётся
`IN_PROGRESS`.

Подзадача с законом, расхождением, границами и критерием была зарегистрирована
до реализации project-коммитом
`48ec7327e45cca2ddfde13c41f52c25ceb4863aa`. Отдельный подготовительный коммит
перехода родителя `READY → IN_PROGRESS` перед кодом записан не был: требование
появилось в `9359da629ad4418f39da5762583a94f859c5a8c8` уже во время выполнения.
Поздний project-коммит `dd78a561d13278b1ece74c28c173d00728c21198` этого
ретроспективно не заменяет. Приёмочная проверка была отдельно подготовлена
коммитом `1e8e7b6e8f4b75f93caeed2b2e42ffb34b85beb9` до дополнительных probes.

Ожидаемый наблюдаемый результат: в каждой ноде Service Worker видна точная
версия загруженного кода в валидном SemVer с обязательными
`MAJOR.MINOR.PATCH`; prerelease/build metadata допустимы только по синтаксису
SemVer. Она не подменяется logical Worker identity, execution incarnation, Git
revision, версией host или произвольной строкой сборки.

Применимы уже принятые ownership/restart законы `MF-428.1` и `MF-428.2`:
несколько профилей имеют разные logical Worker identity, но могут исполнять
одну и ту же версию кода; restart одного execution без смены bundle сохраняет
эту версию. До checkpoint lifecycle и карточка показывали identity и
incarnation, но не имели отдельного доказанного источника версии кода; frozen
regression зафиксировал это исходное расхождение.

Границы: не выводить версию из cache busting URL, времени сборки, connection
order или host label; не менять logical identity при обновлении кода; не
расширять SemVer-контракт на остальные runtime без отдельного решения.

Критерий приёмки: contract отклоняет не-SemVer значение, обе ноды двух
профилей показывают exact code version, restart execution сохраняет её, а
переход на новый bundle меняет только code version и incarnation, сохраняя
logical identity и exact Chrome owner.

#### Результат

Действующий закон находится в разделе `Версия кода Service Worker`
[`hamiltonian/README.md`](../../hamiltonian/README.md). Product checkpoint
`002570bb0` связывает bundle-owned SemVer, внутренний Worker → host contract,
проекцию и live evidence. Приёмочный checkpoint `79e64bd3f` дополнительно
доказывает отклонение несовпадающих identity/snapshot version и смены version
без новой execution incarnation.

Прошли строгие host и WebWorker TypeScript checks, все реальные browser/Worker
builds, `bun run typecheck`, `git diff --check` и полный `bun test hamiltonian`
(`177/177`, `3751` assertions). Exact normal/incognito evidence, restart того же
bundle и контрольные суммы двух снимков сверены в
[`project/artifacts/MF-428`](../artifacts/MF-428/README.md). MF-428.4 и MF-428.5
в checkpoints этой подзадачи не входят.

### MF-428.4 — Обновлять Worker всех запущенных локальных профилей

Статус: `COMPLETED`; product checkpoints `8a9805d49` и `260aa786a` прошли
offline proof, а двухпрофильная live-приёмка завершена на Worker `1.1.2` и
зафиксирована коммитом `1579d11cd`. Оба одновременно работающих профиля имеют
собственные confirmed identity/incarnation, по две восстановленные Window и
не смешивают ownership либо transport.

Классификация по правилу гранулярности: это продолжение уже принятой MF-428.4,
а не новая подзадача, потому что исследование уточняет контракт того же
наблюдаемого результата — одной локальной browser-сборки, установленной во все
сейчас запущенные Chrome/profile. Иные среды и выключенные профили не являются
скрытым результатом этой подзадачи.

Ожидаемый наблюдаемый результат: штатный локальный build Hamiltonian собирает
новую browser-версию и простым build hook обновляет JSON manifest последней
сборки с exact SemVer и hash. Каждый сейчас запущенный локальный Chrome/profile
при подключении сообщает logical Worker identity, execution incarnation и
фактически исполняемую code version. Hamiltonian читает локальный manifest,
просит browser-managed update только у устаревшего Worker и считает exact
профиль обновлённым лишь после подключения новой incarnation с manifest
version.

Это развитие существующего локального пути:

* host уже использует `Bun.build` для `browser/service-worker.ts`;
* `/manifest.json` уже отдаёт version и SHA-256 локального browser module;
* Service Worker уже читает manifest, сверяет hash перед кешированием и
  сообщает host отдельную Worker code version;
* первый patch должен связать эти механизмы простым build hook и exact
  profile-scoped version comparison, не вводя Bun plugin либо macro без
  необходимости.

Manifest обязан однозначно различать SemVer/hash собранного Service Worker и
уже существующие version/hash внутреннего browser module: одну величину нельзя
молча переименовать в другую. Точную минимальную JSON-форму нужно закрепить
frozen contract test перед product patch.

До совпадения declared Worker code version с локальным manifest stale Worker
не начинает прикладную работу: допустим только технический bootstrap,
version check и browser-managed update. Если stale Worker подключился позднее,
но его Chrome/profile сейчас запущен, он проходит ту же проверку и обновляется.
Web Push может лишь разбудить Worker и инициировать проверку; versioned code в
Push отсутствует.

Исторический разрыв до product checkpoint: manifest описывал локальный
versioned browser module, а Service Worker code SemVer жила отдельно и не
сравнивалась с manifest при profile connection. Этот разрыв закрыт отдельным
`serviceWorker {version, sha256}`, build hook и profile-scoped admission;
дальнейшие live-derived handshake причины закрыты в MF-428.6 и MF-428.7.

Границы: только один локальный Mac и только сейчас запущенные browser/profile.
Обновление выключенных профилей и публикация сборки за пределами этого контура
не входят в срез. Не смешивать профили через общий controller, не считать
отправку запроса фактом установки, не уничтожать logical Worker identity, не
обходить browser Service Worker lifecycle и не вмешиваться в `HAM-001`.
Конкретную механику сложнее простого build hook принимать только при доказанной
необходимости.

Критерий приёмки: frozen build/update test сначала доказывает изменение exact
Service Worker SemVer и hash в локальном manifest после новой browser-сборки,
не подменяя существующие module version/hash. Pure lifecycle test доказывает,
что current Worker допускается без обновления, stale Worker не начинает
прикладную работу и каждый exact profile подтверждается только новой
incarnation с manifest version. Live-сценарий с двумя одновременно запущенными
точно адресованными локальными профилями обновляет оба Worker, сохраняет их
разные Chrome owner и logical identity, восстанавливает transport без
cross-profile edges; поздно подключившийся stale профиль также догоняет
manifest version. Отдельная проверка подтверждает, что Web Push не содержит
code payload.

#### Product checkpoint 13 августа 2026 года

Service Worker release `1.1.0` теперь строится из точного результата
`Bun.build`: `/manifest.json` содержит отдельный `serviceWorker {version,
sha256}`, не меняя top-level version/hash внутреннего module. Host допускает
профиль к retained lifecycle, topology и peer только после exact version
admission. Stale execution получает `service-worker-update`, вызывает
`ServiceWorkerRegistration.update()` и подтверждается только новой execution
incarnation с target SemVer. При локальной пересборке host сначала атомарно
отзывает application topology всех stale профилей и дожидается закрытия
старого peer, затем отправляет всем один exact target. Web Push payload по-
прежнему содержит только wake capability.

Frozen regressions доказали stale connect до admission, запрет подмены target
в той же execution, два независимо допущенных профиля, общий rebuild target,
синхронный отзыв обоих profile/leader и восстановление только новыми
incarnation. Дополнительные live-derived regressions закрыли атомарную замену
фактов superseded declaration и очистку transient `reason` после успешного
heartbeat без удаления `lastFailure`. Финальный `bun test hamiltonian` прошёл
`195/195` (`4048` assertions), `bun run typecheck`, строгий WebWorker
TypeScript check, реальная сборка Worker и `git diff --check` зелёные.

Live contour дважды полностью перезапущен из canonical checkout после
live-derived исправлений. Отдельный Chrome-CDP обновил logical Worker
`45d8fde1-9ecb-4c83-b52a-095c974cb4a1` с `1.0.0` до `1.1.0`. Первый проход
локализовал retained stale facts, второй — неснятый transient `reason`; оба
расхождения получили frozen regressions. В финальном contour host epoch
`53db807f-6d0b-4280-8e64-c11534ba7fab` принял execution
`6ea18570-52a5-4044-b736-2e1ad3cdb0b8`: connection одновременно показывает
`1.1.0`, `identityConfirmed=true`, `workerUpdateRequired=false`; Worker
находится внутри Chrome, старой причины нет, topology, WS, peer и оба
RTCDataChannel восстановлены. Exact target
`11E491A8B2D07463D406EBAF7948C35A` не имел console errors.

Этот ранний checkpoint доказывал только CDP-профиль. Недостающее
двухпрофильное evidence позднее получено без неоднозначного AppleScript:
default process `408` подключился своим сохранённым scope, а CDP process
`23906` проверен по exact target. Финальный результат описан в MF-428.7 и
актуальном closing handoff ниже.

### MF-428.5 — Нормализовать шапку ноды Service Worker

Статус: `COMPLETED`; результат выполнен и проверен 12 августа 2026 года в
checkpoint-коммите `70fe491c1`. Исторически в тот момент родительская MF-428 и
MF-428.4 ещё продолжались; сейчас родитель находится в `REVIEW`, а MF-428.4
также завершена.

Ожидаемый наблюдаемый результат: слева в шапке остаётся `Service Worker`,
справа показывается компактная logical Worker identity. Параметр `Identity`
удалён, описательный текст сразу под шапкой отсутствует, а SemVer кода из
`MF-428.3` остаётся видимым параметром.

Текущее расхождение зафиксировано owner screenshot
[`service-worker-card-before-mf-428-3-5.png`](../artifacts/MF-428/service-worker-card-before-mf-428-3-5.png)
12 августа 2026 года: compact identity в шапке отсутствует, тот же identifier
занимает строку `Identity`, а между шапкой и параметрами показывается обрезанное
описание. Причину нужно локализовать в Hamiltonian lifecycle/presentation
projection и сначала закрепить snapshot regression.

Границы: не менять `@nodes/layout`, renderer, размеры карточек или общий закон
заголовков всех типов нод; не дублировать identifier одновременно в шапке и
параметрах; не скрывать SemVer вместе с описанием.

Критерий приёмки: projection snapshot и live evidence для Worker в каждом из
двух Chrome-профилей показывают compact identity только справа в шапке, не
содержат строки `Identity` и описательного блока, сохраняют видимую SemVer и
точное ownership-поддерево.

#### Результат

Сначала frozen projection snapshot на двух разных длинных logical Worker
identity зафиксировал исходное расхождение: справа в шапке не было identity,
в параметрах оставался факт `Identity`, а под шапкой — description; уже
принятый параметр `Версия кода 1.0.0` присутствовал. Regression упал именно по
этим трём presentation-различиям до изменения product code.

Причина локализована в Hamiltonian lifecycle presentation projection. Только
для `service-worker` compact logical identity теперь проецируется в правую
часть шапки, description не проецируется, а дублирующий identity-факт
фильтруется. Проекция `parentId`/`visualParentId`, visibility, ownership,
действие включения Web Push и параметр code SemVer не изменены. `@nodes/layout`,
renderer, размеры карточек, общий закон заголовков и MF-428.4 не затронуты.

Focused snapshot после исправления прошёл (`28/28`, `180` assertions, один
snapshot). Применимые lifecycle/code-version/browser-build regressions
MF-428.1–MF-428.3 прошли (`27/27`, `1221` assertions). Полный `bun test
hamiltonian` прошёл (`177/177`, `3715` assertions), как и `bun run typecheck`,
строгие Hamiltonian host и WebWorker TypeScript checks, реальные app,
embodiment, browser/Worker entry и Service Worker builds, а также
`git diff --check`.

Live evidence точно различает уже открытые default Chrome profile
`06cb9ee3-433d-4ca1-9c9f-56def51a0328` с Worker
`14fdce37-ce46-44cb-8697-cbcee2c7f810` и отдельный Chrome-CDP profile
`b04b5959-28a2-4ac0-ba5c-6bdba29e0091` с Worker
`45d8fde1-9ecb-4c83-b52a-095c974cb4a1`. Обе Service Worker cards на exact CDP
canvas показывают identity только справа в шапке, не содержат `Identity` или
description и сохраняют `Версия кода 1.0.0` внутри правильного Chrome owner.
Из-за двух browser process REST surface не дал exact прямой screenshot default
profile; его scope подтверждён exact host lifecycle evidence и общей сценой,
не разрушительным переподключением либо подменой одним профилем.

Полный кадр также захватил независимый предсуществующий host-epoch/node-system
declaration defect server RTCPeerConnection. По решению владельца он не
относится к MF-428.5 и вынесен в отдельный срез `HAM-001.1`. Поэтому
снимок доказывает только две требуемые Service Worker cards, но не общую
корректность topology всего графа. Точный provenance и checksum сохранены в
[`project/artifacts/MF-428`](../artifacts/MF-428/README.md).

### MF-428.6 — Допускать новый Worker к действующему Hamiltonian

Статус: `COMPLETED`; принята 13 августа 2026 года после двухпрофильной
live-проверки MF-428.4. Это новая подтверждённая причина и другой
handshake-механизм; она не расширяет патч авторства retained Worker из MF-428.4.

Результат зафиксирован checkpoint-коммитом `bf10ae45c`: pre-identity socket
получает отдельную server bootstrap projection без browser boundary, а после
admission Host публикует более новую полную declaration только с exact новой
incarnation. Независимая проверка прошла без блокирующих замечаний.

Требование: новая Service Worker execution после browser-managed update принимает exact
действующую server incarnation, передаёт свою новую browser/profile declaration и только затем
получает полную exact boundary к этой новой incarnation.

Живое расхождение: Worker release `1.1.1` фактически установлен в CDP profile, но его
first host frame отвергается как `invalid host node-system declaration`. Read-only probe показал:
server declaration уже несёт WebSocket boundary к старой browser incarnation `152575ec…`, тогда как
пустой registry новой execution ещё не имеет этой browser declaration и не может валидировать
её boundary endpoint.

Действие: frozen protocol test сначала доказывает тупик старой browser boundary.
Минимальный fix разделяет bootstrap server identity и полную cross-contour boundary: до identity host не
предъявляет новой execution ссылку на чужую старую browser incarnation; после admission обычная
точная boundary восстанавливается.

Защита и приёмка: pure registry test и host/Worker protocol test доказывают `old execution → update →
new execution` без circular declaration dependency, сохраняют exact server identity и восстанавливают
границу только к новой browser incarnation. Live CDP и default profiles оба показывают
`identityConfirmed=true`, Worker `1.1.1`, окна, WS и отсутствие cross-profile edges. `@nodes/layout` и
срезы `HAM-001` не меняются.

### MF-428.7 — Гарантированно закрывать отклонённый WebSocket в browser Worker

Статус: `COMPLETED`; принята 13 августа 2026 года по live CDP
ServiceWorker error report. Это отдельное protocol/error-handling требование, а не скрытая часть
declaration fix.

Результат зафиксирован checkpoint-коммитом `84a1c99af`: все browser rejection
ветки закрывают exact current socket private code `4008`, UTF-8 reason ограничен
123 bytes, а Worker release поднят до `1.1.2`. Независимая проверка прошла без
findings; full host suite — `50/50`.

Живое расхождение: в browser WebSocket API приложение может вызвать `close()` только с кодом
`1000` либо `3000–4999`. Текущие fail-closed branches Worker вызывают `close(1008, reason)`; Chrome бросает
`InvalidAccessError`, и отклонённый socket остаётся жив и продолжает heartbeat без identity.

Действие: ввести один browser-safe fail-closed helper с private application code и bounded reason,
перевести на него все rejection branches и не смешивать это с server-side WebSocket close semantics.

Защита и приёмка: browser contract test доказывает, что каждая rejection branch закрывает exact
current socket до identity/application traffic без `InvalidAccessError`, не оставляет heartbeat-only ghost
connection и запускает обычный reconnect policy.

Live-приёмка завершена на двух одновременно запущенных Chrome process/profile.
Host после обновления и полного heartbeat-интервала одновременно показал:

* default profile `06cb9ee3-…`, Worker `14fdce37-…`, execution `cc6b9a7e-…`,
  code `1.1.2`, `identityConfirmed=true`, update=false, две восстановленные
  Hamiltonian-вкладки;
* CDP profile `b04b5959-…`, Worker `45d8fde1-…`, execution `62c5ce63-…`,
  code `1.1.2`, `identityConfirmed=true`, update=false, две exact CDP-вкладки.

Exact CDP canvas показывает две отдельные Chrome compound-ноды и по одному
Worker `1.1.2` внутри каждой; корневого либо дублирующего Worker и cross-profile
page ownership нет. Evidence и provenance сохранены в
[`project/artifacts/MF-428`](../artifacts/MF-428/README.md).

### MF-428.8 — Синхронизировать host-фикстуры с текущей версией Worker

Статус: `COMPLETED`; принята 13 августа 2026 года после offline-проверки
MF-428.6. Это test-contract расхождение, а не product defect и не часть
declaration handshake.

Результат зафиксирован checkpoint-коммитом `72561b4e2`. Общие defaults теперь
связаны с exported current Worker version, явно versioned update fixtures
сохранены; полный host suite прошёл `50/50`, независимая проверка — без findings.

Требование: обычная host-фикстура действующего Worker заявляет текущую SemVer
из того же единственного источника, что и исполняемый browser Worker. Сценарии
stale/update сохраняют собственные явно закреплённые старую и целевую версии.

Расхождение и причина: executable Worker уже имеет версию `1.1.1`, но defaults
`browserIdentityMessage()` и `browserProfileLifecycleSnapshot()` остались на
`1.1.0`. Поэтому Host корректно отвечает update вместо
`service-worker-current`, а 27 нерелевантных тестов завершаются timeout до своей
предметной проверки.

Действие и защита: общие defaults host tests читают exported current Worker
version; явно versioned update fixtures не меняются. Полный `host.spec.ts`
проходит, stale/update regressions продолжают доказывать различие версий,
product/runtime, Worker bundle и его SemVer не изменяются.

## Целевой закон

* Browser-local identity отличает одновременно работающие Chrome
  process/profile одного приложения и origin; одинаковые title, user agent и
  URL не являются достаточной identity.
* Каждая наблюдённая browser/profile identity материализует отдельную compound-
  ноду: runtime name показывается слева в шапке, компактная profile identity —
  справа, полный identifier — в фактах ноды.
* Retained lifecycle snapshot является ownership-closed: каждая некорневая
  entity содержит доступную в том же scope живую цепочку владельцев до exact
  browser/profile root. Snapshot с orphan child не принимается в aggregate.
* Каждый Service Worker после подтверждённого `connect-window` получает ровно
  один exact browser parent и остаётся внутри него во всех retained snapshots.
* Несколько вкладок одного Chrome/profile используют один логический Service
  Worker scope; несколько профилей не сливают свои Worker, page realm,
  transport или causal frontier.
* Перезапуск execution одного Service Worker обновляет его воплощение внутри
  того же browser scope и не создаёт корневую либо дублирующую ноду.
* Logical Worker identity, execution incarnation и SemVer исполняемого кода не
  взаимозаменяемы. Обновление кода сохраняет identity, но подтверждает новую
  версию и новое execution отдельно в каждом browser/profile scope.
* Локальный Hamiltonian build обновляет JSON manifest latest browser build с
  exact SemVer и hash. При подключении каждого сейчас запущенного локального
  profile его declared Worker version сравнивается с manifest независимо от
  других профилей.
* Stale Service Worker до совпадения с manifest выполняет только техническую
  проверку и browser-managed update и не начинает прикладную работу. Успех
  exact профиля подтверждает новая incarnation с manifest version; Web Push
  переносит только сигнал пробуждения/проверки, но не код.
* В карточке Service Worker compact identity находится только справа в шапке;
  описательный блок и отдельный параметр `Identity` отсутствуют, code SemVer
  остаётся видимым параметром.
* Закрытие одного Chrome/profile удаляет только его ownership-поддерево и не
  изменяет страницы, Worker и transport другого профиля.
* Все страницы одного профиля сходятся к одному retained snapshot, а данные
  разных профилей объединяются в общей сцене только как явно разные browser
  ownership-поддеревья.

## Разрешённые вопросы identity

* Page владеет созданием origin-local UUID, Service Worker получает его только
  через точный `connect-window`/control bootstrap, host — через control
  endpoint. Chrome account name и глобальная user-data-dir не требуются.
* Полный quit/reopen не создаёт новый owner, пока сохранён browser storage.
  Очистка storage или новый off-the-record scope создаёт новое поддерево.

## Границы

* Не менять `@nodes/layout`, UI-геометрию, routing, clearance или renderer.
* Не скрывать корневой Service Worker и не назначать parent по названию
  приложения, URL, user agent, порядку подключения или последнему активному
  окну.
* Не сливать Service Worker разных browser profile только потому, что они
  зарегистрированы на одном origin.
* Не менять production-домены, authority или transport protocol Hamiltonian
  шире необходимого browser lifecycle identity/aggregation.
* В MF-428.4 не расширять результат за пределы одного локального Mac, сейчас
  запущенных browser/profile и manifest последней локальной browser-сборки.
* Развивать существующие `Bun.build`, `/manifest.json` и Worker-side
  hash/version check простым build hook; не вводить Bun plugin либо macro без
  доказанной необходимости.

## Критерии готовности

* Pure lifecycle test с двумя разными browser identity, двумя Service Worker и
  несколькими вкладками доказывает точные parent/child и transport endpoints
  независимо от порядка событий.
* Contract test отклоняет profile snapshot без browser root, с чужим root либо
  с разорванной/cyclic owner-chain; host принимает и агрегирует целиком два
  валидных ownership-closed profile snapshot.
* Две вкладки одного профиля показываются внутри одного Chrome и используют
  один Service Worker; вкладки второго профиля находятся только в его отдельном
  Chrome и используют другой Service Worker.
* В общей сцене присутствуют обе Chrome-ноды; их правые заголовки и полные
  profile identifier однозначно различаются.
* Ни один живой Service Worker после подтверждённого `connect-window` не
  остаётся корневой нодой; cross-profile edges и дубликаты отсутствуют.
* Reload вкладки, restart Worker execution и закрытие одного профиля сохраняют
  корректную ownership-грань другого профиля.
* Два профиля показывают SemVer фактически исполняемого Worker code; invalid
  version отклоняется, restart без обновления сохраняет версию, а локальная
  browser-сборка меняет manifest и обновляет каждый сейчас запущенный stale
  profile с отдельным подтверждением.
* Stale Worker не начинает прикладную работу до manifest version; current
  Worker не обновляется без необходимости, поздно подключившийся stale
  локальный profile догоняет manifest, а Web Push не содержит code payload.
* В шапке каждой Service Worker ноды справа виден compact identifier; строки
  `Identity` и описательного блока под шапкой нет, SemVer остаётся видимым.
* Одинаковые входные lifecycle observations и их стабильные перестановки дают
  одинаковый node-system document.
* Живой сценарий с двумя точно адресованными Chrome-профилями подтверждает
  результат в обеих вкладках каждого профиля; тест или screenshot одного
  профиля не заменяет это доказательство.
* Долговечный закон нескольких browser scopes актуализирован в
  `hamiltonian/README.md`, а независимая closing review подтверждает документ,
  public contracts, код, тесты и live evidence.

## Проверка результата

1. Сначала воспроизвести текущий defect на frozen lifecycle fixture без
   браузера и локализовать потерю exact browser identity.
1. Проверить один профиль с двумя вкладками как обязательный non-regression.
1. Проверить два одновременно запущенных профиля с одинаковым origin, но
   разными browser/Worker identity.
1. В каждом профиле проверить reload вкладки и restart Service Worker
   execution, затем закрыть один профиль и сверить оставшееся поддерево.
1. На frozen fixture проверить обновление SemVer/hash локального manifest
   простым build hook, current/stale version comparison и запрет прикладной
   работы stale Worker до новой incarnation.
1. На двух одновременно запущенных локальных Chrome/profile проверить одну
   новую browser-сборку, независимое обновление обоих Worker и позднее
   подключение stale профиля; отдельно проверить отсутствие кода в Web Push.
1. Только после offline proof провести exact-target live-проверку и сохранить
   evidence.

## Актуальный closing handoff MF-428.1–MF-428.8

Статус передачи: полный результат зафиксирован в `REVIEW`-коммите
`1579d11cdb88bf0915b8f59b014dad56b7361e7d` и готов к повторной независимой
closing review после устранения документальных замечаний. Этот раздел заменяет
по готовности исторический handoff MF-428.1–MF-428.2 ниже, но не удаляет его
provenance.

### Точные коммиты и граница

* MF-428.1 ownership — `c490c3d652390954e234cbf8aeda2c666b27cab2`;
  MF-428.2 retirement/rebirth — `a223d02a6f251d57c346c5ae135ddb5a4535eec5`;
  transport closure после первой review —
  `27b232837acbf8162a61f85482296d126d0da4d5`.
* MF-428.3 Worker SemVer — `002570bb010d3ad5fb777db8dd28fa8124e2d4a2`
  и `79e64bd3f31187d780acc4d0fd6700b4520e52a0`; MF-428.5 card header —
  `70fe491c1`.
* MF-428.4 local update/admission — `8a9805d49` и `260aa786a`; MF-428.6
  declaration bootstrap — `bf10ae45c`; MF-428.8 fixture contract —
  `72561b4e2`; MF-428.7 browser-safe rejection и Worker `1.1.2` — `84a1c99af`.
* Финальное двухпрофильное evidence и перевод родителя в `REVIEW` —
  `1579d11cdb88bf0915b8f59b014dad56b7361e7d`.
* Product scope ограничен private-пакетом `hamiltonian`; project-коммиты меняют
  только `project/TODO.md`, карточку и её артефакты. `@nodes/layout`, generic
  renderer, HAM-001 и production-контуры в результат MF-428 не входят.

### Домены, contracts и долговечные выводы

Постоянный владелец всех новых законов —
[`hamiltonian/README.md`](../../hamiltonian/README.md). Он фиксирует exact
browser/profile ownership, ownership- и transport-closed lifecycle snapshot,
restart/rebirth, atomic declaration replacement, Worker-owned identity,
отдельную SemVer execution code, manifest admission/update, pre-identity server
bootstrap и browser-safe rejection WebSocket.

Реализация затрагивает lifecycle/registry в `hamiltonian/core`, host aggregation
и update orchestration, browser Service Worker, page lifecycle projection и
Hamiltonian node-system materialization. Внутренний Worker ↔ host protocol
runtime-валидируется и собирается внутри одного private package без exports;
отдельный public discriminated type сейчас не требуется. Layout получает уже
проверенные `parentId` и transport endpoints и не угадывает ownership.

Признаны неактуальными допущения, что второй Worker можно обработать особой
веткой, browser identity можно вывести из имени/PID/URL, page observation может
заменить Worker-authored identity, новая execution обязана знать retained
boundary прежней incarnation, а browser WebSocket принимает server close code
`1008`. Теперь один декларативный закон применяется ко всем profile scope.

### Проверки и live evidence

Независимая closing review на HEAD `1579d11cd` выполнила `bun test hamiltonian`:
`201 pass`, `0 fail`, `4197` assertions; `bun run typecheck`, оба strict
TypeScript checks и реальные app/embodiment, entry и Service Worker builds
прошли. SHA-256 финального PNG подтверждён как
`22d0ab5408b06a8a8b9c351546af24d9ae3ff54fe5efff2f4b4358205fcd8aa0`.

После полного heartbeat-интервала authenticated live status одновременно
показал default scope `06cb9ee3-…` / Worker `14fdce37-…` и CDP scope
`b04b5959-…` / Worker `45d8fde1-…`: обе разные identity подтверждены на code
`1.1.2`, update=false, по две Window. Exact CDP canvas показывает две отдельные
Chrome compound-ноды, по одному Worker внутри каждой, отдельные WS boundaries
и отсутствие root/duplicate/cross-profile Worker. Provenance находится в
[`project/artifacts/MF-428`](../artifacts/MF-428/README.md).

Повторный reviewer должен заново подтвердить согласованность этого handoff с
owner, contracts, code, tests и live evidence. Только положительный verdict
разрешает удалить запись MF-428 из `TODO.md`, карточку и всю директорию
артефактов отдельным closing-коммитом.

## Исторический closing handoff MF-428.1–MF-428.2

Статус передачи на тот момент: результат исполнителя был готов к отдельной
независимой closing review. После принятия `MF-428.3`–`MF-428.5` этот handoff
сохраняется только как provenance прежнего baseline и не описывает готовность
родительской задачи к закрытию.

### Точные коммиты и граница результата

* `c490c3d652390954e234cbf8aeda2c666b27cab2` —
  `MF-428.1 retain browser profile ownership`: ownership-closed snapshot,
  отдельные Chrome/profile-ноды, profile ID в заголовке и первичный live proof.
* `a223d02a6f251d57c346c5ae135ddb5a4535eec5` —
  `MF-428.2 retire unreachable browser profiles`: restart execution, retirement
  недостижимого profile scope, authoritative page projection и финальный live
  proof закрытия.
* Проверяемый product diff: диапазон
  `c490c3d652390954e234cbf8aeda2c666b27cab2^..a223d02a6f251d57c346c5ae135ddb5a4535eec5`.
  Он меняет только пакет `@metafor/hamiltonian`; `@nodes/layout`, `@nodes/ui`,
  generic `nodes`, production-контуры и пакеты transport не изменены.
* В рабочем дереве во время реализации присутствовала отдельная работа
  `NODES-008`; она не входит в перечисленные коммиты и не является частью
  результата MF-428.

### Затронутые домены, владельцы и contracts

* Browser lifecycle, ownership, retained aggregation и orchestration page
  принадлежат [`hamiltonian/README.md`](../../hamiltonian/README.md). Владелец
  дополнен законами browser-local profile identity, ownership-closed snapshot,
  profile-scoped retirement/rebirth и authoritative удаления remote scope.
* Реализующий lifecycle contract находится в `hamiltonian/core/lifecycle.js`:
  добавлены общий экспортируемый валидатор
  `isHamiltonianLifecycleOwnershipClosed` и операция retained journal
  `forgetEntityTree`. Их свойства проверяются в `core/lifecycle.spec.ts` и
  `host.spec.ts`.
* Внутренний Service Worker → host control envelope получил сообщение
  `browser-lifecycle-snapshot`; Worker передаёт целый profile scope, host
  принимает его только при exact browser/Worker identity и ownership closure.
  Реализация находится в `browser/service-worker.ts` и `host.ts`, build marker —
  в `browser-build.spec.ts`.
* Browser-side materialization находится в
  `browser/orchestration/lifecycle-projection.ts`: каждая `browser-runtime`
  entity материализуется отдельной compound-нодой, `profileId` показывается
  справа и в фактах, `parentId` выводится только из доступной owner-chain;
  отсутствующий remote scope удаляется root-first.
* Внешний `hamiltonian/types.d.ts` не менялся: lifecycle attributes остаются
  generic record, а control envelope сейчас является внутренним protocol между
  Worker и host. Closing reviewer должен отдельно подтвердить, что новый
  обязательный envelope не требует public discriminated type; при обратном
  выводе задача не закрывается до согласованного изменения owner document,
  public type, кода и тестов.

### Долговечные изменения и отвергнутые допущения

* Добавлен общий закон для любого browser-owned realm: snapshot пересекает
  границу realm только вместе с полной живой owner-chain. Специальной ветки для
  «второго Service Worker» нет.
* Browser/profile identity — сохранённый origin-local UUID Hamiltonian, а не
  имя Chrome, PID, title, user agent, URL или порядок подключения.
* Restart меняет execution incarnation Worker, но не его logical identity и не
  browser owner. Quit/reopen с сохранённым storage может повторно
  материализовать ту же stable identity после nonterminal retirement.
* Старое неявное допущение «достаточно передать Worker с `ownerId`, даже если
  browser owner отсутствует в snapshot» признано неверным. Также неверно
  безусловно сохранять любую ранее увиденную remote Chrome-ноду при
  authoritative snapshot replacement.
* Layout не определяет предметную принадлежность и не исправляет orphan-ноды;
  он получает уже проверенный `parentId` от Hamiltonian projection.

### Проверки и evidence исполнителя

* Offline выполнены `bun run typecheck`, `bun test hamiltonian/host.spec.ts`
  (`35/35`), `bun test hamiltonian/core/lifecycle.spec.ts` (`23/23`) и
  `bun test hamiltonian/browser/orchestration/lifecycle-projection.spec.ts`
  (`27/27`). Реальные browser entry и Service Worker bundles также собраны
  командами из раздела «Проверки» owner document.
* Pure tests покрывают два profile scope, перестановки observation с frozen
  timestamp, orphan/foreign/cyclic owner-chain, restart/reconnect, immediate
  empty inventory, abrupt close после grace, Web Push/pending-wake guard,
  root-first deletion и повтор stable identity.
* Live evidence и provenance находятся в
  [`project/artifacts/MF-428`](../artifacts/MF-428/README.md). Финальный сценарий
  показал две Chrome/profile-ноды с двумя Worker, browser-managed restart обоих
  execution, затем точное удаление одного profile subtree при сохранении
  второго. Финальный retained snapshot — revision `389`; console exact normal
  target за `1500` мс — `0` записей.
* Итоговый снимок — `surviving-profile-after-close.png`, SHA-256
  `6c69b65c384de4ebf163b4708a49fd74346a6ec810ffd0ac081c88f183c149fa`.

### Обязательный независимый verdict

Reviewer должен не доверять этому handoff как доказательству и самостоятельно:

1. Сверить карточку, оба коммита, owner document, фактический public/internal
   contract и границу `@nodes/layout`.
1. Повторить релевантные offline проверки на exact result commits и проверить,
   что тесты доказывают порядок событий, ownership isolation и retirement, а не
   только выбранные примеры.
1. Проверить provenance live evidence и установить, достаточно ли оно для
   критериев двух одновременно адресованных profile scope.
1. Записать в карточку отдельный verdict с найденными несоответствиями либо
   явным подтверждением document, contracts, code, tests и live evidence;
   зафиксировать verdict коммитом до closing deletion.
1. Только после положительного verdict перенести оставшиеся долговечные выводы
   к владельцам, удалить MF-428 из графа, карточку и всю директорию артефактов,
   разблокировать зависимости и записать отдельный closing commit.

## Независимая closing review — FAIL

Дата review: 12 августа 2026 года. Проверены exact result commits
`c490c3d652390954e234cbf8aeda2c666b27cab2` и
`a223d02a6f251d57c346c5ae135ddb5a4535eec5`, closing handoff
`8417e20fb2ec541e8ccf84977cc4c2df77a577d8` и result HEAD. Verdict: **FAIL**;
карточку, артефакты и запись MF-428 в графе удалять нельзя до исправления и
повторной независимой review.

### Блокирующее несоответствие

Заявленная ownership closure проверяет только owner-chain entity, но не
принадлежность transport. `isHamiltonianLifecycleOwnershipClosed` в
`hamiltonian/core/lifecycle.js` собирает и обходит только entity; поля
`ownerId`, `sourceEntityId` и `targetEntityId` transport не проверяются.
`isBrowserProfileLifecycleSnapshot` в `hamiltonian/host.ts` подтверждает exact
browser и Worker entity, но также не проверяет transport endpoints. Поэтому
host принимает profile snapshot, transport которого пересекает границу другого
browser profile, а lifecycle projection материализует cross-profile edge.

Это нарушает owner document `hamiltonian/README.md`: page, Service Worker и их
transport не должны пересекать границу profile scope, а полученный snapshot
должен быть проверен до retained aggregation. Одновременно не выполнены
критерии карточки о точных transport endpoints и отсутствии cross-profile
edges.

### Независимые probes

* Pure validation probe создал формально корректные browser A, Worker A и page
  A, затем transport с owner Worker A и source page A, но target Worker B.
  Результат: базовый `isHamiltonianLifecycleSnapshot` вернул `true` и
  `isHamiltonianLifecycleOwnershipClosed(snapshot, [browserA])` также вернул
  `true`, хотя endpoint Worker B отсутствовал в scope A.
* End-to-end host probe подключил валидный profile B и описанный вредоносный
  profile A через настоящий `browser-lifecycle-snapshot`. Host принял snapshot,
  не закрыл WebSocket A (`readyState === 1`) и сохранил в retained aggregate
  оба browser root вместе с cross-profile transport A → B.
* Pure projection probe агрегировал эти два profile snapshot и получил прямую
  грань `service-worker-api:cross-profile` от page A к Worker B и обратную грань
  Worker B → page A. При этом parent page A и Worker A оставался browser A, а
  parent Worker B — browser B: нарушение не маскируется ошибкой parentId, это
  именно пропущенная проверка transport boundary.

### Штатные проверки и остальные evidence

На result HEAD независимо прошли:

* `bun run typecheck`;
* `bun test hamiltonian/host.spec.ts` — `35 pass`, `0 fail`;
* `bun test hamiltonian/core/lifecycle.spec.ts` — `23 pass`, `0 fail`;
* `bun test hamiltonian/browser/orchestration/lifecycle-projection.spec.ts` —
  `27 pass`, `0 fail`;
* `bun test hamiltonian/browser-build.spec.ts` — `1 pass`, `0 fail`;
* strict Hamiltonian TypeScript checks и реальные app/embodiment,
  window/worker-entry и Service Worker bundles командами owner document.

Эти зелёные проверки подтверждают заявленные positive paths, restart,
retirement и сборку, но не содержат отрицательного случая cross-profile
transport и поэтому не снимают блокирующее несоответствие. Provenance и
SHA-256 всех шести live PNG из `project/artifacts/MF-428` сверены; изображения
подтверждают две Chrome-ноды, restart и удаление одного profile subtree, но не
доказывают отклонение вредоносного lifecycle envelope.

### Public type

Новый public discriminated type в `hamiltonian/types.d.ts` сейчас не требуется:
пакет `hamiltonian` private, не имеет package exports, `types.d.ts` содержит
только ambient declaration для `multicast-dns`, а
`browser-lifecycle-snapshot` является внутренним control protocol между
собираемыми вместе Service Worker и host. Это не освобождает internal protocol
от полной runtime validation. Если host и Worker станут независимо
версионируемыми либо envelope будет экспортирован потребителям, discriminated
protocol type должен стать public contract.

### Условия повторной review

До повторной независимой closing review необходимо:

1. Валидировать у каждого retained transport его owner и оба endpoint: все они
   существуют в том же snapshot, а их entity owner-chain заканчиваются в exact
   объявленном browser root.
1. Отклонять любой transport с отсутствующим либо cross-profile endpoint до
   retained aggregation и не материализовывать его в node-system document.
1. Добавить отрицательные lifecycle и host contract tests для такого envelope,
   а также projection proof отсутствия прямой и обратной cross-profile грани.
1. Повторить штатные offline проверки и независимые pure/end-to-end probes на
   новом exact result commit; только положительный повторный verdict разрешает
   closing deletion.

## Исправление после FAIL — готово к повторной review

Статус: blocking finding исправлен 12 августа 2026 года в product commit
`27b232837acbf8162a61f85482296d126d0da4d5`. До принятия новых подзадач задача
оставалась в `REVIEW`; сейчас раздел является provenance исправления прежнего
baseline, а родитель возвращён в `READY` для `MF-428.3`–`MF-428.5`.

### Исправленный contract

* `isHamiltonianLifecycleOwnershipClosed` теперь разрешает retained transport
  только если его `ownerId`, `sourceEntityId` и `targetEntityId` существуют как
  entity того же snapshot и owner-chain всех трёх заканчиваются в одном exact
  declared root. Transport между двумя roots отклоняется даже тогда, когда оба
  root перечислены как допустимые для общего snapshot.
* Host продолжает принимать browser/profile snapshot только через этот общий
  validator с единственным exact browser root. Поэтому entity другого profile,
  присутствующая в глобальном retained aggregate, не становится доступным
  endpoint локального profile snapshot.
* Lifecycle projection получила независимый защитный инвариант: transport,
  owner/source/target которого обнаруживают больше одного browser-runtime
  ancestor, не участвует в visibility traversal, не создаёт параметры, порты,
  прямую или обратную грань и не допускает message presentation. Обычный
  browser-owned → server transport остаётся допустимым, потому что server не
  является вторым browser root.
* Долговечный закон exact-scope transport closure добавлен в постоянный owner
  [`hamiltonian/README.md`](../../hamiltonian/README.md). Вывод независимой
  review о том, что отдельный public discriminated type сейчас не требуется,
  принят: `hamiltonian/types.d.ts` не изменялся.

### Regression proof исполнителя

* Lifecycle contract test доказывает positive same-profile transport,
  отклонение отсутствующих owner/source/target и отклонение foreign endpoint
  при двух одновременно declared browser roots.
* End-to-end host test сначала подключает и удерживает валидный profile B,
  затем отправляет из profile A snapshot с target Worker B. Host закрывает
  атакующий socket кодом `1008`; Worker B остаётся в aggregate, а page A и
  cross-profile transport в retained snapshot не попадают.
* Projection test намеренно обходит host boundary и подаёт две Chrome-ноды с
  cross-profile Service Worker API transport. В node-system document нет
  прямой/обратной грани и transport-портов; message presentation возвращает
  `null`.

После исправления прошли:

* `bun run typecheck`;
* `bun test hamiltonian/host.spec.ts` — `36 pass`, `0 fail`, `2051 expect()`;
* `bun test hamiltonian/core/lifecycle.spec.ts` — `23 pass`, `0 fail`;
* `bun test hamiltonian/browser/orchestration/lifecycle-projection.spec.ts` —
  `28 pass`, `0 fail`;
* `bun test hamiltonian/browser-build.spec.ts` — `1 pass`, `0 fail`;
* оба strict Hamiltonian TypeScript-check и реальные app/embodiment,
  window/worker-entry и Service Worker bundles командами owner document.

Повторный независимый reviewer должен проверить exact product commit
`27b232837acbf8162a61f85482296d126d0da4d5`, заново выполнить исходные
pure/end-to-end/projection attack probes и штатные проверки и записать новый
PASS либо FAIL отдельным коммитом до любого closing deletion.

## Артефакты

Исходное визуальное наблюдение и его provenance находятся в
[`project/artifacts/MF-428`](../artifacts/MF-428/README.md).
