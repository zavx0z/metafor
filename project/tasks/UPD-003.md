# UPD-003 — Синхронизировать пакетные сборки по среде и состоянию кэша

## Коротко

Один package можно будет импортировать по одному имени, но собирать отдельно
для Window, browser Worker, Service Worker, Bun server и server Worker. При
обновлении Service Worker покажет host фактическое содержимое своих постоянных
кэшей, получит только необходимые изменения и применит их через одну
восстанавливаемую транзакцию без отдельного active manifest.

## Зачем

Доказанный checkpoint `UPD-002` уже доставляет browser packages по
каноническим package URL и раскладывает их по owner caches, но дальнейшая
проверка владельца открыла четыре связанных ограничения другого договора:

* один package пока имеет только одну browser-сборку и не может явно выбрать
  разные entrypoints для разных сред выполнения;
* env отсутствует в identity cache entry, поэтому разные сборки одного package
  нельзя хранить и проверять независимо;
* host передаёт Worker готовый package state с `endpoint` и `cache`, хотя URL и
  cache owner однозначно выводятся из package name, env и namespace;
* постоянный cache `release` одновременно хранит code и
  `/code?state=active`, а прерванная попытка использует несколько
  `*:release:<uuid>` caches. Из-за этого техническое состояние дублирует
  фактическое содержимое постоянных caches и оставляет лишние storages.

Параллельно действующая host publication пишет child manifests раньше root
manifest. Владелец выбрал обратную модель: root `hamiltonian/package.json`
сначала фиксирует намерение, а после остановки server по нему восстанавливает
несошедшиеся child versions и artifacts.

## Связь с дорожной картой

Это следующий отдельный этап линии
[`Обновления Hamiltonian`](../ROADMAP.md#обновления-hamiltonian) после
[`UPD-002 — Обновлять всю клиентскую сборку через Service
Worker`](UPD-002.md). `UPD-002` остаётся владельцем уже доказанного
checkpoint `startup → release → internal`, package SemVer, canonical package
URL, namespace owner caches, development artifacts и diagnostics.

`UPD-003` меняет не отдельную ошибку этого checkpoint, а следующий package и
transaction contract: env-aware builds, cache-derived current state,
delta-only server response, один transaction cache и root-first recovery.
Поэтому работа вынесена в самостоятельную задачу по прямому решению владельца,
а не добавлена correction-подзадачей `UPD-002`.

## Зависимость

Состояние `IN_PROGRESS`. Прямым поручением владельца от 17 августа 2026 года
задача открыта до формального закрытия `UPD-002`: доказанный checkpoint до
`UPD-002.15` достаточен как основание новой реализации, а незавершённый owner
review прежней задачи не блокирует первый независимый contract-срез.

## Связанные задачи и история

* `LOAD-001` доказала неизменяемый startup, загрузку сменяемого release через
  `fetch` и независимые Cache Storage по namespace.
* `UPD-002.6` ввела групповой package release, точные SemVer, root caret
  dependencies, child-last/root-last publication и временные owner transaction
  caches с UUID.
* `UPD-002.10`–`UPD-002.12` отделили `@internal/visual` от
  `@release/main`, направили его в `internal` cache и удалили завершённые
  технические caches.
* `UPD-002.13` заменила `/code?module=...` на канонические package URL и
  сохранила bare imports через namespace import map.
* `UPD-002.14` связала public types `@internal/visual` непосредственно с source,
  чтобы новый named export был виден потребителю без предварительного build.
* `UPD-002.15` опубликовала единый development contour с inline source maps и
  структурированными owner-scoped diagnostics.
* Применимые result checkpoints: `8fcac82f0`, `0c30318cd`, `944e1be45`,
  `a5304c2b8`, `88631dbf6`. Эти результаты не отменяются: новая задача меняет
  следующий способ описания нескольких env и синхронизации release.

## Подтверждённые факты

На baseline `88631dbf63b1faa862d93881c56290e749a436c7`:

* `@startup/main` выполняет `import("@release/main")`, но не объявляет
  `@release/main` в своих `dependencies`.
* `@startup/service` загружает `@release/service`, но не объявляет его в
  `dependencies`.
* `@release/service` импортирует Loader-типы относительным путём через границу
  package из `../../startup/service/loader`.
* `@internal/visual`, `@release/main`, `@release/service` и startup packages
  имеют по одному build entrypoint и одному `dist/index.js`.
* `artifact.cache` дублирует cache owner, уже выводимый из namespace
  `@startup`, `@release`, `@internal` или `@metafor`.
* Server кеширует найденный `PackageOwner` вместе со значениями `build`,
  `cache` и `headers`, прочитанными из `package.json`; последующее изменение
  manifest не видно этому объекту без перезапуска процесса.
* Browser release state содержит передаваемые host поля `endpoint` и `cache`.
* Постоянный `release` cache хранит `/code?state=active` и
  `/code?state=pending` рядом с code responses.
* Одна попытка обновления создаёт отдельные
  `<owner>:release:<crypto.randomUUID()>` caches.
* Host сейчас сначала записывает новые exact versions в child manifests, затем
  последним записывает root caret dependencies.

## Решения владельца

### 1. Одно имя package, разные среды выполнения

Source во всех средах использует только bare package specifier, например:

```ts
await import("@internal/visual")
```

Env не добавляется в import specifier и не становится package subpath.
Одинаковый specifier должен разрешаться в нужный source entrypoint выбранной
средой сборки.

Package объявляет entrypoints стандартным полем `exports` корневого subpath
`"."`. Имена сред являются project conditions внутри стандартного conditional
exports mechanism; отдельное custom-поле manifest для artifact type не
создаётся:

```json
{
  "exports": {
    ".": {
      "metafor:main": {
        "types": "./main.ts",
        "browser": "./main.ts"
      },
      "metafor:worker": {
        "types": "./worker.ts",
        "browser": "./worker.ts"
      },
      "metafor:service-worker": {
        "types": "./service-worker.ts",
        "browser": "./service-worker.ts"
      },
      "metafor:server": {
        "types": "./server.ts",
        "bun": "./server.ts"
      },
      "metafor:server-worker": {
        "types": "./server-worker.ts",
        "bun": "./server-worker.ts"
      }
    }
  }
}
```

Package объявляет только реально поддерживаемые им branches. Обязательного
`default` fallback нет: сборка в неподдерживаемой или неуказанной среде должна
завершиться ошибкой разрешения, а не молча выбрать чужой entrypoint.

Текущие канонические env:

| Env | Место исполнения | Build target |
| --- | ---------------- | ------------ |
| `main` | Window main realm | `browser` |
| `worker` | browser Dedicated Worker | `browser` |
| `service-worker` | browser Service Worker | `browser` |
| `server` | основной Bun server process | `bun` |
| `server-worker` | Worker, созданный Bun server | `bun` |

`server` и `server-worker` различаются обязательно: одинаковый Bun target не
означает одинаковый lifecycle, globals или entrypoint. Будущие точные среды
вроде `shared-worker`, `audio-worklet` и `paint-worklet` можно добавить новыми
conditions; эта задача их не вводит.

Builder выбирает ровно одну project condition
`--conditions=metafor:<env>` и соответствующий target. TypeScript package check
использует ту же condition через `customConditions`. Если package имеет
несколько env entrypoints, каждый env является отдельной build и typecheck
единицей; одна резолюция не пытается одновременно выбрать несколько branches.

### 2. Версия и identity env artifact

SemVer принадлежит package целиком, а не отдельному env. Если изменился хотя бы
один опубликованный env package, package получает новую версию. Для этой версии
должны существовать все объявленные и входящие в выпуск env artifacts; env с
неизменившимися bytes можно безопасно переиспользовать при публикации новой
версии, но нельзя оставить выпуск частично собранным.

Package, который не изменялся, не получает новую версию, не пересобирается и не
перекладывается в browser cache. Полная composition нужна для сравнения и
удаления, а не для механической замены неизменившихся entries.

Точная identity одного artifact:

```text
package name + env + package version + SHA-256 + byte size
```

Для browser-delivered artifact pathname остаётся одним каноническим package
path. Env передаётся query parameter, а не сегментом пути:

```text
стабильный: /@internal/visual?env=main
точный:     /@internal/visual?env=main&version=0.1.3
```

Один общий helper обязан одинаково строить и разбирать URL на server, в
Service Worker и в tests. Порядок и набор query parameters каноничны; разные
строки не должны обозначать одну identity.

Browser Cache Storage применяется только к browser env
`main | worker | service-worker`. `server` и `server-worker` входят в package
resolution/build law, но их живые Bun processes и artifacts не становятся
browser cache entries и не обновляются Service Worker в этой задаче.

### 3. Никаких передаваемых endpoint, cache owner и дополнительных ID

`endpoint` не хранится и не передаётся в package state, RPC request, RPC
response, transaction intent или server plan: он всегда выводится общим URL
helper из package name, env и version.

`artifact.cache` удаляется из package manifests и public contracts. Owner
постоянного cache выводится из namespace:

* `@startup/*` → `startup`;
* `@release/*` → `release`;
* `@internal/*` → `internal`;
* `@metafor/*` → `metafor` после отдельного включения этой namespace.

Дополнительный `artifact.type` не вводится. `artifact.headers` может остаться
только для HTTP delivery properties, которые нельзя вывести из env или
namespace; headers не являются маркером среды.

Отдельные `releaseId`, `generation`, transaction UUID и Worker incarnation для
этого release protocol не вводятся. Package versions, env, digest, фактическое
содержимое canonical caches и наличие фиксированного transaction cache дают
все необходимые признаки.

### 4. Полный desired state принадлежит server, фактический current state — Worker

После успешной host publication server отправляет по существующему release RPC
только сигнал `release-changed` без package list, versions, endpoints и старого
state. Несколько сигналов разрешено объединить: сигнал означает только
«проверь текущее состояние сейчас» и поэтому не может нести устаревший release.

При сигнале, новом RPC connection и восстановлении прерванной транзакции
Service Worker динамически собирает полный current state из постоянных
canonical code caches и отправляет server по одной записи на фактически
найденный browser artifact:

```text
package name, env, version, SHA-256, byte size
```

Server читает актуальный root intent, child manifests, dependency graph и
published artifacts, сравнивает их с current state Worker и отвечает только
операциями:

```text
update: package name, env, version, SHA-256, byte size
remove: package name, env, version
```

Полный `desired` list не возвращается. Unchanged entries в ответ не входят.
URL, cache owner, release ID и transaction ID не входят ни в одну операцию.
Server не хранит и не replay-ит прежние package-state notifications; после
reconnect всегда выполняется новая сверка с текущими root и caches.

Server определяет removal сравнением полного желаемого состава и dependency
closure с полным current state Worker. Удаление package не требует отдельного
вида release и не заставляет заново заменять остальные packages.

### 5. Одна фиксированная browser-транзакция

На origin существует не более одного технического Cache Storage с точным
именем `transaction`. Owner-specific `release:<uuid>` caches не создаются.

Первой операцией обновления Worker открывает `transaction` и сразу записывает
marker под единственным служебным ключом `/transaction`. Marker не содержит
server delta, desired composition, endpoint, cache owner или дополнительный ID.
Только после этой записи разрешено начинать network fetch и записи code
responses. После остановки release получает fresh delta заново из фактического
canonical current.

Успешный порядок:

1. Worker получает server delta.
2. Worker первой записью сохраняет пустой marker
   `transaction[/transaction]`.
3. Worker fetch-ит только `update` artifacts и проверяет package name, env,
   version, SHA-256 и byte size.
4. Worker сохраняет подготовленные responses в `transaction`; неизменившиеся
   canonical entries не копируются.
5. После полной staging Worker переносит все новые exact entries в namespace
   owner caches, не удаляя ни одной старой entry.
6. Worker повторно проверяет, что весь новый состав находится в canonical
   caches и полностью соответствует identity. До этой проверки полный старый
   состав остаётся доступен; после остановки дальнейшее действие определяется
   только новой сверкой фактического current с актуальным server state.
7. Только после доказательства полного нового состава Worker начинает
   необратимый forward cleanup старых и удаляемых entries. Старый
   `@hamiltonian/release:service-worker` удаляется последним.
8. Во время cutover для одного `(package, env)` разрешено временное
   сосуществование old и new exact entries. После успешной transaction во всех
   canonical owner caches остаётся ровно одна exact entry на slot.
9. Worker проверяет итоговый canonical состав и последней durable операцией
   удаляет Cache Storage `transaction`.
10. После commit Worker выполняет один предусмотренный runtime handover и
    reload управляемых Window без удаления Service Worker registration.

Постоянного `/code?state=active` и `/code?state=pending` больше нет ни в
`release`, ни в другом code cache. Действующий current state собирается из
canonical entries динамически. Наличие `transaction` означает незавершённую
операцию, но startup не читает intent, не ждёт transaction и не выбирает
recovery executor по её содержимому. До первой old deletion canonical caches
содержат полный старый состав; после первой old deletion они уже содержат
полный новый состав. Поэтому после аварии release может либо отменить ещё не
начатый cutover, либо только завершить начатый cleanup вперёд.

При self-update новая exact service-worker entry добавляется после старой.
Пока old entry существует, она остаётся первым boot candidate фиксированного
service-worker slot в порядке Cache; SemVer, active pointer и безверсийная
копия bytes не нужны. Old service-worker entry нельзя повторно `put`, потому
что это переместит её в конец. После её последнего удаления boot candidate
становится new release; соседняя версия release обязана уметь проверить уже
завершённый canonical cutover и удалить оставшуюся transaction.

Если Worker остановлен после любой промежуточной записи, новый startup
берёт первую exact entry фиксированного service-worker release slot в порядке
Cache и полностью проверяет именно её, не читая transaction. Если первая entry
повреждена, startup завершается fail closed, а не пропускает её и не запускает
следующий candidate преждевременно. Запущенный release первым действием
локально восстанавливает, отменяет либо завершает свою transaction и только
после этого открывает RPC. Частично подготовленные bytes можно переиспользовать
только после повторной проверки exact identity. Успех определяется отсутствием
`transaction`, поэтому UUID и incarnation для восстановления не нужны.

Разные Chrome profiles не делят Cache Storage origin между собой: каждый
profile независимо имеет собственные canonical caches и не более одной своей
транзакции.

### 6. Root manifest сначала фиксирует host-намерение

`hamiltonian/package.json` является единственным host intent и recovery source.
Отдельные release manifest, journal, `generation` и `releaseId` не создаются.

Обычная публикация выполняется в таком порядке:

1. Host перечитывает manifests, валидирует входной request, полный membership и
   dependency graph, затем вычисляет target package versions.
2. Host сохраняет прежние root dependency values в памяти текущей операции и
   атомарно записывает target caret versions в root
   `hamiltonian/package.json` до typecheck, build и artifact publication.
3. Host выполняет необходимые checks/builds только для изменяемых packages;
   для новой версии package материализует все его опубликованные env artifacts.
4. После полного успеха artifacts host атомарно обновляет exact versions только
   изменившихся child package manifests.
5. Host перечитывает root, children и artifacts и доказывает их схождение,
   включая digest и size каждого env.
6. Только после схождения server открывает новый state для delivery и отправляет
   один `release-changed` signal.

Если обычная операция завершилась ошибкой в том же процессе, host атомарно
возвращает прежние root dependency values, сохраняет прежние child versions и
published state, возвращает caller текущий failure response с результатами и
не отправляет сигнал.

После аварийной остановки server до открытия package routes сравнивает root
intent с child exact versions и immutable env artifacts. Любое расхождение
считается незавершённой host-транзакцией: необходимые checks, builds,
publication и child writes выполняются заново до схождения. Пока состояние не
сошлось, server не выдаёт его Worker и не отправляет `release-changed`.
Неустранимая recovery error оставляет видимое root intent и явную диагностику;
server не выдумывает прежнее намерение и не создаёт второй journal.

Root write не делает несколько файлов физически атомарными. Атомарность
результата обеспечивается ранним durable intent, повторяемыми действиями,
запретом delivery несошедшегося state и финальной проверкой.

### 7. Dependency graph и границы типов проверяются до публикации

Перед root intent write host проверяет полный runtime composition текущих
`@release/*` и `@internal/*` packages:

* каждый runtime dependency release package присутствует в root membership;
* выбранная root version удовлетворяет dependency range потребителя;
* новый package можно добавить только вместе с валидной dependency closure;
* package нельзя удалить, пока он нужен оставшемуся package;
* исключённый package и все лишние env entries попадут в browser `remove`;
* package manifest перечитывается перед каждой сборкой и публикацией; кешировать
  разрешено только найденный package root и путь к `package.json`, но не его
  изменяемое содержимое.

`@startup/main` обязан объявить dependency на `@release/main`, а
`@startup/service` — на `@release/service`, потому что они фактически загружают
эти packages.

Public Loader contract переносится из startup package в `@release/service`.
Release определяет требуемый ему интерфейс, startup импортирует этот type через
объявленную package dependency и предоставляет реализацию. Относительные
type-imports через границу `web/release → web/startup` удаляются.

Source-linked public types сохраняют закон `UPD-002.14`: изменение export
package видно зависимому typecheck без предварительного artifact build.

### 8. Immutable env artifacts проверяются по bytes

Для каждого опубликованного `(package, env, version)` server вычисляет SHA-256
и byte size. Worker вычисляет их по полученному body и принимает response
только при полном совпадении server delta, canonical URL и identity headers.

Если exact artifact отсутствует, server возвращает ошибку и не запускает
ленивый build под уже объявленной exact version. Уже существующий exact target
можно переиспользовать только при совпадающих bytes; другой digest под той же
package version запрещён. После схождения root, child manifest и artifacts
package version обозначает фиксированный набор env bytes.

## Поведение процесса

* Сначала новый договор переносится в постоянный документ-владелец Hamiltonian
  и public types; только затем меняются host и Worker mechanisms.
* Каждый следующий срез изменяет один механизм и получает отдельную подзадачу,
  preparatory project commit, focused regressions и result checkpoint.
* В `UPD-003.1` оставленный development contour `UPD-002` не изменяется;
  последующие срезы заменяют его механизмы только после отдельного
  preparatory project commit.
* При обнаружении требования к обновлению живого Bun process, новому transport,
  `@metafor/*` или package resources работа останавливается и предъявляется как
  отдельное расширение, а не включается молча.

## Подзадачи

### UPD-003.1 — Зафиксировать единый env-контракт package

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента.

Классификация: первый contract-срез нового самостоятельного package update
договора; не исправляет code checkpoint `UPD-002.15` и не меняет runtime.

Требование: постоянный владелец Hamiltonian и development guide должны
однозначно закрепить список env, conditional `exports`, bare imports,
source-linked types, один package SemVer, отдельные build/typecheck единицы и
отсутствие `default` fallback.

Основание и связанная история: `UPD-002.13` доказала один канонический package
URL и bare import, `.14` — source-linked public types. Новый owner-approved
контракт сохраняет эти результаты, но различает несколько entrypoints одного
package по среде сборки.

Наблюдаемое расхождение: Hamiltonian README и development guide сейчас
описывают один browser entrypoint, один `dist/index.js`, `artifact.cache` и URL
без env. Они не различают `main`, `worker`, `service-worker`, `server` и
`server-worker` и противоречат принятому следующему направлению.

Причина: прежний договор был намеренно ограничен одним browser artifact на
package и не имел project conditions в standard `exports`.

Разрешённое изменение одного механизма: изменить только постоянный
Hamiltonian package law и его development guide. Package manifests, public
types, builder, server, Worker, artifacts, versions и live contour не менять.

Regression или опровергающее доказательство: документация одновременно
сохраняет bare package specifier и требует явный `metafor:<env>` без default;
`server` и `server-worker` перечислены отдельно; browser URL содержит env в
query, а не в pathname. `git diff --check` проходит, product files отсутствуют
в diff.

Среда и критерий приёмки: canonical `main`, baseline `3f71e76bf`; перечитать
документы и diff, не запускать build/runtime. После result checkpoint `.2`
может реализовывать уже закреплённый build contract.

Фактические действия:

* Hamiltonian owner contract закрепил один bare package name, единый SemVer,
  пять точных env, project conditions `metafor:<env>`, отсутствие `default` и
  отдельную build/typecheck единицу каждого объявленного env;
* browser URL сохранил один package pathname, а env получил обязательный query
  перед exact version;
* development guide получил тот же conditional `exports` пример, таблицу
  `main | worker | service-worker | server | server-worker` и правила сборки,
  типов и version bump без изменения manifests или runtime.

Результат и вывод:

Постоянный документ-владелец и development guide больше не трактуют package как
единственный browser entrypoint. Bare `@internal/visual` сохранён, env
выбирается только build/typecheck condition и browser URL query; `server` и
`server-worker` разделены. Локальный Bun `1.3.14` подтвердил поддержку
`bun build --conditions=<val>`, TypeScript имеет версию `6.0.2` и поддерживает
`customConditions`. `git diff --check` прошёл; product code, manifests,
artifacts, versions и live contour не изменялись.

Подготовительный commit: `16c98e0a7`.

Result checkpoint: этот commit.

### UPD-003.2 — Собирать и проверять каждый объявленный env отдельно

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `6509a4a2e` среза `.1`.

Классификация: следующий independently observable mechanism того же package
env contract. Документ-владелец уже принят в `.1`; теперь меняются manifests,
package discovery, build и typecheck, но не HTTP/RPC/cache identity.

Требование: каждый package объявляет поддерживаемые env через conditional
`exports`, а server запускает отдельные package-owned `typecheck:<env>`,
`prebuild:<env>` и `build:<env>` с точной `metafor:<env>` condition. Manifest
перечитывается перед каждой операцией; package names не записываются в registry.

Основание и связанная история: `.1` закрепила пять env, bare self-specifier,
один package SemVer, no-default resolution и отдельную единицу каждого env.
`UPD-002.4` сохраняет production command как источник development profile,
inline source map и удаление `console.debug` только в production.

Наблюдаемое расхождение: manifests всё ещё имеют `default` или строковый
`exports`, одну общую `typecheck/prebuild/build` команду с source path и без
condition; `packageOwner` кеширует изменяемые значения manifest и находит root
через default package resolution.

Причина: прежний builder обслуживал ровно один entrypoint package и поэтому
считал всю build-конфигурацию одним неизменным `PackageOwner`.

Разрешённое изменение одного механизма:

* перевести пять browser packages и `@release/server` на conditional exports;
* в browser packages ввести env-specific scripts; source import остаётся bare,
  а CLI build entrypoint обязан точно совпадать с source path выбранного branch
  exports и condition;
* package discovery кеширует только real root и manifest path, но перечитывает
  содержимое и проверяет env contract при каждом build/state read;
* `buildPackage` и его results получают env; одинаковые pending builds
  схлопываются по `(package, env, output)`;
* package-owned outfile сохраняется. Несколько env одного package обязаны иметь
  разные outfile, но фиксированный `dist/<env>` layout не навязывается;
* до `.3` существующий HTTP route без env временно принимает только package с
  одним browser env и внутренне выбирает его. Новый env query и URL identity в
  этом срезе не вводятся.

Regression или опровергающее доказательство: manifests не содержат `default`;
каждая текущая browser build выбирается своей condition и сохраняет прежний
production/development профиль; synthetic manifest с `server` и
`server-worker` возвращает две разные build единицы; изменение manifest после
первого resolve видно следующему read; неизвестный/unsupported env отклоняется.

Подтверждённое ограничение реализации: Bun `1.3.14` поддерживает custom
conditions, но не разрешает bare package specifier как CLI entrypoint `bun
build`: проба `bun build @release/main --conditions=metafor:main ...`
завершилась `FileNotFound opening root directory "@release"`. Поэтому source
path присутствует в `build:<env>` повторно и принимается только при точном
совпадении с conditional export; это не меняет bare imports внутри source и
готового artifact.

Среда и критерий приёмки: canonical `main`, implementation baseline
`6509a4a2e`; strict package typechecks, focused build/profile/boundary tests,
production и development artifacts, полный Hamiltonian suite и
`git diff --check`. Runtime и browser не запускать, versions и immutable
artifacts не публиковать.

Фактические действия:

* `@startup/main`, `@startup/service`, `@release/main`, `@release/service`,
  `@release/server` и `@internal/visual` получили conditional root exports без
  `default`, env-specific scripts и совпадающие TypeScript
  `customConditions`;
* `@release/server` и корневой Bun contour запускаются с
  `metafor:server`; root TypeScript также явно проверяет server environment;
* public contracts получили пять допустимых env и env в build request/result;
* package discovery больше не использует default `Bun.resolveSync`: real root и
  manifest path находятся через workspace symlink, кешируются как location, а
  изменяемый manifest перечитывается и полностью проверяется при каждом read;
* parser требует ordered `types + browser|bun`, точное совпадение build source
  path, condition и target, существующие source files и уникальный outfile;
* pending builds разделены по package, env и output; до `.3` старый HTTP route
  внутренне выбирает единственный browser env package;
* development guide синхронизирован с env-specific scripts, Bun CLI boundary,
  fresh manifest read и package-owned outfile.

Результат и вывод:

Один package contract теперь действительно может объявить несколько env без
package-name registry; `server` и `server-worker` разбираются как разные build
units, unsupported env и `default` отвергаются. Все шесть текущих packages
собираются прямыми production-командами, а browser artifacts сохраняют bare
imports, development diagnostics и inline source maps.

Проверки:

* strict typecheck всех шести package environments прошёл;
* `bun run build` в Hamiltonian собрал шесть production artifacts, включая
  новый `@release/server` env;
* focused build/profile/boundary suite: `10 pass`, `0 fail`, `212 expect()`;
* test-contour TypeScript прошёл;
* первый полный Hamiltonian run дал один timing failure прежнего browser
  restart-сценария при `259 pass`; тот же сценарий изолированно прошёл, а
  повторный полный run завершился `260 pass`, `0 fail`, `3551 expect()`;
* общий root TypeScript не имеет новых ошибок и останавливается на семи ранее
  записанных ошибках старого `hamiltonian/update`;
* `git diff --check` и no-default audit прошли; package versions, immutable
  artifacts, `bun.lock` и live contour не изменялись.

Подготовительные commits: `386f04bef`, `03ca0d6f6`.

Result checkpoint: этот commit.

### UPD-003.3 — Сделать env частью URL и artifact identity

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `9573865d1` среза `.2`.

Классификация: следующий independently observable delivery mechanism уже
реализованного env-aware build contract. Срез меняет точную identity одного
browser artifact, но не меняет способ вычисления release delta, host
publication order или browser transaction lifecycle.

Требование: один общий helper строит и строго разбирает canonical browser URL
`/<package>?env=<env>[&version=<semver>]`; package state содержит только
`name`, `env`, `version`, `sha256` и `size`. HTTP response доказывает те же
поля. Cache owner всегда выводится из namespace, а `endpoint` и
`artifact.cache` отсутствуют в manifests и public contracts.

Основание и связанная история: `.1` закрепила URL с env query и exact artifact
identity, `.2` дала build/result выбранный env. `UPD-002.13` уже доказала
канонический package pathname и namespace routes, но оставила URL только с
version и передаваемые `endpoint/cache`.

Наблюдаемое расхождение: текущий URL helper не принимает env; server и Worker
передают готовые endpoint/cache; package manifests повторяют namespace owner в
`artifact.cache`; immutable artifact и HTTP response не несут SHA-256, а
Worker проверяет только name/version.

Причина: прежняя package identity имела ровно один browser artifact на package,
поэтому `(name, version)` считалась достаточной, а cache owner и endpoint были
сохранены как готовые поля предыдущего transaction protocol.

Разрешённое изменение одного механизма:

* ввести общий browser env/URL contract с единственным canonical порядком
  query `env`, затем `version`, и запретом неизвестной среды, лишних или
  повторных параметров;
* добавить SHA-256 к build artifact, published state и HTTP identity headers;
* передавать и хранить package identity без endpoint/cache, вычисляя URL и
  namespace owner локально в server/startup/release code;
* удалить `artifact.cache` из всех Hamiltonian package manifests и public
  manifest/owner contracts, сохранив только необязательные delivery headers;
* сохранить действующие RPC full-state notification и active/pending/UUID
  cache mechanisms до `.6` и `.7`; они используют новый identity, но их
  lifecycle в этом срезе не переписывается;
* назначить новые patch versions всем packages, чьи manifests, build contract
  или исполняемые bytes изменены с `.2`/`.3`, и материализовать новые exact
  browser artifacts. Прежние immutable version files не изменять.

Regression или опровергающее доказательство: stable и exact URL round-trip
только в canonical форме; переставленные, повторные, лишние params и
server/server-worker browser delivery отклоняются; `@release/main` и
`@internal/visual` имеют разные namespace owners без manifest field; exact
response совпадает по name/env/version/digest/size, а изменённые bytes или
headers отвергаются Worker; старые `endpoint`, public `cache` и
`artifact.cache` отсутствуют.

Среда и критерий приёмки: canonical `main`, implementation baseline
`9573865d1`; focused URL/state/delivery/Worker tests, strict affected-package
typechecks, production/development builds, полный Hamiltonian suite и
`git diff --check`. Package publication использует пока действующий
serialized publisher без изменения его root-last order; live contour не
изменять.

Уточнение по наблюдаемому переходу: первый fixture browser run получил `404`
при регистрации `@startup/service`, потому что manifest всё ещё объявлял
`0.1.2` и server правильно отдал старый immutable `0.1.2` startup main, где
URL Service Worker ещё не содержал env. Это доказывает, что новый contract
нельзя открыть под прежними package versions или исправлением старых versioned
files; необходимы новые patch versions и новые exact artifacts в границе этого
же identity-среза.

Фактические действия:

* общий `package-environment` разделил пять package env и три browser env, а
  единый URL helper строит и разбирает только
  `/<package>?env=<env>[&version=<semver>]` в canonical query order;
* общий integrity contract вычисляет SHA-256/byte size, формирует пять identity
  headers и повторно хеширует response body в Worker;
* server state теперь возвращает только `name`, `env`, `version`, `sha256` и
  `size`; exact response не запускает ленивую сборку отсутствующей уже
  опубликованной версии;
* active/pending protocol до `.7` сохранён, но endpoint и cache owner удалены
  из его metadata: stable slot, exact URL и namespace owner вычисляются
  локально на startup/release сторонах;
* `artifact.cache` удалён из пяти browser manifests и public package owner;
  Service Worker delivery headers остались единственными artifact metadata;
* static import map, startup main, Service Worker registration и release loader
  используют env-specific URLs; server/server-worker browser URL отклоняются;
* publisher собирает и материализует каждый объявленный env изменённого package
  под одним новым SemVer, а versioned filename выводит из package-owned
  outfile, поэтому разные env не сталкиваются;
* опубликованы patch versions `@startup/main@0.1.3`,
  `@startup/service@0.1.3`, `@release/main@0.1.6`,
  `@release/service@0.1.6`, `@internal/visual@0.1.4` и
  `@release/server@0.1.1`; прежние versioned files не изменялись.

Результат и вывод:

Три действующих browser release artifacts доказаны как:

* `@release/main:main@0.1.6` —
  `4575a656092297853dc3c6199ade0a5c1e8a70dc7dcb46786775eae02339d933`,
  `300` bytes;
* `@release/service:service-worker@0.1.6` —
  `7ace41e737ef6e9afe90e276e8ae86d730ffb702c51ce6783d119d0a64c27a85`,
  `9750` bytes;
* `@internal/visual:main@0.1.4` —
  `ae30e7d8a73afda2463883d52010ad79825edf3d6531755cf3c74a3cd48fef12`,
  `243849` bytes.

Первый browser run воспроизвёл ожидаемую защиту immutable identity: прежний
startup main `0.1.2` запросил no-env Service Worker URL и получил `404`. После
новых patch versions тот же group update/restart прошёл. Это подтверждает, что
SemVer здесь является частью работающего перехода, а не декоративной metadata.

Проверки:

* strict typecheck всех шести package env и test-contour TypeScript прошли;
* production build всех шести packages прошёл; development profile проверен
  focused build test;
* focused URL/state/build/boundary suite прошёл `16 pass`, `0 fail`, затем
  новый URL/integrity regression также прошёл;
* полный `load-001.browser` набор прошёл все шесть update, partial failure,
  reconnect, migration, offline restore и retry scenarios;
* полный `bun test hamiltonian --timeout 180000` прошёл `262 pass`, `0 fail`,
  `3522 expect()`;
* exact responses всех трёх release artifacts повторно прошли header и
  фактическую SHA-256/size проверку;
* общий root TypeScript не имеет новых ошибок и останавливается на тех же семи
  ранее записанных ошибках старого `hamiltonian/update`;
* `git diff --check`, no-`artifact.cache`, no-default и unchanged-`bun.lock`
  audits прошли. Live managed contour не запускался и не является acceptance
  этого result checkpoint.

Подготовительные commits: `587af656f`, `5bcf0c58a`.

Result checkpoint: этот commit.

Один срез вводит общий URL helper, canonical `?env=...&version=...`, SHA-256 и
size, удаляет передаваемый `endpoint` и выводит cache owner из namespace вместо
`artifact.cache`.

### UPD-003.4 — Проверять membership, dependencies и public type boundaries

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `d47e89e96` среза `.3`.

Классификация: independently observable composition mechanism поверх уже
доказанных env builds и artifact identity. Срез проверяет package graph и
public type ownership, но не меняет root intent order, recovery или browser
delta/transaction.

Требование: root caret dependencies задают полный browser release membership.
Каждая runtime dependency `@release/*`/`@internal/*` участника присутствует в
membership и её выбранная version удовлетворяет workspace range. Тот же закон
проверяется для целевых versions до build. Startup manifests объявляют
фактические release dependencies; Loader interface принадлежит
`@release/service`, startup предоставляет реализацию через type-only bare
import.

Основание и связанная история: owner decision 7 и подтверждённые факты
карточки; `.2` уже гарантирует свежее чтение manifests, `.3` — env-specific
package identity. `UPD-002` намеренно не проверяла транзитивный composition
graph и оставляла Loader type относительным импортом через package boundary.

Наблюдаемое расхождение: `readReleasedPackages` перечисляет root caret entries,
но не проверяет зависимости между ними; major bump dependency можно начать до
обнаружения несовместимости. `@startup/main` и `@startup/service` не объявляют
загружаемые release packages, а `@release/service` импортирует Loader type из
`../../startup/service/loader`.

Причина: прежний publisher принимал только список уже существующих packages и
считал root membership достаточным, а Loader contract исторически определялся
реализацией неизменяемого startup.

Разрешённое изменение одного механизма:

* ввести чистую проверку dependency graph для current и target membership с
  `workspace:*` и точной caret-SemVer семантикой;
* current state отвергает отсутствующего участника, incompatible range и
  browser member без browser env; target versions проверяются до build;
* ручное addition через root принимается только при полном child manifest и
  closure; ручной removal требуемого package отвергается тем же missing-member
  законом;
* добавить реальные startup dependencies на текущие release versions;
* определить минимальный public Loader interface в `@release/service`, удалить
  относительные imports release → startup и проверить startup implementation
  через type-only bare dependency;
* назначить новые patch versions изменённым packages и не изменять старые
  immutable artifacts.

Regression или опровергающее доказательство: valid closure и addition проходят;
missing member, incompatible caret target, forbidden removal и server-only
member отвергаются; major target dependency не доходит до build. Source audit
не находит относительного import через package boundary, а named Loader type
виден startup typecheck без предварительного build.

Среда и критерий приёмки: canonical `main`, implementation baseline
`d47e89e96`; focused graph/type tests, strict package typechecks, production
build, полный Hamiltonian suite и `git diff --check`. Live contour не менять;
root-first publication остаётся `.5`.

Один срез валидирует полный runtime dependency graph, добавляет фактические
startup dependencies и переносит Loader contract в `@release/service`.

Фактические действия:

* server получил единый `readReleaseComposition` и чистую проверку current и
  target graph; root caret entries являются membership, а server-only package
  не может стать browser member;
* runtime dependencies `@release/*`/`@internal/*` проверяются на presence и
  `workspace:*`/caret compatibility; target versions проверяются до build;
* synthetic regressions доказали valid independent addition, missing-member
  removal, incompatible `0.x` target, target вне membership и server-only env;
* `@startup/main` объявил `@release/main`, `@startup/service` —
  `@release/service` с актуальными caret ranges;
* минимальный `ReleaseLoader` определён и публично экспортирован
  `@release/service`; release использует локальный contract, startup проверяет
  реализацию type-only bare import;
* относительные imports из release в startup удалены, source-linked type виден
  без предварительного artifact build;
* опубликован `@release/service@0.1.7` с exact SHA-256
  `0434ed0e0671f122ce5f7edc3ea9879faf06d2101fdc659a139d86bbd4dfbbf8`
  и size `9832`; неизменившиеся `@release/main@0.1.6` и
  `@internal/visual@0.1.4` не публиковались повторно;
* package contracts startup подняты до `0.1.4`, `@release/server` — до
  `0.1.2`; `bun.lock` намеренно не менялся.

Результат и вывод:

Current membership состоит из трёх browser packages. `@release/main@0.1.6`
требует `@internal/visual` по `workspace:^0.1.0`, выбранная `0.1.4`
удовлетворяет range; release service не имеет runtime package dependencies.
Неполный либо несовместимый состав теперь останавливается до build и до
будущего root intent write. Loader type принадлежит потребителю, а startup
остаётся только реализацией этого public договора.

Проверки:

* strict typechecks `@release/server`, `@release/service`, обоих startup
  packages и test contour прошли;
* focused graph/type/boundary suite: `10 pass`, `0 fail`, `155 expect()`;
* production build всех шести packages прошёл;
* полный `bun test hamiltonian --timeout 180000`: `263 pass`, `0 fail`,
  `3558 expect()`;
* current composition и три exact HTTP artifacts повторно перечитаны и
  проверены по фактическим digest/size;
* root TypeScript остановился только на тех же семи известных ошибках старого
  `hamiltonian/update`; новых ошибок нет;
* `git diff --check`, no-relative-import и unchanged-`bun.lock` audits прошли.
  Live contour не запускался.

Подготовительный commit: `27d6b4249`.

Result checkpoint: этот commit.

### UPD-003.5 — Восстанавливать host-публикацию по раннему root intent

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `4b71b61f9` среза `.4`.

Классификация: новый host transaction/recovery mechanism поверх доказанных
env builds, immutable identity и dependency graph. Срез меняет порядок durable
writes и cold-start gate, но не меняет RPC notification payload или browser
cache transaction.

Требование: после полной preflight-проверки root dependencies атомарно получают
target caret versions до typecheck/build/artifact/child writes. Обычная ошибка
в живом процессе возвращает прежний root и прежние child versions. После crash
root остаётся единственным намерением: до `Bun.serve` server заново собирает
все env, материализует missing exact artifacts, сравнивает существующие exact
bytes и доводит child manifests вперёд либо fail-stop без routes.

Основание и связанная история: owner decision 6; `.2` дала fresh manifests и
all-env builds, `.3` — immutable digest/size и запрет lazy exact build, `.4` —
preflight current/target dependency closure. Прежний publisher пишет children
и только затем root, поэтому crash не оставляет durable target intent.

Наблюдаемое расхождение: `publishVersions` сейчас обновляет child manifests
первым и root последним; `server.ts` открывает listener без recovery. Existing
versioned target без сравнения может быть перезаписан через `rename`, а cold
start проверяет только наличие exact file, не совпадение с воспроизводимой
сборкой source.

Причина: прежняя атомарность строилась на in-process rollback и root-last
commit point; после process death отдельного durable направления восстановления
не было.

Разрешённое изменение одного механизма:

* разделить root intent composition и converged composition: intent разрешает
  child mismatch, но уже валидирует target graph; delivery требует полного
  совпадения root/children/artifacts;
* normal publication сохраняет прежний root source, первой durable записью
  пишет target caret dependencies, затем строит/публикует all-env artifacts и
  последними обновляет только изменённые child versions;
* build/typecheck failure и перехваченная normal error атомарно возвращают root
  и уже записанные children; непоказанные target artifacts могут остаться
  только как immutable unreferenced files;
* cold recovery до listener заново строит все root members: совпадающий exact
  artifact переиспользуется, missing публикуется, другой digest/size под тем же
  identity вызывает fail-stop и не перезаписывается;
* recovery следует root вперёд и не откатывает intent при ошибке; повторный
  cold start повторяет идемпотентные действия без отдельного journal;
* signal отправляется существующим кодом только после converged result;
  incomplete state недоступен `/code` и package routes, потому что listener ещё
  не открыт либо publication queue не завершена;
* назначить patch versions изменённым publisher/server packages; browser
  release package получает новую version только при изменении его own contract.

Regression или опровергающее доказательство: write-order test видит root до
build/child; build failure восстанавливает exact прежний root; interruption
после root, после одного artifact и после одного child сходится вперёд;
existing equal artifact reuse проходит, conflict не меняет target; server
bootstrap вызывает recovery до `Bun.serve`; notification отсутствует при
failure/recovery error.

Среда и критерий приёмки: canonical `main`, implementation baseline
`4b71b61f9`; focused publication/recovery tests с temporary manifests/artifacts,
strict typechecks, production build, полный Hamiltonian suite и
`git diff --check`. Live contour не менять; RPC delta и browser transaction
остаются `.6`/`.7`.

Один срез меняет publication order, normal failure rollback, startup recovery,
immutable artifact checks и запрет delivery несошедшегося state без отдельного
journal.

Фактические действия:

* `readReleaseIntentComposition` отделяет durable target root от полностью
  сошедшегося release: target graph проверяется до действий, а обычная выдача
  требует exact совпадения root и child versions;
* normal publication первой атомарной записью меняет только root caret
  dependencies, затем строит все env и materialize exact artifacts, а child
  versions пишет последними;
* build failure и перехваченная normal error восстанавливают сохранённые root
  и child manifests; уже созданные exact files остаются только immutable и
  не становятся действующим выпуском;
* cold-start `recoverPublication()` выполняется до `Bun.serve`, воспроизводит
  root target, публикует missing artifact, переиспользует byte-identical exact
  artifact и останавливается на digest/size conflict без перезаписи;
* отдельный journal, release ID и recovery cache не добавлены;
  `hamiltonian/package.json` остаётся единственным durable intent;
* изменённый server package получил patch version `0.1.3`; browser packages и
  их exact versions не менялись.

Результат и вывод:

Host publication теперь имеет раннюю durable точку восстановления. Пока root и
children расходятся, обычное чтение release отвергает состояние. После process
restart server до открытия listener строит root target заново и доводит child
versions вперёд; уже существующие другие bytes под той же exact identity
считаются повреждением и остаются нетронутыми.

Проверки:

* focused publication/release/server suite: `15 pass`, `0 fail`,
  `129 expect()`;
* полный `bun test hamiltonian --timeout 180000`: `267 pass`, `0 fail`,
  `3603 expect()`;
* strict typechecks `@release/server` и test contour прошли;
* production build всех шести packages прошёл;
* root TypeScript остановился только на тех же семи известных ошибках старого
  `hamiltonian/update`; новых ошибок нет;
* `git diff --check`, unchanged-`bun.lock` и no-extra-journal/ID audits прошли.
  Live contour не запускался.

Подготовительный commit: `7d67dc921`.

Result checkpoint: этот commit.

### UPD-003.6 — Сверять фактический browser cache и возвращать только delta

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `5d35a7818` среза `.5`.

Классификация: новый RPC reconciliation mechanism поверх доказанных artifact
identity, root desired composition и host recovery. Срез меняет только обмен
состоянием и вычисление delta; применение browser transaction остаётся `.7`.

Требование: successful publication рассылает только payload-free
`release-changed`. На signal и при каждом новом RPC connection Worker заново
сканирует фактические exact entries canonical code caches, вычисляет bytes
SHA-256/size и отправляет полный current state. Server в этот момент читает
действующий root desired state и отвечает только массивами `update` и
`remove`; unchanged entries, полный desired, URL, cache owner и любые IDs в
ответ не входят.

Основание и связанная история: owner decision 4; `.3` дала canonical identity
и namespace ownership, `.4` — полный desired membership, `.5` — запрет чтения
несошедшегося root state. Старый RPC был допустимым checkpoint `UPD-002`, но
передаёт host state вместо сравнения с фактическим browser cache.

Наблюдаемое расхождение: publisher отправляет `{type: "release", packages}`;
Worker при reconnect читает тот же список через `GET /code`; server
`messageRpc` игнорирует сообщения и не знает current cache. Старый signal
может нести уже устаревший package list, removal нельзя вывести без полного
browser current state.

Причина: прежний protocol был односторонней доставкой полного host state и
делегировал Worker локальное сравнение только по перечисленным packages.

Разрешённое изменение одного механизма:

* ввести общий строгий wire contract для `release-changed`, full current
  request и delta response;
* canonical scan читает только browser code caches `release`, `internal` и
  будущий `metafor`, принимает только canonical exact package URL в cache его
  namespace и вычисляет digest/size из фактического response body;
* server сравнивает desired и current по `(name, env)`: отсутствующая либо
  несовпадающая exact identity попадает в `update`, лишняя version или slot вне
  desired — в `remove`, полностью совпавшая entry отсутствует в ответе;
* current request разрешает пустой массив; delta может иметь оба пустых
  массива и тогда не вызывает transaction/reload;
* Worker держит не более одной request/delta сверки в полёте; следующий signal
  после неё снова собирает current, поэтому correlation ID не требуется;
* reconnect всегда начинает новую сверку, server не хранит и не replay-ит
  прежние current, desired, delta либо notifications;
* successful host publication отправляет один signal только после converged
  result; failure не отправляет ничего;
* назначить patch versions изменённым release service и server packages,
  опубликовать новый exact service-worker artifact; `.7` пока продолжает
  применять только `update`, а `remove` и фиксированный transaction остаются
  следующим срезом.

Regression или опровергающее доказательство: protocol rejects payload в
signal, malformed/duplicate current и поля endpoint/cache/desired/ID; cache
scan доказывает фактические bytes; server delta покрывает empty cache,
unchanged, digest mismatch, stale version, extra package и removal-only;
reconnect отправляет новый current, повторный signal не применяет старый list;
publication failure не публикует signal.

Среда и критерий приёмки: canonical `main`, implementation baseline
`5d35a7818`; focused RPC/state/delta tests, strict typechecks, production и
development service builds, новый immutable exact artifact, полный Hamiltonian
suite, `git diff --check` и audit старых message fields. Live contour не
менять; transaction storage/recovery и removal apply остаются `.7`.

Один срез заменяет package-state notification на payload-free signal,
cache-derived current request и server `update/remove` response без replay,
`desired`, endpoint или дополнительных IDs.

Фактические действия:

* общий строгий wire contract принимает только payload-free
  `release-changed`, full `release-current` и `release-delta` с двумя полями
  `update/remove`; лишние package lists, endpoint, cache, desired и IDs
  отвергаются;
* Worker сканирует только уже существующие canonical caches `release`,
  `internal`, `metafor`, принимает exact URL только в cache его namespace и
  повторно вычисляет digest/size из body; повреждённая identity не выдаётся как
  current;
* server по свежему root state вычисляет update для missing/mismatched identity
  и remove для stale version либо slot вне desired; unchanged entries не
  возвращаются;
* publisher отправляет ровно `{type: "release-changed"}` только после успешной
  publication; reconnect и signal всегда запускают новое чтение caches;
* Worker сериализует request/delta, а signal во время сверки превращает только
  в следующую fresh сверку; response/correlation/release ID не введены;
* пустая delta новой Worker execution подтверждает завершённый прежний reload и
  очищает старый restart marker без повторной навигации;
* `@release/service` опубликован как final patch `0.1.9`, exact SHA-256
  `39444f3f8005b4a5ffaa1a5ac401b4f3243d0af02f565761f920bf7804351708`,
  size `12214`; `@release/server` получил version `0.1.4`.

Результат и вывод:

Notification больше не переносит состояние и не может устареть. Истиной
current является фактический cache конкретного profile, истиной desired — root
composition в момент server request. Полностью совпавший profile получает
пустую delta; disconnected profile при reconnect получает изменения от своего
реального состояния. `remove` уже вычисляется и валидируется, но его
транзакционное применение намеренно принадлежит `.7`.

Проверки:

* focused protocol/state/delta suite и boundary checks прошли;
* production build всех шести packages прошёл; development profile сохранил
  новые owner-scoped diagnostics и inline source map, production удалил debug;
* изолированный browser suite доказал initial empty cache, group update,
  failed fetch, reconnect, offline restore и один reload;
* полный `bun test hamiltonian --timeout 180000`: `271 pass`, `0 fail`,
  `3595 expect()`;
* strict typechecks release service/server и test contour прошли;
* root TypeScript остановился только на тех же семи известных ошибках старого
  `hamiltonian/update`; новых ошибок нет;
* `git diff --check`, unchanged-`bun.lock` и old-full-state-message audits
  прошли. Live managed contour не запускался.

Подготовительный commit: `0478902e7`.

Result checkpoint: этот commit.

### UPD-003.7 — Применять delta через один фиксированный transaction cache

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `81d5d8269` среза `.6`.

Классификация: новый browser cache transaction/recovery mechanism поверх
fresh server delta. Срез меняет Cache Storage, startup lookup/guard, removal и
reload commit; host publication и RPC comparison остаются доказанными `.5/.6`.

Требование: на origin существует не более одного технического cache с точным
именем `transaction`. Первой записью каждой новой либо возобновляемой попытки
Worker сохраняет строгую свежую server delta в
`transaction[/code?state=active]`, затем fetch-ит только update artifacts,
проверяет identity и применяет update/remove к canonical owner caches. Успех
фиксируется только удалением всего `transaction`; постоянных active/pending
state entries и UUID storages нет.

Основание и связанная история: owner decision 5; `.3` дала canonical URL/cache
owner и byte verification, `.6` — fresh current/delta без replay. Старый
active metadata и owner UUID caches являются непосредственно наблюдавшимся
лишним Storage из owner screenshots.

Наблюдаемое расхождение: `release` одновременно хранит code и
`/code?state=active|pending`; каждый update создаёт
`<owner>:release:<randomUUID>`. Startup loader без active record не умеет
разрешить stable request в единственную exact entry, а removal отсутствует.
Ошибка подготовки удаляет intent вместо сохранения точки recovery.

Причина: прежний protocol открывал group отдельным metadata switch и считал
остановленную до него попытку откатываемой, поэтому transaction state был
разнесён между постоянным `release` и несколькими временными caches.

Разрешённое изменение одного механизма:

* заменить release `state.ts` и UUID staging на один cache `transaction`;
  пустой cache без intent безопасно удаляется, потому что до первой intent
  write никаких code/cache действий не разрешено;
* каждую полученную delta — в том числе recovery — первой записью сохранять под
  `/code?state=active`; obsolete prepared responses остаются только внутри
  transaction и исчезают вместе с ним;
* verified response update переиспользовать из transaction либо fetch-ить по
  canonical exact URL; canonical owner сначала получает новый exact response,
  затем удаляются прежние versions этого slot и явные remove entries;
* crash между canonical writes оставляет `transaction`; новый Worker собирает
  фактический mixed current, получает свежую delta и идемпотентно продолжает
  вперёд. Rollback active manifest не создаётся;
* если canonical state уже совпал, но transaction остался после последней
  canonical write, пустая fresh delta удаляет transaction и завершает commit;
* startup loader без active metadata разрешает stable package request через
  единственную exact entry derived owner cache; при transaction он не запускает
  произвольный mixed release, а использует prepared exact
  `@release/service:service-worker` recovery executor либо его untouched
  canonical entry;
* startup fetch/cache записывает browser code только по exact versioned URL,
  выведенному из response identity headers; stable code entries не остаются;
* после commit удалить legacy `/code?state=active|pending`, старые
  `*:release:*` caches и superseded exact entries; code caches содержат только
  package code;
* delta with only remove является полноценной transaction; unchanged packages
  не fetch-ятся и не копируются;
* один non-empty committed transaction закрывает RPC и запускает один reload;
  empty delta без transaction ничего не меняет и не перезагружает;
* назначить patch versions изменённым `@release/service` и immutable
  `@startup/service`, опубликовать новый exact release service artifact.

Regression или опровергающее доказательство: first cache write — intent;
empty transaction, partial fetch, digest mismatch, crash after one canonical
put/delete и crash before transaction deletion сходятся при fresh delta;
remove-only удаляет точный slot; loader не исполняет mixed service; после
успеха остаются только `startup/release/internal` code caches, один exact entry
на slot, нет state keys/UUID/transaction и выполняется один reload.

Среда и критерий приёмки: canonical `main`, implementation baseline
`81d5d8269`; focused transaction/startup-loader tests и изолированный browser
suite с deliberate interruption, strict typechecks, production/development
builds, exact artifacts, полный Hamiltonian suite и static cache-name audit.
Live managed contour остаётся `.8`.

Один срез удаляет постоянный active state и UUID caches, вводит first intent
write, guarded canonical apply, crash resume, removal и commit удалением
`transaction`.

Фактические действия:

* прежние pending/active metadata, owner-specific UUID staging и validator
  полного package list удалены; общий browser contract задаёт один cache
  `transaction` и intent key `/code?state=active`;
* release service первой записью сохраняет строгую fresh delta, повторно
  проверяет prepared exact responses, fetch-ит только `update`, применяет
  `update/remove` к namespace owners и последней операцией удаляет весь cache;
* canonical update сначала пишет новую exact version и только затем удаляет
  остальные versions того же `(package, env)` slot; unchanged entries не
  копируются;
* startup loader больше не читает постоянный active manifest: stable request
  разрешается только через одну exact canonical entry, а при transaction может
  исполняться только prepared либо untouched recovery `@release/service`;
* пустой cache без intent удаляется без reload, оставшийся после canonical
  commit transaction завершается по свежей empty delta с одним recovery reload;
* cleanup удаляет legacy state keys из code owners и старые `*:release:*`
  storages; browser regressions требуют только `startup/release/internal` после
  commit;
* опубликован production artifact `@release/service@0.1.11` с SHA-256
  `9199f25c232cf84ff83608fd6d353cf9e2f54abeb055ac71b33a4bffbca55ec0`
  и size `9790`; startup service поднят до `0.1.6` и объявляет dependency
  `workspace:^0.1.11`. Промежуточная immutable `0.1.10` не перезаписывалась:
  после удаления оставшегося старого validator финальный source получил
  следующую patch version.

Результат и вывод:

Cache Storage больше не использует постоянный release manifest или UUID как
commit point. Единственный признак незавершённого browser update — наличие
`transaction`; единственный успешный commit — его удаление. Browser proof
покрыл network failure до canonical writes, пустой transaction, legacy cleanup,
смешанное состояние после новой canonical put до удаления старой version,
remove-only fresh delta и один reload после завершения.

Проверки:

* strict typechecks всех шести package env и test contour прошли;
* development build сохранил owner-scoped `console.debug` и inline source map,
  production build удалил debug и source map;
* focused transaction/browser suite прошёл `8 pass`, `0 fail`; отдельные
  boundary/release/build-profile проверки прошли `19 pass`, `0 fail`;
* полный `bun test hamiltonian --timeout 180000` прошёл `273 pass`, `0 fail`,
  `3699 expect()`;
* production base и immutable `0.1.11` release service совпадают по bytes,
  SHA-256 и size; `git diff --check` и static old-cache audit прошли;
* общий root TypeScript не имеет новых ошибок и остановился на тех же семи
  известных ошибках старого `hamiltonian/update`.

Подготовительный commit: `f7c2c8aa3`.

Result checkpoint: этот commit.

### UPD-003.7.1 — Синхронизировать development guide с fixed transaction

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoint `a8dbf5059` среза `.7`.

Классификация: correction того же browser transaction contract без изменения
кода, package versions или среды приёмки.

Требование: обязательный guide skill `metafor-dev` должен описывать только
реализованный закон `.7`: canonical owner caches содержат exact code entries,
на origin существует не более одного cache `transaction`, первая его entry —
fresh delta под `/code?state=active`, успешный commit удаляет cache целиком.

Наблюдаемое расхождение: после result review guide всё ещё говорил, что Worker
сохраняет stable package URL, а plural `<owner>:release:<transaction>` caches
существуют во время подготовки. Эти положения относятся к удалённому
active-switch/UUID protocol.

Причина: `.7` синхронизировала domain README и карточку, но обязательный
development reference, на который направляет skill, не вошёл в result diff.

Разрешённое изменение: заменить только устаревший cache/transaction раздел
guide, не менять executable code и не расширять live acceptance `.8`.

Regression: guide не содержит разрешения на stable code entries,
owner-specific transaction caches, постоянный active state или UUID; команды
publication и env contract остаются без изменений.

Среда и критерий приёмки: canonical `main`, baseline `a8dbf5059`; перечитать
diff, выполнить static terminology audit и `git diff --check`.

Фактический результат: mandatory development guide теперь различает stable
request и exact cache entry, описывает один `transaction`, first intent write,
recovery по fresh current/delta и commit удалением всего cache. Старые
owner-specific transaction caches и постоянный active manifest явно запрещены.
Executable code, package manifests и artifacts не менялись.

Проверки: targeted terminology audit и `git diff --check` прошли.

Подготовительный commit: `5ff66baa6`.

Result checkpoint: этот commit.

### UPD-003.8 — Доказать полный update и recovery в живом browser contour

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без
субагента после result checkpoints `a8dbf5059` и `7ef8e87d7`.

Один closing acceptance-срез проверяет env-specific imports, изменение одного
env, совместное изменение нескольких packages, removal, failed host build,
host crash recovery, Worker crash recovery, один reload и независимость двух
Chrome profiles. Owner acceptance остаётся отдельным gate после live proof.

Классификация: closing evidence и owner gate поверх реализованных механизмов;
нового release protocol или package contract этот срез не вводит.

Разрешённые действия и границы evidence:

* перед lifecycle проверить `metafor-dev status`; чужой Hamiltonian не
  присваивать и второй постоянный CDP Chrome не запускать;
* до managed dev startup опубликовать новые patch versions всех текущих
  browser members именно development profile, чтобы cold recovery не пыталась
  заменить production bytes под прежним SemVer; startup base artifacts также
  собрать в development profile;
* изолированным двухпрофильным browser regression доказать отдельные Cache
  Storage/transaction/reload для двух profiles; он не заменяет живую приёмку;
* автоматизированные deliberate failure/crash proofs `.5/.7` считать evidence
  только своих изолированных сред и не воспроизводить повреждение manifests или
  убийство owner-visible contour без отдельной необходимости;
* в managed origin очистить site data через skill, подтвердить development
  source maps и структурированные server/Service Worker/Window diagnostics,
  затем опубликовать реальный package update без server restart;
* через точный CDP target доказать один reload, bare import
  `@internal/visual`, exact env/version cache entries, отсутствие permanent
  state keys, UUID storages и `transaction` после commit;
* оставить Hamiltonian, iTerm и CDP Chrome работающими для owner review.

Regression или опровергающее доказательство: production bytes под тем же
SemVer не перезаписываются dev bytes; два profiles не делят transaction или
canonical caches; live update содержит debug/source map, меняет exact version,
не создаёт duplicate slot и завершается одним reload. Console/runtime error,
лишний storage, повторный reload либо отсутствие owner-visible target не дают
перевести срез в `REVIEW`.

Критерий завершения среза: automated two-profile proof, managed status/log/CDP
evidence, точный список версий и caches, оставленный работающий contour и
явный owner gate. До ответа владельца задача остаётся открытой.

Фактические действия:

* isolated browser regression поднял два независимых Chrome profile, доказал
  раздельный Cache Storage через profile-only sentinel и доставил один Visual
  update в оба profile с одним reload каждого;
* `metafor-dev status` обнаружил managed iTerm `/dev/ttys003`, CDP Chrome PID
  `12306` и target `44D16B99D13C1A8B35F293D86AEFE0D5`, но Hamiltonian был
  запущен прежней командой без `metafor:server`; только этот managed process
  остановлен dispatcher перед переходом на актуальный server code;
* до нового startup опубликован полный development composition с новыми SemVer,
  затем startup main/service собраны с diagnostics и inline source maps; новый
  server успешно выполнил development cold recovery до listener;
* разрешённый skill `clear-site-data` очистил только origin `127.0.0.1:4444`,
  переиспользовал тот же target и восстановил controller
  `/@startup/service?env=service-worker`;
* live Visual-only publication `0.1.5 → 0.1.6` прошла без server restart:
  Worker записал intent, подготовил exact artifact, переключил canonical cache,
  удалил transaction и выполнил ровно одну Window navigation;
* следующая live group publication одновременно обновила release main,
  recovery release service и Visual; CDP зафиксировал три prepared responses,
  три canonical puts, один transaction commit, один reload и ноль Console
  errors;
* main artifact сохранил bare import `@internal/visual`; все три финальных code
  artifacts содержат `console.debug` и inline source map;
* отдельная reproducibility-проверка подтвердила byte-for-byte совпадение
  final immutable artifacts с fresh development build при тех же
  детерминированных staging basenames `0.js/1.js/2.js`; временная директория
  проверки после сравнения удалена.

Финальный development release:

| Package | Env | Version | SHA-256 | Size |
| ------- | --- | ------- | ------- | ---- |
| `@release/main` | `main` | `0.1.8` | `b652f59c6a4a8493042168a1c56b0cdafdb7ef93c0ff0f2855b17cf903f6645f` | `1047` |
| `@release/service` | `service-worker` | `0.1.13` | `8ce0a2c31291aaf5193a18cc267935d390b4a7c8123266141c804399f18dc3aa` | `74676` |
| `@internal/visual` | `main` | `0.1.7` | `0ecb736b7fedb5a600b927f4c930f3ca198e0f6ac76f56109fcfaa44e12fb25f` | `1304976` |

Финальный browser cache snapshot:

```text
startup: /, /@startup/main?env=main, /manifest.webmanifest, font asset
release: /@release/main?env=main&version=0.1.8
         /@release/service?env=service-worker&version=0.1.13
internal: /@internal/visual?env=main&version=0.1.7
```

`transaction`, `*:release:*`, `/code?state=active|pending`, stable code entries
и duplicate package slots отсутствуют.

Проверки и evidence boundary:

* two-profile scenario: `1 pass`, `0 fail`, `33 expect()`;
* result suite `.7`: `273 pass`, `0 fail`, `3699 expect()`; deliberate host
  failure/crash и Worker interruption относятся к этим isolated proofs;
* обе live publications вернули `200/success: true`, каждая дала ровно один
  main-frame reload и ноль собранных Window/Worker errors;
* server logs показали payload-free notification, fresh current, delta только
  `update/remove` и финальную empty delta после reconnect;
* managed contour оставлен работающим: Hamiltonian PID `19020` с server child
  `19021`, iTerm `/dev/ttys003`; CDP Chrome PID `12306`, profile
  `/Users/zavx0z/Library/Application Support/Google/Chrome-CDP`, target
  `44D16B99D13C1A8B35F293D86AEFE0D5` на `http://127.0.0.1:4444/`.

Подготовительный commit: `a74fa609b`.

Result checkpoint: этот commit. Owner-visible acceptance остаётся явным gate;
до ответа владельца UPD-003 не закрывается и live contour не останавливается.

### UPD-003.9 — Унифицировать состав Hamiltonian packages

Статус и исполнитель: `REVIEW`; выполнено в текущей задаче без субагента после
owner review среза `.8` и прямого поручения владельца продолжить UPD-003.

Классификация: новое структурное требование того же package/env договора.
Срез сохраняет доказанные SemVer, browser identity, root-first publication,
delta и fixed transaction, но устраняет второй способ представлять env через
отдельное package name.

Требование:

* package является единственной единицей имени, SemVer, typecheck, публикации
  и полного набора env artifacts;
* каждый env package находится только в директории `<env>/index.ts`; файлы,
  принадлежащие одному env, находятся внутри той же директории;
* conditional root export напрямую связывает condition `<scope>:<env>` с
  `./<env>/index.ts`; вложенные `types/browser|bun`, env subpath и `default`
  отсутствуют;
* `@release/main`, `@release/service`, `@release/server` заменяются одним
  package `@hamiltonian/release` в `hamiltonian/release` с env `main`,
  `service-worker` и `server`;
* `@startup/main` и `@startup/service` заменяются одним package
  `@hamiltonian/startup` в `hamiltonian/startup` с env `main` и
  `service-worker`;
* `@internal/visual` сохраняет одно имя и получает env directories `main` и
  `server`; bare import во всех consumers не содержит env;
* package объявляет один script `typecheck`, не объявляет `prebuild`, generic
  `build` или env-specific typecheck, а каждый artifact создаёт отдельный
  `build:<env>`;
* build executor запускает package-wide `typecheck` ровно один раз перед
  группой env builds и при его ошибке не запускает ни один `build:<env>`;
* изменение package создаёт один новый SemVer и полный набор объявленных env
  artifacts; browser update получает только browser env, а `server` artifact
  не попадает в Cache Storage и не заменяет работающий Bun process;
* canonical browser URLs используют новые package names и прежний обязательный
  query `env`; cache `startup`, `release`, `internal` сохраняют роли, а
  transaction/current/delta продолжают работать по `(package, env)` без
  специальных main/service packages.

Основание и связанная история: `.1–.8` доказали conditional resolution,
package-wide SemVer, immutable env identities, root recovery и browser cache
transaction. Owner proof в staged `@internal/visual` показал, что один bare
specifier выбирает разные public APIs `main/index.ts` и `server/index.ts` по
conditions. Новое отличие: прежние release/startup entrypoints всё ещё
оформлены отдельными packages и находятся внутри transport-директории `web`,
поэтому build/update code поддерживает две модели состава.

Наблюдаемое расхождение: package discovery и composition считают
`@release/main`, `@release/service`, `@release/server`, `@startup/main` и
`@startup/service` разными package identities; manifests требуют
`typecheck:<env>` и `prebuild:<env>`, а permanent owner law всё ещё описывает
`metafor:<env>` и вложенные export branches.

Причина: срез `.2` расширил существующие single-env packages, но не объединил
их по логической package identity; поэтому env остался одновременно condition
и частью имени package.

Разрешённое изменение одного механизма: заменить package composition,
directory layout, conditional resolution и package build orchestration как
один согласованный механизм. Обновить root membership/dependencies, imports,
routes, import map, startup/release loaders, package parser, publisher,
recovery, tests, Hamiltonian owner contract, roadmap и обязательный development
guide только в объёме новых identities. Доказанные delta/transaction алгоритмы
не переписывать сверх необходимых переименований.

Regression или опровергающее доказательство:

* package manifests имеют только direct `<scope>:<env> → ./<env>/index.ts`,
  один `typecheck` и по одному `build:<env>` с разными outfiles;
* executor при all-env publication вызывает typecheck package один раз, а при
  его failure не запускает env builds;
* TypeScript consumers одного bare import получают разные env APIs без build;
  unsupported condition не получает fallback;
* одна публикация `@hamiltonian/release` создаёт `main`, `service-worker` и
  `server` artifacts одной версии, но browser state/cache содержит только два
  browser env;
* startup cold load, offline restore, Visual-only update, release group update,
  root recovery, delta/remove, fixed transaction и один reload проходят с
  новыми package identities;
* source, manifests, routes, tests и постоянная документация не содержат
  действующих ссылок на старые package identities и `prebuild:<env>`.

Среда и критерий приёмки: canonical `main`, baseline `0b31bd1cd`; сохранить
owner staged proof, выполнить package/test TypeScript, focused package/build/
release/browser regressions, production/development profiles, полный
Hamiltonian suite и `git diff --check`. Затем через единственный managed
`metafor-dev` contour доказать cold startup и development update с точными
новыми cache URLs, одним reload и отсутствующим `transaction` после commit;
контур оставить владельцу.

Граница: автоматическое обновление уже работающего Bun server/server-worker
runtime является отдельным будущим большим этапом и в `.9` не входит.

Фактические действия:

* `@hamiltonian/startup` перенесён в `hamiltonian/startup` и объединяет env
  `main` и `service-worker`; `@hamiltonian/release` перенесён в
  `hamiltonian/release` и объединяет env `main`, `service-worker` и `server`;
  transport-каталог `web` больше не владеет package identities;
* каждый env entrypoint находится в `<env>/index.ts`; `@internal/visual`
  приведён к тому же layout с `main` и `server`, а consumers во всех средах
  импортируют один bare specifier `@internal/visual`;
* manifests содержат только direct conditional exports `<scope>:<env>`, один
  package-wide `typecheck` и отдельные `build:<env>` без `prebuild`, generic
  `build` и env-specific typecheck;
* общий executor валидирует этот contract и запускает один `typecheck` перед
  параллельной группой env builds; при ошибке typecheck ни один env build не
  начинается;
* root `hamiltonian/build.ts`, publisher и recovery работают с одним package и
  полным набором его env artifacts; recovery переиспользует уже совпавшие
  exact artifacts и не пересобирает завершённые или неизменившиеся packages;
* membership, dependency closure, routes, import map, startup/release loaders,
  browser cache state, delta, fixed transaction и tests переведены на новые
  identities; browser state исключает `server`, а browser-запрос этого env
  отвечает `404`;
* Service Worker delivery headers теперь выводятся из env, без custom artifact
  metadata; development diagnostic scopes унифицированы как
  `[@package:env]` и сохраняются в development bytes;
* добавлен прямой TypeScript proof: один bare import `@internal/visual` без
  предварительной сборки выбирает разные API для `internal:main` и
  `internal:server`, а unsupported condition не получает fallback;
* опубликованы development artifacts `@hamiltonian/release@0.1.3` и
  `@internal/visual@0.1.10`; startup development artifacts собраны без
  включения startup в сменяемый release membership.

Результат и вывод:

Одна package identity теперь действительно владеет SemVer и всем набором env:

* `@hamiltonian/release:main@0.1.3` —
  `1bc268d4a517a1a5fca0b305705ad8fc50f94849fde6f6fce2a8bc5a89a182f9`,
  `1083` bytes;
* `@hamiltonian/release:service-worker@0.1.3` —
  `bf740d688b406995290760c825f05e2c697aa1cbe2e10cee3c0c1ba6205cb0d0`,
  `76079` bytes;
* `@hamiltonian/release:server@0.1.3` —
  `a12dbde0094bca82e43959300297087a6b58456077dcac7d0ddd565c2ad294b9`,
  `157404` bytes;
* `@internal/visual:main@0.1.10` —
  `8347b18a0b9f36554b5f891b1026eb85ec0671406e52e560f565a71fd770f95a`,
  `1305109` bytes;
* `@internal/visual:server@0.1.10` —
  `1681b331e551431899f645d9f6c278f325020603edb01fb8a90ed71eaee0a9d9`,
  `554` bytes.

Development startup artifacts доказаны отдельно: `main` —
`1b1e0fccf42a7d7eae0fa7fcf323f5a5b77da59cd48315f732585c545f21f17c`,
`3395` bytes; `service-worker` —
`5d01e4944b3e11a562f9ccc8e750c583ebbbcc696f3b464cb7917c505c1d4b01`,
`58853` bytes.

Проверки:

* package typecheck всех трёх packages и test-contour TypeScript прошли;
* focused architecture/build/publication/release suite прошёл `40 pass`,
  `0 fail`, `624 expect()`;
* полный Hamiltonian suite прошёл `277 pass`, `0 fail`, `3632 expect()`;
* production profile удаляет `console.debug` и source map, development profile
  сохраняет оба; failed package-wide typecheck не запускает env builds;
* recovery regression с намеренно падающим typecheck переиспользовал пять уже
  завершённых artifacts и закончил child/root convergence без rebuild;
* общий root `bun run typecheck` успешно прошёл новые package typechecks, после
  чего остановился на семи ранее зафиксированных ошибках неизменённого старого
  `hamiltonian/update`; это существующий root-check gap, а не регрессия `.9`;
* `git diff --check` прошёл.

Live proof выполнен через единственный managed `metafor-dev` contour после его
контролируемого restart и очистки site data точного origin. Server сначала
вычислил update из пустого current state для `release(main/service-worker)` и
`visual(main)`, затем получил полный current state и вычислил пустую delta.
Browser доказал:

* Cache Storage содержит ровно `startup`, `release`, `internal`; постоянного
  state cache и transaction cache нет;
* `release` содержит только
  `/@hamiltonian/release?env=main&version=0.1.3` и
  `/@hamiltonian/release?env=service-worker&version=0.1.3`, `internal` — только
  `/@internal/visual?env=main&version=0.1.10`;
* `/code` возвращает те же три browser identities, server env отсутствует, а
  прямой browser request `@internal/visual?env=server` отвечает `404`;
* active registration и controller используют
  `/@hamiltonian/startup?env=service-worker`;
* cached development artifacts содержат inline source maps и debug calls;
  после чистой перезагрузки Console показывает только три scopes
  `[@internal/visual:main]`, `[@hamiltonian/release:main]` и
  `[@hamiltonian/startup:main]`, без page errors и failed requests.

Managed contour оставлен работающим: Hamiltonian PID `52082`, iTerm
`/dev/ttys003`; CDP Chrome PID `12306`, target
`44D16B99D13C1A8B35F293D86AEFE0D5` на `http://127.0.0.1:4444/`.

Подготовительный commit: `b1ddd6842`.

Result checkpoint: этот commit. Owner-visible acceptance остаётся явным gate;
до ответа владельца UPD-003 не закрывается и live contour не останавливается.

### UPD-003.10 — Сохранять полный рабочий состав до готовности нового

Статус и исполнитель: `REVIEW`; Codex, текущая задача. Принято владельцем
после обнаружения глобальной ошибки commit-протокола результата `.7`, сохранённой
срезами `.8` и `.9`.

Классификация: correction-срез существующей fixed browser transaction. Он не
меняет env identity, server delta, root-first publication или package
composition, а исправляет нарушение уже поставленного требования: отсутствие
`transaction` означает полностью завершённое обновление, а её наличие не
должно оставлять canonical caches без полного рабочего состава.

Требование: новый состав полностью stage-ится, устанавливается candidates и
повторно проверяется до удаления любой старой entry. До первой old deletion
существует полный old composition; после неё уже существует полный new
composition и recovery идёт только вперёд. Старый service-worker release
удаляется последним, `transaction` — последней durable операцией.

Основание и связанная история: `.7` ввела один fixed `transaction`, first
intent write и staged bytes, но canonical commit реализовала как повтор
`put(new slot) → delete(old slot)`. `.8` доказала только успешный live path, а
не kill после каждой canonical mutation. `.9` сохранила алгоритм при
объединении package identities. На baseline `1bb698e1e` остановка после первой
slot replacement оставляет смесь old и new packages и лишает систему полного
старого состава раньше готовности полного нового.

Причина: наличие durable intent, полная staging и удаление transaction в конце
были ошибочно приняты за транзакционность всей операции. Cache API не даёт
общего атомарного batch для нескольких owner caches, а существующие regressions
не инъецируют остановку между каждой `put/delete` boundary.

Разрешённое изменение одного механизма:

* сначала записать все new candidates в canonical caches без old deletion;
* повторно проверить полный новый composition по exact identity;
* до этой границы сохранить полный old composition независимо от уже
  добавленных candidates;
* после неё выполнять только forward cleanup, удаляя old service-worker
  release последним;
* разрешить overlap old/new exact entries только во время cutover и сохранить
  Cache order, не выполняя повторный `put` old entry;
* удалить `transaction` только после итоговой проверки одной exact entry на
  каждый desired slot и отсутствия всех removals;
* не давать startup читать transaction, разбирать delta, сравнивать SemVer или
  использовать active pointer;
* не удалять Service Worker registration для runtime handover;
* первой entry фиксированного cache сохранять только marker `/transaction`, а
  не delta, desired state или прежнее `/code?state=active`; после остановки
  release заново получает fresh delta из фактического canonical current;
* не поддерживать migration-код прежних active/pending и UUID-cache formats:
  действующий source, tests и новый clean acceptance contour не содержат этих
  хвостов.

Regression или опровергающее доказательство: fault injection после intent,
каждого staged put, каждого candidate put, полной candidate verification,
каждой old deletion, удаления old service release и удаления transaction.
После каждого kill новый Worker offline
должен получить либо полный old, либо полный new composition; incomplete
unbootable mixture запрещена. Отдельно доказать Cache order `[old, new] → old`,
`[new] → new`, fail closed при повреждении первой entry без пропуска к new,
отсутствие повторного `put(old)`, соседнюю совместимость transaction и
отсутствие прежнего Window-main deadlock.

Граница: срез исправляет durable cache transaction. Окончательный минимальный
runtime contract startup/release и точный способ делегирования browser events
остаются следующим основным предметом обсуждения и не расширяются молча в
`.10`.

Фактические действия:

* `transaction` теперь первой entry хранит только `/transaction`; delta и
  прежние active/pending/UUID migration branches удалены из действующего
  startup/release source;
* startup больше не читает transaction, не ждёт её завершения и не выбирает
  recovery artifact: он проверяет и запускает первую exact service-worker
  entry в Cache order, а повреждение первой entry завершает boot fail closed;
* release выводит candidate composition из фактического current и fresh delta,
  stage-ит все artifacts, добавляет все candidates без old deletion и только
  после полной exact-проверки начинает cleanup;
* cleanup перечисляет фактические keys всех canonical owners, поэтому удаляет
  также invalid stale bytes, stable/noncanonical entries и дубликаты в чужом
  owner cache; old release service-worker exact удаляется последним;
* final verification требует ровно одну exact entry каждого target slot и
  только после неё удаляет весь `transaction` cache;
* test fixture удерживает единственный отказавший artifact request достаточно
  долго, чтобы browser proof детерминированно наблюдал незавершённый marker до
  fresh synchronization.

Результат и вывод:

После любой durable mutation существует либо полный старый, либо уже полный
новый состав. До cleanup startup продолжает запускать old release как первую
entry. После начала cleanup все new candidates уже записаны и проверены, поэтому
recovery движется только вперёд. Отсутствие `transaction` теперь действительно
означает, что canonical composition финально проверен и технический cache удалён
последней операцией.

Проверки:

* in-memory fault suite инъецировал остановку после каждой durable mutation,
  затем получил fresh delta и довёл тот же cache state до полного нового
  состава: `3 pass`, `0 fail`, `79 expect()`;
* Cache order regression доказал `[old, new] → old`, `[new] → new` и fail
  closed на повреждённой первой entry без изменения порядка;
* focused release/boundary suite: `14 pass`, `0 fail`, `177 expect()`;
* полный browser suite: `8 pass`, `0 fail`, `203 expect()`;
* strict typecheck startup, release и test contour прошёл;
* полный Hamiltonian suite после синхронизации development build expectation:
  `279 pass`, `0 fail`, `3715 expect()`;
* поиск прежних state keys, UUID cache names и recovery helpers, а также
  `git diff --check` прошли.

Подготовительный commit: `5d49c10e5`.

Result checkpoint: `cd7712e69`.

### UPD-003.11 — Оставить в startup только неизменяемый запуск release

Статус и исполнитель: `REVIEW`; Codex, текущая задача. Принято владельцем
после предметного разбора Service Worker events, односторонней передачи
зависимостей и self-update release. Выполняется после result checkpoint `.10`.

Классификация: новый runtime-механизм поверх исправленной durable transaction.
Он меняет границу startup/release, но не package identity, server delta,
host-publication или набор browser package env.

Требование:

* startup остаётся минимальным, безопасным и не входит в сменяемый release;
* при каждом новом ServiceWorkerGlobalScope startup синхронно регистрирует
  необходимые browser listeners и сразу начинает загрузку, проверку и запуск
  `@hamiltonian/release:service-worker`, не ожидая Window `connect`;
* `install` и `activate` принадлежат startup; `fetch` и `message` являются
  тонкой browser-protocol границей: во время dispatch startup вызывает
  `respondWith`/`waitUntil` и передаёт событие уже запущенному release, не
  реализуя application cache policy;
* startup не читает transaction, не разбирает delta, не открывает WebSocket,
  не сравнивает package versions и не решает, какие packages обновлять или
  удалять;
* startup создаёт один объект зависимостей и передаёт его release только вниз.
  Release не изменяет этот объект и не регистрирует callbacks обратно;
* factory release возвращает startup отдельный inert runtime result. Это
  обычный результат вызова, а не обратная запись в DI. Runtime явно предоставляет
  запуск, обработку делегируемых событий и `destroy()`;
* startup предоставляет низкоуровневые primitives подготовки и активации
  release runtime. Release использует их и тот же объект зависимостей для
  проверки своей новой версии, но сам владеет моментом self-update;
* новая release version сначала исполняется как inert candidate без WebSocket,
  timers и иных постоянных side effects. После завершения durable transaction
  candidate запускается, новые события направляются ему, а у прежнего runtime
  вызывается один централизованный `destroy()`;
* `destroy()` собирает cleanup RPC socket, reconnect timers, AbortController и
  других ресурсов. Новые события не попадают старому runtime, а уже начатые
  promises завершаются до окончательной очистки;
* Service Worker registration не удаляется и Worker не пытается перезапустить
  себя. При естественной выгрузке UA память очищается полностью; на следующем
  пробуждении startup с нуля запускает release из зафиксированного cache;
* после успешного package update release навигирует каждый Window ровно один
  раз; новый Window realm получает новый `release:main` и dependencies;
* новый тип browser event, которого нет среди синхронно зарегистрированных
  startup listeners, требует явного изменения startup: универсального позднего
  listener или wildcard-механизма в Service Worker нет.

Regression или опровергающее доказательство:

* release RPC подключается при cold Worker startup до любого Window message;
* source и production/development artifacts startup не содержат transaction,
  WebSocket, release cache policy, `registration.unregister()` или ожидание
  `connect`;
* `fetch` вызывает `respondWith` синхронно и может дождаться общего boot Promise;
  первый controlled navigation не теряется во время асинхронной загрузки
  release;
* release candidate до activation не создаёт WebSocket и timers; failed
  candidate оставляет прежний runtime и полный old composition;
* успешный self-update сохраняет ту же Service Worker registration, создаёт
  ровно один новый RPC runtime, очищает прежний и навигирует каждое Window один
  раз;
* принудительный cold wake после commit запускает новый release без active
  pointer, SemVer selection и transaction policy в startup;
* focused TypeScript/runtime tests доказывают односторонний DI, inert candidate,
  последовательность activate/swap/destroy и завершение in-flight event.

Граница: срез не добавляет `push`, `sync`, notification events и автоматическое
обновление живого Bun runtime. Их появление потребует отдельного закона и
синхронного startup listener, если такой event обслуживает Service Worker.

Фактические действия:

* startup синхронно регистрирует `install`, `activate`, `fetch`, `message`,
  немедленно запускает release и больше не ждёт Window `connect`;
* application cache policy перенесена в release, а startup больше не содержит
  transaction recovery, WebSocket, package delta или version decisions;
* startup передаёт release один frozen dependency object только вниз; factory
  возвращает inert runtime с `start`, `fetch`, `message`, `destroy`;
* self-update готовит target runtime после проверки полного candidate
  composition и до cleanup, активирует только после final verification и
  удаления `transaction`, затем направляет новые events target runtime;
* startup ждёт in-flight `fetch/message` прежнего runtime и вызывает один
  централизованный `destroy`, который закрывает RPC socket, reconnect timers и
  AbortController;
* Service Worker registration больше не удаляется; release навигирует каждый
  управляемый Window один раз после успешного package update;
* exact startup read сверяет version URL с response identity и остаётся fail
  closed при повреждении первой Cache-order entry.

Результат и вывод:

Startup теперь является только неизменяемой browser-protocol и runtime-host
границей. Release запускается при каждом новом ServiceWorkerGlobalScope без
сообщения Window, владеет прикладным fetch/RPC/update поведением и безопасно
сменяет собственный runtime через те же переданные primitives без второй
транзакции и без перезапуска Service Worker registration.

Проверки:

* strict typecheck startup, release и test contour прошёл;
* focused runtime/transaction/boundary suite: `11 pass`, `0 fail`,
  `207 expect()`;
* browser update/recovery contour без production cold artifact: `7 pass`,
  `0 fail`; одна production cold-проверка ожидаемо осталась gate финальной
  публикации нового immutable package version, потому что опубликованный
  `0.1.3` содержит прежние bytes;
* source audit не находит Window `connect`, `registration.unregister()`,
  transaction/RPC/cache policy в startup; `git diff --check` прошёл.

Подготовительный commit: `1a098f2a4`.

Result checkpoint: `d346b6dd6`.

### UPD-003.12 — Разложить Hamiltonian release по предметным каталогам

Статус и исполнитель: `REVIEW`; Codex, текущая задача. Принято владельцем вместе с runtime correction.
Выполняется после result checkpoint `.11`, чтобы механические перемещения не
скрывали причинный patch transaction/runtime.

Классификация: структурный срез без нового runtime-поведения. Он устраняет
исторический transport-каталог `hamiltonian/web` и плоскую смесь нескольких
предметов внутри release.

Требование:

* удалить директорию `hamiltonian/web` полностью;
* неизменяемые HTML и Web App Manifest поднять в `hamiltonian/static`;
* общие package environment, URL и integrity contracts разместить предметно в
  `hamiltonian/shared/package`; startup использует их как свои primitives и
  передаёт release service-worker через dependency object, server env может
  импортировать тот же canonical source напрямую;
* общий для server/service-worker release wire protocol разместить в
  `hamiltonian/release/shared`, не в корне package;
* transaction implementation принадлежит только предмету update внутри env
  `release/service-worker`; файлов `release/protocol.ts` и
  `release/transaction.ts` в package root не остаётся;
* внутри `release/server` разделить package build/manifest, release
  composition/publication, HTTP delivery, RPC и общие server contracts;
* внутри `release/service-worker` разделить runtime, fetch policy, current cache,
  update transaction и RPC; env entrypoint остаётся ровно
  `service-worker/index.ts`;
* публичный env `release/server/index.ts` сохраняет один понятный surface для
  корневого `server.ts`; consumers и tests не импортируют случайные внутренние
  пути без предметной необходимости;
* relative executable imports между packages запрещены. Type-only
  cross-package import допустим; общий executable source либо принадлежит
  startup primitives и передаётся release, либо находится внутри одного
  package/env owner;
* manifests, tsconfig, build outfiles и публичные package/env identities не
  меняются только из-за filesystem layout.

Regression или опровергающее доказательство:

* repository boundary test не находит `hamiltonian/web`, root-level
  `release/protocol.ts`, `release/transaction.ts`, старые flat server paths и
  старые imports;
* `server.ts`, fixture server и HTML/static tests читают новый canonical static
  location;
* package TypeScript, focused release/publication/build/browser tests и полный
  Hamiltonian suite проходят после moves;
* production/development build profiles сохраняют прежний conditional export,
  debug и source-map contracts;
* `git diff --check` и поиск старых путей проходят до live acceptance.

Фактические действия:

* `hamiltonian/web` удалён: static поднят в `hamiltonian/static`, а package
  environment, URL и integrity helpers перенесены в
  `hamiltonian/shared/package`;
* wire protocol перенесён в `release/shared`, server разложен на `http`,
  `package`, `release`, `rpc`, `shared`, а Service Worker — на `runtime`,
  `fetch`, `cache`, `update`, `rpc`;
* корневой `release/service-worker/index.ts` оставлен тонким env entrypoint,
  transaction implementation принадлежит `service-worker/update`, публичный
  `release/server/index.ts` сохранён;
* fixture, source-reading boundaries, static paths, imports и постоянный
  Hamiltonian owner contract синхронизированы с новой структурой;
* опубликован development artifact `@hamiltonian/release 0.1.4`; clean profile
  получил одну startup registration, exact release `main` и `service-worker`
  `0.1.4`, exact internal Visual и ни одного transaction cache.

Результат и вывод:

Filesystem layout теперь повторяет предметное владение, а не прежний общий
transport `web`. Перенос не изменил conditional exports, env entrypoints,
build outfiles или package URL. Полный browser run после публикации отдельно
обнаружил семантический stale-delta no-op в уже принятом runtime-механизме:
валидная exact entry, появившаяся между current snapshot и apply, ошибочно
считалась изменением и вызывала навигацию. Этот другой механизм вынесен в
`.13`, а не скрыт внутри structural patch.

Проверки:

* strict typecheck release, startup и test contour прошёл;
* focused structural/release/publication suite: `41 pass`, `0 fail`,
  `546 expect()`; после финального thin-entrypoint и transaction ownership
  audit повторные срезы: `15 pass`, затем `36 pass`, `0 fail`;
* прямые development builds всех текущих env прошли и сохранили inline source
  maps и development diagnostics;
* browser update/recovery subset: `7 pass`, `0 fail`, `132 expect()`;
* `git diff --check`, old-path audit и отсутствие `hamiltonian/web` прошли;
* live identity headers, SHA-256 и byte size release `0.1.4` совпали с bytes
  canonical cache; production tails `/code?state=active` и
  `registration.unregister()` отсутствуют.

Подготовительный project commit: `b4e625e7c`. Реализация в него не входит.

Result checkpoint: `e9cf6e3fb`.

### UPD-003.13 — Не перезагружать Window для уже установленной exact delta

Статус и исполнитель: `REVIEW`; Codex, текущая задача. Следует за
structural checkpoint `.12` и исправляет отдельный runtime-дефект, открытый
полным production cold browser-сценарием.

Классификация: correction-срез применения fresh server delta. Он не меняет
transaction phases, package identity, server comparison, startup/release DI
или filesystem layout.

Требование: server delta является результатом конкретного current snapshot,
но к моменту её применения exact artifact мог уже попасть в canonical cache
через обычный intercepted page fetch. Если response под exact URL полностью
проходит identity и bytes verification, apply не должен считать этот slot
изменённым и не должен навигировать Window только из-за устаревшего snapshot.
Навигация выполняется лишь когда canonical composition действительно
изменилась: добавлен новый exact artifact, удалена лишняя entry либо завершён
непустой recovery cleanup.

Наблюдаемое расхождение: на чистом production profile release RPC успевает
отправить current до завершения page fetch `release:main`/`internal:main`.
Server законно возвращает update; к apply exact entries уже находятся в своих
caches и валидны, но `updateRelease` добавляет их slots в `changed`. RPC видит
непустой результат, вызывает `client.navigate()`, а тестовое чтение попадает в
уничтоженный execution context. Изолированный cold test воспроизводит это
стабильно.

Причина: ветвь `installed + verified` трактует подтверждённое присутствие
entry как mutation, выполненную текущим apply. Она должна только
переиспользовать уже установленный candidate; `changed` является журналом
выполненных canonical mutations, а не копией входной delta.

Разрешённое изменение одного механизма:

* не добавлять slot в `changed`, если exact candidate уже присутствует и
  полностью проверен;
* сохранить staging, candidate/final verification и transaction deletion без
  сокращений;
* добавить focused regression с stale update для уже установленной exact
  entry: результат пустой, canonical bytes и Cache order не меняются,
  transaction удаляется;
* сохранить непустой результат при реальном candidate put и при cleanup
  лишней canonical entry, чтобы необходимые Window navigation не потерялись.

Regression или опровергающее доказательство: focused transaction test различает
stale no-op, реальный install и cleanup. Затем production cold browser test и
полный browser suite проходят без `Execution context was destroyed`; полный
Hamiltonian suite, typecheck и `git diff --check` остаются зелёными.

Граница: срез не вводит debounce, новый synchronization ID, сохранённый desired
state или подавление реального browser reload. Он исправляет только ложную
классификацию уже выполненной canonical mutation.

Фактические действия:

* проверенная exact entry больше не добавляется в `changed`, если текущий
  apply не выполнял её canonical `put`;
* отдельный in-memory regression доказывает пустой result, неизменные bytes и
  Cache order, отсутствие canonical `put/delete` и удаление transaction
  последней операцией;
* HTTP route probes cold browser test вынесены из временной app page в обычный
  host `fetch`: проверка delivery больше не запускает второй посторонний
  startup/Service Worker runtime до настоящего пользовательского сценария;
* source fix опубликован как полный development package
  `@hamiltonian/release 0.1.5` для `main`, `service-worker` и `server` env.

Результат и вывод:

Входная stale delta больше не подменяет журнал фактических cache mutations.
Если exact artifact уже установлен и проверен, transaction всё равно проходит
staging и полный composition verification, но возвращает no-op; RPC поэтому не
выполняет ложную Window navigation. Реальный candidate put и cleanup по-прежнему
возвращают изменённые slots и сохраняют требуемый reload.

Проверки:

* focused transaction suite: `6 pass`, `0 fail`, `99 expect()`;
* strict typecheck release, startup и test contour прошёл;
* production cold browser proof изолированно: `1 pass`, `0 fail`,
  `68 expect()`;
* полный browser suite: `8 pass`, `0 fail`, `200 expect()`;
* полный Hamiltonian suite: `287 pass`, `0 fail`, `3832 expect()` по
  35 файлам;
* `git diff --check`, old state/UUID/path audits прошли; найденные
  `crypto.randomUUID()` принадлежат только atomic temporary host filenames, а
  не browser transaction identity;
* live `0.1.5`: одна startup registration/controller, canonical caches
  `startup | release | internal`, release exact `main/service-worker 0.1.5`,
  Visual `0.1.10`, transaction отсутствует; все code digest/size совпали с
  response headers, inline source maps и structured `console.debug` есть;
* после реального page reload получены debug scopes `@internal/visual:main`,
  `@hamiltonian/release:main`, `@hamiltonian/startup:main`, ошибок console и
  pageerror нет.

Managed contour оставлен работающим: Hamiltonian PID `57457`, iTerm
`/dev/ttys003`; CDP Chrome PID `12306`, target
`44D16B99D13C1A8B35F293D86AEFE0D5` на `http://127.0.0.1:4444/`.

Подготовительный project commit: `705f35566`. Реализация в него не входит.

Result checkpoint: этот commit.

## Открытые вопросы

Минимальная runtime-граница startup/release принята и реализована в `.11`;
автоматическая и live browser acceptance выполнена после `.12`/`.13` на
development release `0.1.5`. Результат оставлен владельцу для review; до его
решения вся `UPD-003` не закрывается и live contour не останавливается.

Для `.2` выбран package-owned outfile из `build:<env>`: server проверяет
уникальность paths нескольких env, но не навязывает `dist/<env>` layout.
Public package URL и identity от filesystem layout не зависят.

## Границы

* Не обновлять и не перезапускать живые Bun server/server-worker processes.
  Здесь фиксируются их package env и build entrypoints, а не deployment.
* Не включать неизменяемые HTML и executable `@hamiltonian/startup` bytes в сменяемый
  browser release.
* Не переносить code bytes через WebSocket; RPC передаёт signal, current
  metadata и delta, artifacts загружаются через `fetch`.
* Не вводить отдельные manifest/journal/releaseId/generation, transaction UUID,
  Worker incarnation или endpoint fields.
* Не вводить `artifact.type`; удалить `artifact.cache`, сохранив только
  действительно необходимые delivery headers.
* Пока не реализовывать `@metafor/*`; namespace rule сохранить как дальнейшее
  расширение.
* Packages и принадлежащие им CSS/WASM/assets держать в следующей задаче; эта
  задача проверяет JavaScript artifacts и не скрывает resource gap.
* Не менять `bun.lock` только ради package version writes: текущий runtime от
  этого не ломается. Реальный lockfile defect требует отдельного evidence.
* Не вводить remote registry, signatures, trust root, anti-rollback,
  межмашинную доставку или общий rollback выпусков.
* Не заменять Service Worker executable через `registration.update()` и не
  менять startup ownership, logical identity, topology или RPC transport.

## Критерии готовности

* Один bare import package разрешается в разные source entrypoints только по
  явно выбранному `<scope>:<env>`; unsupported env не получает fallback.
* `main`, `worker`, `service-worker`, `server` и `server-worker` различены в
  package contract, build и types; package публикует один SemVer с полным
  набором объявленных env artifacts.
* Browser cache URL содержит env query и version, а pathname остаётся единым
  package name. В metadata и RPC нет endpoint или cache owner.
* `artifact.cache` отсутствует; owner cache однозначно выводится из namespace.
* Host перечитывает manifests и отклоняет invalid membership, dependency range,
  addition или removal до root intent write.
* `@hamiltonian/startup` объявляет фактическую dependency на
  `@hamiltonian/release`; Loader types принадлежат release package, а relative
  cross-package import отсутствует.
* Root manifest записывает target intent до действий; normal failure возвращает
  прежние root versions, crash recovery сходится вперёд, а incomplete state не
  выдаётся browser.
* Successful publication отправляет только один payload-free signal. Worker
  отправляет фактический полный current state, а server возвращает только
  `update/remove` для различий.
* Canonical caches содержат только code и не содержат `/code?state=active` либо
  `/code?state=pending`; технические `*:release:<uuid>` storages отсутствуют.
* Прерванную browser transaction однозначно обозначает один непустой cache
  `transaction`, где первой записью является `/transaction`; успешный
  commit удаляет cache целиком.
* До первой old deletion сохраняется полный old composition, а после неё уже
  доказан полный new composition; slot-by-slot разрушение old запрещено.
  Old service-worker release удаляется последним, `transaction` — после
  итоговой canonical проверки. После recovery остаётся один exact artifact на
  `(package, env)` желаемого состава и выполняется один reload.
* Изменение одного package/env не пересобирает и не заменяет неизменившиеся
  packages; removal удаляет только entries вне desired dependency closure.
* Server и Worker проверяют package, env, version, SHA-256 и size; другие bytes
  под уже опубликованной exact identity отвергаются.
* Два Chrome profiles независимо сходятся к одному desired state без общих
  caches, transactions, Workers или лишних reload.

## Проверка результата

* Package-resolution tests каждого поддерживаемого и неподдерживаемого env,
  включая различие `server`/`server-worker` и bare `@internal/visual`.
* TypeScript checks каждого env с `customConditions`; named export виден из
  source без build.
* Build tests нескольких env одного package, package-wide SemVer, reuse
  неизменившихся env bytes и отсутствие частичного выпуска.
* URL/parser tests canonical query, namespace cache ownership и отсутствие
  endpoint/artifact.cache в public contracts.
* Dependency graph tests: valid closure, missing member, incompatible range,
  addition и forbidden removal.
* Host tests: root-first intent, failed-build rollback, process interruption на
  каждой write boundary, restart recovery, immutable digest conflict и запрет
  delivery incomplete state.
* RPC tests: payload-free signal, current cache request, delta-only response,
  unchanged state, removal, reconnect и отсутствие stale replay.
* Service Worker tests: first intent write, partial fetch, digest/size mismatch,
  partial canonical apply, restart resume, transaction deletion, loader guard,
  removal и один reload.
* Strict affected-package typechecks, production/development env builds, полный
  `bun test hamiltonian`, `git diff --check` и audit старых fields/cache names.
* Живой managed contour через `metafor-dev`: два Chrome profiles, изменение
  одного env, group update, removal, failed host build, interrupted Worker,
  восстановление exact caches и owner review оставленного результата.
