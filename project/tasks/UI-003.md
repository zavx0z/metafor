# UI-003 — Создать общий skill разработки UI и Node playground

## Коротко

Перенести node-local development skill в `pkg/ui` и превратить его в общий
`ui-dev`: агент одной командой запускает, перезапускает, читает logs, проверяет
и останавливает playground нужного UI/Node package, а browser evidence и
profiling получает фоново через exact CDP target.

## Зачем

Текущий `node-system-dev` умеет безопасно обслуживать только Node playground
4016. Для Components, Elements, общего Playground package и Node агент снова
вручную ищет command/port/PID/target и может случайно отнять у владельца focus.
Владелец потребовал UI-аналог `$metafor-dev`, скрывающий эту механику.

## Связанные задачи и история

* NODES-017.7 result `7fd390923` создал versioned node-local skill; correction
  `e29d374fd` перевёл lifecycle в long-lived PTY.
* `$metafor-dev` служит reference: exact checkout ownership, один dispatcher,
  `status/start/restart/logs/stop`, stable CDP target и отдельный visual/
  profiling workflow.
* Owner review запретил automated `Page.bringToFront`, AI macOS screenshots и
  другое управление интерфейсом: canvas evidence читается background CDP.
* UI-002 result `efbad1689` создал `@ui/playground`, поэтому общий skill теперь
  имеет package-level owner в `pkg/ui`.
* По прямому решению владельца работу выполняет тот же пользовательский чат,
  который создавал NODES-017.7, после чего root интегрирует result в node line.

## Решения владельца

1. Новый path: `pkg/ui/.agents/skills/ui-dev`; старый
   `pkg/nodes/.agents/skills/node-system-dev` удаляется переносом без duplicate.
2. Skill охватывает UI Elements, UI Components, shared UI Playground fixtures,
   Node UI и при необходимости Node layout playground как отдельный non-WebGPU
   contour.
3. Dispatcher скрывает package-specific cwd, command, env port, PID/log/target
   и реализует `status/start/restart/logs/stop/health` по образцу metafor-dev.
4. Любой listener сначала точно идентифицируется. Foreign process сохраняется;
   duplicate/adoption/kill по номеру порта запрещены.
5. Browser operations работают по exact target URL/ID в фоне. Automated path
   не вызывает `Page.bringToFront`, `/focus`, window APIs или AI macOS.
6. Canvas capture — `toDataURL` через CDP. Mobile emulation обязательно
   возвращает native metrics даже после failure.
7. Visual profiling включает DOM/console/canvas, browser performance metrics,
   frame/heap evidence и routing к внешнему WebGPU Inspector workflow без
   instrumentation в production source.
8. Skill не меняет UI/Node production implementation и не подменяет explicit
   owner acceptance automated capture.

## Dispatcher contract

```text
ui-dev.sh status  <checkout> <playground>
ui-dev.sh start   <checkout> <playground>
ui-dev.sh restart <checkout> <playground>
ui-dev.sh logs    <checkout> <playground>
ui-dev.sh stop    <checkout> <playground>
ui-dev.sh health  <checkout> <playground>
```

Registry для каждого selector задаёт package owner, cwd, start command, exact
origin/port, ready marker и WebGPU/canvas capability. Недоступный ещё package
playground возвращает typed unsupported state, а не выдумывает command.

## Browser contract

Background helper принимает selector либо exact origin, находит ровно один page
target, выполняет reload/DOM/console/canvas/viewport/profile и никогда не меняет
OS/browser focus. Route-specific URLs поддерживаются полностью. Profiling
возвращает source target ID, viewport/DPR, `Performance.getMetrics`, heap/frame
samples и явную границу GPU capture.

## Подзадачи

### UI-003.1 — Перенести skill в общий UI owner

* Статус и исполнитель: `IN_PROGRESS`, `/root` в отдельном worktree
  `codex/ui-dev-skill`.
* Классификация: принятый owner-ом implementation-срез той же UI-003; один
  изменяемый механизм — versioned `ui-dev` skill и его собственная проверка.
* Требование: заменить node-local skill одним общим UI owner с registry-driven
  lifecycle, background exact-CDP evidence и structured profiling.
* Основание и связанная история: result `7fd390923`, lifecycle correction
  `e29d374fd`, package owner `efbad1689` и прямое решение владельца в этой
  карточке.
* Наблюдаемое расхождение: текущий helper знает только Node UI 4016, содержит
  отдельный focus action и не владеет Components либо shared UI fixture.
* Причина: selector contract, process identity и browser/profiling policy не
  вынесены в общий UI registry.
* Разрешённое изменение одного механизма: перенести skill целиком в
  `pkg/ui/.agents/skills/ui-dev`, заменить helpers и references, добавить только
  его tests/assets; production UI/Node/playground source не менять.
* Regression или опровергающее доказательство: isolated Node UI и Components
  lifecycle, foreign listener preservation, restart/logs/stop, source no-focus
  assertion, route-specific background canvas, viewport restore и structured
  performance profile.
* Среда и критерий приёмки: exact checkout/branch, test-owned ports и background
  CDP targets; automated evidence явно не является owner acceptance.
* Подготовительный commit: `213013023`.
* Фактические действия: skill и maintained Blender asset перенесены в
  `pkg/ui/.agents/skills/ui-dev`; добавлены selector registry, foreground
  dispatcher, direct background-CDP helper, profiling reference и isolated
  tests. Старый skill path удалён без alias.
* Результат и вывод: Node UI и Components lifecycle доказаны на test-owned
  ports; Components route capture/profile и Node touch работают по exact target,
  mobile metrics восстанавливаются. Production source не менялся.
* Result checkpoint: текущий result commit, переводящий UI-003 в `REVIEW`.

## Границы

* Менять только skill directory, его tests/assets/references и project files.
* Не менять production UI/Node, package playground implementation либо NODES-018.
* Не копировать metafor-dev целиком: переиспользовать его законы, сохраняя
  отдельный UI/package lifecycle.
* Не использовать detached/nongit temp checkout для result.

## Критерии готовности

1. Skill проходит `skill-creator` validation и имеет точный discriminating
   description/openai metadata.
2. Registry и dispatcher покрыты isolated lifecycle tests минимум для two
   selectors, foreign listener и restart/logs/stop.
3. Background exact CDP capture доказан без focus events.
4. Desktop/portrait/landscape restore и route-specific target проходят.
5. Performance evidence structured, repeatable и не требует source changes.
6. Старый node-local skill отсутствует, Node workflow описан в UI skill.
7. Result commit находится в отдельном UI skill worktree и готов к интеграции.

## Результат

* `ui-dev` является единственным discoverable skill для UI/Node package
  playground. Registry содержит `node-ui`, `components`, diagnostic
  `ui-fixture` и typed unsupported `elements`; solver-only Node layout не
  включён из-за текущего HMR contour.
* Dispatcher возвращает structured state с exact cwd, argv, env port, origin,
  ready/canvas capability, process start identity, PID и log. `start/restart`
  живут в long-lived PTY; foreign listener не присваивается и не останавливается.
* Browser helper создаёт route-specific target с `background:true`; в source
  отсутствуют bring-to-front, focus/activate/window, screenshot и AI macOS
  operations. Canvas читается через `toDataURL`.
* Components route `/button/basic/text` доказан на test-owned `45117` и live
  foreign `4017`: до/после activity emulation target `hidden`, `focused=false`,
  console 0; portrait `390×844 @2`, landscape `844×390 @2` и native
  `1920×1088 @2` прошли без horizontal overflow.
* Structured profile вернул `Performance.getMetrics`, heap before/after и 10
  положительных rAF deltas; GPU evidence явно направлено во внешний Inspector
  без source/runtime instrumentation.
* Node UI test-owned `45116` доказал exact background target, canvas и atomic
  pan/pinch (`x≈42`, `y≈340`, `scale 0.3125→0.5529`) с native restore.

## Проверка результата

* `skill-creator quick_validate.py` — valid.
* Strict standalone TypeScript helper/tests — pass; Bash syntax, JSON registry,
  no-focus source assertion и `git diff --check` — pass.
* Skill lifecycle suite — `5/5`: Node UI, Components restart/new PID,
  logs/health/stop, foreign preservation и Elements unsupported.
* `@ui/components` — `14/14`; `@ui/playground` — `7/7`; `@nodes/ui` — `26/26`.
  Одновременный общий Bun run воспроизвёл существующую конкуренцию двух
  `Bun.build` (`EISDIR/Unexpected reading file`); те же owner suites и оба
  build-boundary tests проходят раздельно.
* Typechecks `@ui/components`, его playground, `@ui/playground`, `@nodes/ui` и
  его playground — pass.

## Closing handoff

Затронут только versioned development skill и его собственные
references/assets/tests. Package requirements, public types и production
UI/Node/playground source не менялись. Постоянным владельцем operational
contract является `pkg/ui/.agents/skills/ui-dev`.

Integrated main содержит единственный `pkg/ui/.agents/skills/ui-dev`; старый
node-local skill и compatibility route отсутствуют. Node comparison обслуживает
`/ui-dev/blender-4.5.5-reference.png` прямо из UI owner asset. Dispatcher
поднял canonical Node/Components/fixture/Elements PID
`31521/31690/31910/67883`. После owner correction каждый selector имеет один
stable target: Node `809BF…`, pre-existing Components `D077…`, fixture
`1AFE2E…`, Elements `E208…`; routes навигируются в той же вкладке. Все четыре
прошли DOM/console/canvas, desktop/portrait/landscape restore и structured
profiling там, где применимо. Automated evidence не является physical device,
GPU timing либо owner acceptance.

Integrated result: merge `46c0311c6`, Node/public-shell integration
`24af6c2a5`, shared retained surfaces `28ed2ce06`, Components consumer
`41970d955`. Affected integrated tests/typechecks и no-focus source assertions
проходят. Независимая closing review должна подтвердить operational owner,
отсутствие duplicate skill/path и полноту references.

## Состояние

`REVIEW`: первый closing verdict corrections завершены: AGENTS routing
`58fdc3491`, texture readiness/capture `f1b69ebd9`, singleton target contract
`9e87c9bac`, Elements restore/evidence `6100a8289`. Ожидается повторный
независимый verdict.
