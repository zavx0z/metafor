# UI-004 — Интегрировать retained UI и общий UI skill в main

## Коротко

Собрать в canonical `main` расходящиеся UI/Node результаты, сначала принять
общий `UI dev` и поднять через него существующие playgrounds, затем перенести
закрытый retained parent/child mechanism и довести Elements, Components и их
consumers до одного engine-owned hierarchy. После интеграции package playgrounds
должны работать на общем публичном shell, а NODES-017 продолжает визуальную
доводку без возврата к плоскому immediate-mode пути.

## Зачем

Ветка UI development уже содержит восстановленный Components playground,
принятый `@ui/playground` и общий skill, а закрытая NODES-018 независимо
реализовала retained `Object3D` hierarchy. Простое слияние любой одной ветки не
даёт целого: актуальный `main` сохраняет параллельную Hamiltonian project line,
Node playground WIP не запустил browser entry, Node comparison использует
перемещённый reference asset, а Elements/Components ещё не имеют полного retained
inventory и migration proof.

## Связь с дорожной картой

Работа продолжает действующее направление UI/Node observability из раздела
`Наблюдаемость и управление Hamiltonian`, но не мигрирует Hamiltonian consumer и
не меняет semantic Node/Parameter/Field design. Постоянные retained законы уже
принадлежат Engine, `@ui/elements`, `@ui/components` и `@nodes/ui`; эта карточка
владеет только интеграцией, полным inventory, миграционными срезами и evidence.

## Связанные задачи и история

* NODES-016 result `7aab6269a` создал FlexBox-oriented component library, но её
  UI surface оставалась плоской immediate-mode materialization.
* NODES-017 research `5ca434ae4` доказал независимые visual scale floors,
  повторный Node plan и необходимость одного inherited transform. Blender-style
  Node, Parameter и Field design остаются заморожены до отдельной visual стадии.
* NODES-018 results `57c665558`, `0f511a575`, `a4d611767`, `00d982041` и
  `0a4ce7f81` реализовали retained `UiSurface`, один `NodeCanvas` content-root,
  matrix clip/culling/input и exact standalone proof. Owner принял NODES-018;
  closing `d3a9bfa35` удалил её временную карточку и артефакты, поэтому UI-004
  не восстанавливает закрытую задачу.
* UI-001 result `d0f6f702d` восстановил historical Components playground после
  слишком широкого удаления `0b3b0b2ca`. Исторический Elements playground тем
  же commit был удалён, но отдельного принятого решения восстанавливать его нет.
* UI-002 result `efbad1689` и closing `79e3b65e4` создали public
  `@ui/playground` с typed router, FlexBox shell и no-HMR server.
* UI-003 result `60e0b58c1` перенёс node-local helper в единственный
  `pkg/ui/.agents/skills/ui-dev`, сохранил exact process ownership и запретил
  browser focus. UI-003 находится в `REVIEW` до независимой проверки уже в
  integrated `main`.
* NODES-019 WIP `05658b094` начал перенос Node playground на public shell, но
  isolated origin `4019` отдал HTML без исполняющегося entry: readiness marker
  отсутствовал, canvas остался `300×150` black, console был пуст. WIP не принят.
* Все три source branches имеют общий merge-base
  `625b9dfa5eb4ac8f676f569d6b61ae64a94aa961`; canonical baseline этой задачи —
  clean `main` `60038868822bdbe92b432965fdb23ae5f40a45a5` с параллельной
  MF-411.1 line. Project files source branches не заменяют актуальный main graph.

## Решения владельца

1. Работа выполняется прямо в canonical checkout и branch `main`; новый branch,
   clone или worktree запрещён. Integration/merge/cherry-pick разрешены, push —
   только по отдельному запросу.
2. Причинный порядок: сначала интегрировать `codex/ui-dev-skill`, затем через
   уже интегрированный `UI dev` поднять и проверить все существующие
   playgrounds, и только после этого интегрировать `codex/nodes-018`.
3. После retained merge сознательно адаптировать WIP `codex/node-layot` к
   integrated public shell; не выбирать project/Node conflicts целиком одной
   стороной.
4. Engine `Object3D` остаётся единственным scene graph. Parallel transform tree,
   local visual scale floors и DOM/CSS renderer запрещены.
5. System называется `FlexBox`; CSS-style является только declarative
   description того же layout.
6. Existing Blender-style Node, Parameter и current Field не заменяются до
   отдельной Blender+Field design stage. В UI-004 меняются retained mechanics и
   dev playground composition.
7. UI Elements/Components больше не frozen: UI-004 полностью мигрирует каждый
   scalable/nested visual subtree, которому полезны parent transform и retained
   lifecycle. Осознанный flat immediate path остаётся только там, где этих
   свойств нет, и получает явную границу в owner doc.
8. Browser/canvas evidence выполняется только background exact CDP через
   `UI dev`. Запрещены `Page.bringToFront`, focus/activate/window APIs,
   AI macOS и screenshot services; canvas читается через `toDataURL`, mobile
   metrics всегда восстанавливаются в `finally`.
9. Existing listeners/processes/targets сохраняются: foreign process не
   присваивается, duplicate не запускается, lifecycle относится только к exact
   checkout/selector/PID/command/listener.
10. Pre-closing проверки NODES-018 не повторяются как приёмка старой ветки;
    проверяется только новый integrated `main`.

## Целевой retained закон

```text
dirty local FlexBox plan
          ↓
atomic materialization changed subtree under exact Object3D parent
          ↓
parent transform only
          ↓
matrixWorld / inverse → frame, clip, culling, hit and wheel
```

Visual Text, Icon, Button, Field, Switcher, padding, gap, radius, border,
stroke и Socket наследуют parent transform. Screen minimum существует только
как отдельный невидимый hit target. Recursive remove/rematerialize/dispose
очищает owned subtree и interaction records без orphan либо double disposal.

## Подзадачи

### UI-004.1 — Зафиксировать integration baseline в main

Статус и исполнитель: `COMPLETE`, руководитель текущей задачи.

Классификация: диагностический project slice; один результат — доказанный
baseline, причинный порядок integration и границы сохранения main.

Требование или диагностический результат: полностью прочитать действующие
rules/owners/project state и source lineage, вычислить merge bases, проверить
root/branch/HEAD/status/worktrees и зарегистрировать одну authoritative UI-004.

Основание и связанная история: owner delegation и commits, перечисленные в
разделе `Связанные задачи и история`.

Наблюдаемое расхождение: source branches владеют расходящимися project graphs и
runtime helpers; canonical main не содержит ни одного integrated UI/Node result.

Причина: три линии развивались от общего старого merge-base независимо, пока
main продолжал Hamiltonian/MF-411 work.

Разрешённое изменение одного механизма: только `project/TODO.md` и эта карточка.

Regression или опровергающее доказательство: root/branch/HEAD/status/worktree
gate, exact merge bases и `git diff --check`; production/runtime не меняются.

Среда и критерий приёмки: canonical clean `main`; отдельный preparation commit
до integration и delegation.

Фактические действия: полностью прочитаны действующие правила, project state,
Engine/UI/Node owners, карточки UI-001/UI-003/NODES-017/NODES-018/NODES-019 и
source lineage; для трёх веток подтверждён общий merge-base `625b9dfa5`, а
canonical baseline `600388688` — clean main с отдельной MF-411.1 line.

Результат и вывод: UI-004 зарегистрирована одной authoritative задачей с
owner-corrected порядком `UI dev → все playgrounds → NODES-018`; production и
runtime не изменялись.

Result checkpoint: `bf28bed7a84ce7befcaac16f8308ffc79e442bf1`.

### UI-004.2 — Интегрировать общий UI owner и skill

Статус и исполнитель: `IN_PROGRESS`, внутренний исполнитель
`UI-004.2 — Интегрировать общий UI owner и skill`.

Один механизм: causal merge `codex/ui-dev-skill` в актуальный main с сохранением
main project/owner docs, принятием public `@ui/playground`, Components playground
и единственного `UI dev`; старый node-local skill и compatibility alias
отсутствуют.

Проверка: exact merge diff, skill validation/tests, package typechecks и
authoritative reconciled project graph.

### UI-004.3 — Поднять существующие playgrounds через UI dev

Статус: `WAITING` до UI-004.2.

Один результат: dispatcher `status/start/health/logs` точно классифицирует и,
где это безопасно, поднимает `node-ui`, `components`, `ui-fixture`, а `elements`
честно возвращает `unsupported`. Foreign 4016/4017 listeners и все переданные
targets сохраняются; exact DOM/console/canvas доказывают baseline до NODES-018.

### UI-004.4 — Интегрировать закрытый retained результат NODES-018

Статус: `WAITING` до UI-004.3.

Один механизм: causal merge closed `codex/nodes-018`, где retained Engine/UI/
Node core побеждает flat path, но integrated UI skill/public playground/project
state сохраняются. Временная NODES-018 card/artifacts и old node-local skill не
восстанавливаются.

### UI-004.5 — Зафиксировать полный retained inventory

Статус: `WAITING` до UI-004.4.

Один диагностический результат: перечислить каждый Elements, Components и
relevant Surface/playground subtree как already retained, requires retained
migration либо consciously flat; связать scalable/nested candidates с exact
parent ownership, dirty trigger, materialization и transform-only criterion.

### UI-004.6 — Мигрировать scalable UI Elements subtrees

Статус: `WAITING` до UI-004.5.

Один механизм: Elements primitives/containers автоматически materialize под
существующим exact retained parent, сохраняют local geometry и instrumentation;
flat boundary не расширяется и не создаёт второй transform tree.

### UI-004.7 — Мигрировать UI Components subtrees

Статус: `WAITING` до UI-004.6.

Один механизм: representative Button/Field/Switcher/nested controls получают
retained parent ownership и dirty-local materialization. Pure parent transform
не увеличивает layout/materialization counters и не меняет child identity.

### UI-004.8 — Завершить public playground consumers и NODES-019

Статус: `WAITING` до UI-004.7.

Один механизм: адаптировать WIP `05658b094` к retained integrated main, исправить
entry/server routing и bind Node comparison asset к новому UI skill owner без
старого `/node-system-dev` path. Components и Node используют public
`@ui/playground`; Elements не изобретается заново без отдельного owner decision.

### UI-004.9 — Доказать integrated result и согласовать project state

Статус: `WAITING` до UI-004.8.

Один verification result: root/package typechecks, relevant Engine/UI/Node
suites и boundaries; background exact DOM/console/canvas desktop/portrait/
landscape с native restore; transform-only performance для representative
Element/Component/Node. После независимой closing проверки закрыть UI-003,
UI-001 и NODES-019, перевести UI-004 в `REVIEW` либо закрыть при полном verdict,
а NODES-017 вернуть к retained visual corrections без ложного закрытия physical
mobile и owner acceptance.

## Границы

* Не менять semantic Node/Parameter/Field shape, layout solver и Hamiltonian
  product consumer в интеграционных срезах.
* Не читать и не менять архивный `/Users/zavx0z/production`.
* Не создавать новый branch/worktree, не переключать main и не выполнять push.
* Не перезапускать, останавливать или присваивать существующий listener без exact
  ownership evidence UI dev.
* Не считать test, isolated origin, canvas capture, emulation либо profile
  owner-visible или physical-device acceptance.
* Не восстанавливать исторический Elements playground автоматически.

## Критерии готовности

1. `main` содержит explicit causal integration commits всех трёх source lines и
   сохраняет параллельные main changes.
2. Единственный `UI dev` управляет package playground lifecycle, а старый
   node-local skill/path отсутствует.
3. Public `@ui/playground` обслуживает Components и Node routes; shared fixture
   также работает, Node entry исполняется, Blender reference загружается из UI
   owner asset.
4. Exact retained inventory закрывает все scalable/nested Elements, Components
   и relevant consumer subtrees либо документирует осознанную flat boundary.
5. Pure parent transform не растит layout/materialization counters у
   representative Element, Component и Node, при сохранении child identities.
6. Text/Icon/Button/Field/Switcher, padding/gap/radius/stroke наследуют engine
   parent transform; hit minimum остаётся невидимым и отдельным.
7. Root/package typechecks, relevant tests, boundaries и exact background browser
   matrix integrated main проходят; native metrics восстановлены.
8. UI-003 независимо закрыта после integrated acceptance, NODES-019 завершена,
   UI-001 reconciled, NODES-017 снова выполняется на retained foundation.
9. Agreed playground contours оставлены в точном состоянии с отчётом PID,
   selector, URL/target и remaining owner/physical gates.
10. Перед handoff canonical `main` clean; push не выполнялся.

## Состояние

`IN_PROGRESS`: UI-004.1 завершена checkpoint `bf28bed7a`; выполняется UI-004.2.
Production integration ещё не началась.
