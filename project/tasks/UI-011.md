# UI-011 — Перевести все package playgrounds на Workbench

## Коротко

Перевести `@nodes/ui`, `@ui/elements` и `@ui/components` с прежних ручных
route/info screens на принятый `@ui/playground` Workbench: package-owned
stories, конкретные sections, production preview, variants, TypeScript/copy и
controls/events.

## История и подтверждённое расхождение

* NODES-019 разделила перегруженный Node экран на старый пятипанельный catalog,
  но была закрыта до появления typed stories и правой code/copy panel.
* UI-007 создала shared Workbench contract и эталон `4192/overview`, но
  ошибочно была закрыта без миграции package consumers.
* UI-001.4 подготовила отдельный Components-owned перенос. По следующему прямому
  решению владельца её внутренний исполнитель остановлен до code patch, а
  execution/closing UI-001 переданы руководителю UI-011. Соседняя задача владеет
  только production Components и Node components.
* Owner screenshot `project/artifacts/UI-011/owner-node-socket-gap.png`
  показывает `4016/socket/types`: вторая панель содержит абстрактные
  `Типы / Формы / Состояния`, center — aggregate grid 19 Socket types, справа —
  статический `Socket contract`. Это не принятая Workbench structure.
* Current Node и Elements entries создают `PlaygroundInfoSurface` и вручную
  дублируют catalog/sections/dock routes; package-owned stories отсутствуют.

## Решения владельца

1. Все standalone UI playground используют один принятый Workbench composition:
   catalog слева, конкретные sections во второй панели, production preview в
   центре, variants снизу, постоянный TypeScript/copy и controls/events справа.
2. Shared shell/Engine/UiRuntime сохраняются. DOM/iframe/Storybook runtime,
   второй renderer и Blender dataset не добавляются.
3. Package consumer владеет story metadata и lazy implementation; shared
   package не получает Node/Socket/Field vocabulary.
4. В Node UI `Socket` является component. Во второй панели перечисляются
   конкретные Socket types: Boolean, Float, Integer, Vector, Rotation и все
   остальные реализованные типы, а не мета-разделы `Типы / Формы / Состояния`.
5. Для выбранного Socket center показывает один production preview. Dock хранит
   direction variants `input / output / bidirectional`; shape и interaction
   state меняются controls справа и обновляют тот же preview/source state.
6. Aggregate inventory допускается только как отдельная явная documentation
   story, но не заменяет detail story выбранного component.
7. Visible navigation/controls/docs — по-русски; API identifiers, routes,
   import specifiers и TypeScript остаются точными.

## Подзадачи

### UI-011.1 — Перевести Node Socket на detail stories

Статус и исполнитель: `COMPLETE`, руководитель текущей задачи.

Ввести package-owned Socket story registry/lazy module. Вторая панель получает
все exact Socket types, center — выбранный Socket, dock — direction variants,
right panel — рабочий `@nodes/ui/blender-node` TypeScript/copy и controls.
Старый `SocketCatalogSurface` aggregate grid не является default detail route.

Результат: Node Socket стал package-owned typed catalog с `19` concrete type
sections и тремя direction variants. Lazy module импортирует production только
через `@nodes/ui/blender-node`; selected Socket, shape/selected controls,
preview и копируемый TypeScript используют одно story state. Старые
`/socket/types|shapes|states` нормализуются в `socket/boolean/input`.

Code checkpoint `348453120`; permanent law `778889c4a`. Node suite прошла
`43/43` (`1223` assertions), package/playground/shared typechecks и diff-check
зелёные; split build содержит отдельный lazy Socket chunk `3.79 KB`.
После production Socket calibration `574de1db6` exact restart дал PID `24430`;
target `809BF08D88E4582CA819EFE847FE1450` сохранён. DOM: ready, `19` sections,
`3` variants, exact source/args; console `0`, native `1920×1088 @2`, non-black
canvas `619169` bytes. Automated capture не является owner acceptance.

### UI-011.2 — Перевести остальные Node UI routes

Статус: `COMPLETE`, руководитель текущей задачи.

NodeEditor, Frame, Link и comparison получают stories того же contract без
статического `Node components`/`Blender reference` info panel.

Результат: catalog содержит отдельные NodeEditor, Frame, Link, Socket и
Comparison components. Typed lazy modules владеют routes/source/controls;
`PlaygroundInfoSurface` удалён, StoryPanel постоянно занимает правую панель.
Actual production NodeEditor, selected Frame/Link и Blender reference/live Node
остались настоящими Surface previews и не заменены mock-карточками.

Code checkpoint `77bfa7264`: Node suite `48/48` (`1370` assertions), package и
playground typechecks, diff-check зелёные. Split build выдаёт отдельные lazy
`node-components`, `node-editor`, `blender-node`, `link-curve` chunks. После
production header `860076720` один restart дал PID `38236`; четыре routes
достигли ready, console `0`, native `1920×1088 @2`. Exact target сохранён.

### UI-011.3 — Перевести Components playground

Статус и исполнитель: `COMPLETE`, внутренний исполнитель текущей задачи.

Button, Pane и реализованный Field catalog из UI-001.4 получают package-owned
stories, русский catalog, production preview и exact Components TypeScript.
UI-011 владеет execution/closing UI-001, но не меняет production Components.

Результат: manual routes/Fields entry заменён metadata-first registry на `17`
concrete Components и все прежние Button/Pane/Field routes. Все `10` Field
kinds являются sections; Badge, TextField, Divider, Scrollbar, List, Table и
Noti сохранены явно. Right StoryPanel заменил static Info, visible UI русский,
lazy modules используют exact production subpaths.

Source checkpoint `b1a113491`: Components suite `36/36` (`524` assertions),
package/playground typechecks, split build и diff-check зелёные. После
production NumberInput `99f80c0e2`, ColorInput `c935de436`, VectorInput
`b926baf00` один restart дал PID `44440`; Number/Color/Vector Field routes ready,
console `0`, native `1920×1088 @2`, non-black desktop captures сохранены.

### UI-011.4 — Перевести Elements playground

Статус и исполнитель: `COMPLETE`, внутренний исполнитель текущей задачи.

Primitives/Layout/Style/Events публикуют конкретные Elements stories,
production preview и копируемый exact Elements TypeScript.

Результат: `11` конкретных Elements и `42` detail stories заменили manual
route/info wiring. Все `12` прежних paths нормализуются; четыре lazy modules
импортируют production только через exact public leaves. Source checkpoint
`ee3a47350`, permanent law `66a273c6a`; Elements `70/70` (`506` assertions),
playground `9/9` (`179`), оба typecheck, split build и diff-check зелёные.

После одного restart PID `55110` и exact target
`E2087390E08913DD8CA4142D5D9E8C48` сохранены. Primitives, Flex CSS, Theme и
Pointer routes готовы, console `0`, canvas `3840×2176`; renderer-activity
desktop captures non-black, native `1920×1088 @2` после каждого прогона
восстановлен. Первые idle-black captures отклонены.

### UI-011.5 — Добавить выбранное состояние Node в story

Статус и исполнитель: `IN_PROGRESS`, руководитель текущей задачи.

Классификация: новое наблюдаемое требование того же parent и Node playground
owner; это не продолжение уже завершённой UI-011.2 и не пробел только в
доказательстве.

Требование: NodeEditor detail story публикует exact variants для ordinary и
selected expanded Node, а также ordinary и selected collapsed Node, и применяет
выбранное состояние к production Node той же сцены. Route metadata, args,
preview, source и controls используют одно состояние без manual CDP или OS
input; прежний `node-editor/scene/default` остаётся ordinary expanded route.

Основание и история: UI-011.2 перенесла NodeEditor на общий Workbench, но её
single `node-editor/scene/default` story не публиковала Node selection. Live gate
production shadow checkpoint `b9f9419fb` потребовал ordinary и selected state.

Наблюдаемое расхождение: current story публикует `selected:false`, не имеет
selected Node control/variant, а `applyProductionStoryState` очищает selection.
Existing selected Frame/Link stories выбирают других owners и не доказывают
selected Node shadow.

Причина: UI-011.2 сохраняла полную scene composition, но selected Node state не
был записан в её story metadata и production-state adapter.

Разрешённое изменение одного механизма: только
`pkg/nodes/ui/playground/**` — добавить controlled selected Node story state,
точный production selection и focused regression. Production `@nodes/ui`,
shared Workbench и другие playground не меняются.

Regression и приёмка: metadata/route/controls/source однозначно задают target
`expanded | collapsed` и state `ordinary | selected`; DOM сообщает exact
selected Node id либо пустое selection, console `0`, четыре non-black captures
получены на существующем `node-ui` target после одного restart. Automated
capture не является owner acceptance.

Подготовительный commit: этот project checkpoint, до source patch.

### UI-011.6 — Закрыть общую acceptance matrix

Статус: `WAITING` на UI-011.5.

`4016`, `4017`, `7901`, `4192` используют один interaction contract; на каждом
exact target проверяются representative detail route, code/copy, controls,
variants, lazy chunk, DOM/console и non-black desktop canvas.

## Границы

* Playground changes ограничены `pkg/nodes/ui/playground/**`,
  `pkg/ui/components/playground/**` и `pkg/ui/elements/playground/**`.
* Не менять production `pkg/ui/components/*.ts`, `pkg/nodes/ui/*.ts`, package
  exports либо shared `pkg/ui/playground/**` без нового owner slice.
* Не менять production component semantics ради catalog presentation и не
  дублировать production implementations в stories.
* Не удалять Node Blender comparison, но оставить его package-owned reference,
  а не структуру всего catalog.
* Lifecycle/browser evidence выполнять только `$ui-dev`; один target на origin
  сохраняется.
* Не трогать unrelated Hamiltonian files и параллельные Node layout задачи.

## Критерии готовности

1. Node/Components/Elements entries не создают `PlaygroundInfoSurface` вместо
   code/copy.
2. Все три consumers используют typed story metadata и lazy production imports.
3. Node Socket second panel перечисляет все реализованные Socket types; выбор
   синхронно обновляет preview, dock variants, source и controls.
4. NodeEditor story явно переключает ordinary/selected production Node без
   manual browser input и синхронно обновляет args, source и preview.
5. Components и Elements имеют ту же структуру без consumer vocabulary в
   shared package.
6. UI-001 execution/closing завершены этим руководителем без production patch.
7. Visible UI полностью русский; exact code/API/routes не переводятся.
8. Focused tests/typechecks и четыре live desktop contours проходят; screenshot
   не считается owner acceptance.

## Состояние

`IN_PROGRESS`: Node, Components и Elements consumers перенесены; UI-001 закрыта
commit-ом `63c8c1983`. Current slice UI-011.5 добавляет отсутствующий selected
Node story для production shadow gate; общая matrix UI-011.6 ждёт его.
