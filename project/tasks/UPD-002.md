# UPD-002 — Обновлять всю клиентскую сборку через Service Worker

## Коротко

Service Worker будет отвечать за атомарную смену всей загружаемой браузерной
части Hamiltonian после неизменяемого startup. Он заранее скачает и проверит
новый набор `import`/`internal`/`metafor`, затем переключит страницы на него
одним reload без промежуточной смеси старых и новых файлов.

## Зачем

После `UPD-001` весь код обновлений собран под одной физической границей, но
действующее поведение всё ещё разделено на три параллельных пути:

* сам Service Worker обновляется через browser-managed `registration.update()`;
* Service Worker отдельно проверяет и кеширует один маленький versioned module,
  выбранный вручную через `HAMILTONIAN_VERSION`;
* вся настоящая клиентская сборка определяется host source fingerprint, после
  чего страница сама решает выполнить reload.

Такое устройство сохранило прежний экспериментальный механизм, но не даёт
Service Worker полной ответственности за загружаемый браузерный выпуск. Новый
clean-room loader из `LOAD-001` уже отделил неизменяемый startup от сменяемых
importers и modules; эта задача должна продолжать именно его, а не прежний
prototype release path.

## Связь с дорожной картой

Это вторая задача линии
[`Обновления Hamiltonian`](../ROADMAP.md#обновления-hamiltonian). Закрытая
`UPD-001` создала в прототипе структуру
`hamiltonian/update/{shared,host,browser}` без изменения поведения. Новый
update-механизм не переносит оттуда source автоматически и строится поверх
фактического loader contract `LOAD-001`.

Задача начинается после
[`LOAD-001 — Загружать браузерный функционал через минимальный Service
Worker`](LOAD-001.md). `LOAD-001` определяет неизменяемый startup, обновляемые
importers, `fetch` loading, пространства `internal`/`metafor` и независимые
caches; `UPD-002` затем определяет полный состав и атомарную смену
многосоставного functional release после startup.

## Зависимость

Состояние `IN_PROGRESS`. Владелец признал доказанный текущий loader checkpoint
достаточным для локальных development update-срезов; `LOAD-001` остаётся
открытой для собственной незавершённой работы и больше не блокирует эту задачу.

## Связанные задачи и история

* Закрытая `MF-412` ввела `HAMILTONIAN_VERSION` и отдельный versioned module как
  испытание одинаковых bytes и SHA-256 в Window, Dedicated Worker и Bun process.
  Это был standalone proof, а не окончательная модель клиентской сборки.
* Закрытая `MF-428` отдельно доказала SemVer и browser-managed обновление самого
  Service Worker с сохранением logical identity и новой execution incarnation.
* Закрытая `UPD-001` намеренно только перенесла существующие решения. Её
  инвентаризация зафиксировала два самостоятельных browser-перехода: обновление
  Service Worker и reload всей отданной host клиентской сборки по source
  fingerprint.
* `LOAD-001` доказала цепочку `startup → import → internal`, загрузку code bytes
  через `fetch`, запуск internal RPC внутри Service Worker и восстановление
  caches. WSS принадлежит RPC/control, а не delivery code bytes.
* Владелец выбрал Service Worker единственным владельцем атомарной смены всей
  загружаемой клиентской части после startup. Серверные процессы остаются
  отдельной будущей работой.

## Подтверждённые факты

Следующие пункты описывают прежний prototype update path, а не clean-room
loader `LOAD-001`:

* Текущий Service Worker перехватывает и обслуживает из Cache Storage только
  URL под `/versions/`, а не полный набор файлов клиентской сборки.
* Текущий top-level manifest описывает один синтетический module через ручные
  `version`, `moduleUrl` и `sha256`; выпуск самого Service Worker находится в
  отдельном поле `serviceWorker`.
* Настоящие HTML, JavaScript, CSS, orchestration, layout Worker и Web Push
  client сейчас входят в host source fingerprint и отдаются напрямую host.
* Page принимает `source-update`, самостоятельно сравнивает fingerprint и
  вызывает reload независимо от release cache Service Worker.
* Уже исполняющийся Window main нельзя заменить внутри прежнего JavaScript
  realm; для перехода на новый полный выпуск всё равно требуется reload.
* `HAMILTONIAN_VERSION` также используется испытательными Bun embodiments.
  Service Worker не может обновлять серверные процессы.

## Решения владельца

* Service Worker отвечает за обновление всей загружаемой клиентской части
  Hamiltonian после startup.
* Статические HTML, `@startup/main` и `@startup/service` не входят в сменяемый
  functional release этой задачи.
* Release состоит из согласованного набора importers, `internal`/`metafor`
  modules и принадлежащих им ресурсов; cache ownership из `LOAD-001`
  сохраняется.
* Code bytes и manifest загружаются через `fetch`. Internal RPC WSS может
  сообщить актуальный source address, но не переносит artifact bytes.
* Идентичность клиентского выпуска выводится из точных файлов и хешей сборки, а
  не задаётся вручную значением `v1`, `v2` или временем запуска host.
* Host собирает и атомарно публикует выпуск и manifest, но не принимает за page
  самостоятельное решение о reload.
* Page выполняет один reload только после сообщения Service Worker о полностью
  готовом выпуске.
* Обновление серверных процессов не входит в эту задачу и позже получает
  отдельный server-release механизм.

### Требования к собираемому пакету

* Каждый собираемый browser artifact, включая фиксированные `@startup/*` и
  сменяемые `@import/*`/`@internal/*` modules, оформляется отдельным
  workspace-пакетом. Поле `name` его `package.json` является единственным
  каноническим именем модуля в build-запросе, результате сборки и уведомлении об
  обновлении.
* Корневой export пакета обязан вести на собираемый клиентский entrypoint. Если
  `exports` является объектом с дополнительными server или route subpath, в нём
  обязательно присутствует `"."`; внутренний subpath не используется как имя
  модуля при сборке.
* Пакет обязан объявлять строгий `scripts.typecheck` и lifecycle
  `scripts.prebuild`, запускающий `bun run typecheck`. Ошибка `prebuild` не
  допускает запуск основной сборки.
* `scripts.build` является непосредственно исполняемым выбором package-owned
  режима: только `NODE_ENV=development` запускает `scripts.build:development`,
  любое другое значение — `scripts.build:production`. Обе команды указывают
  относительный к package root source entrypoint, все необходимые
  `target`/`format`/`external` параметры и единый outfile `dist/index.js`; host
  не разбирает и не дополняет эти команды.
* Development-сборка минифицируется, сохраняет `console.debug` и включает
  inline source map. Production-сборка выполняется с `--production`,
  минифицируется, полностью удаляет `console.debug` через `--drop` и не создаёт
  source map. Обязательная рабочая логика не помещается в аргументы
  `console.debug`, потому что production drop удаляет и их вычисление.
* Корневой `scripts.dev` явно запускает Hamiltonian с
  `NODE_ENV=development`, чтобы ленивые и явные package builds наследовали
  development-режим. Корневые `scripts.start` и `scripts.build` явно выбирают
  production-режим. Одноразовые клиентские diagnostics, не нужные production,
  записываются через `console.debug`; ошибки остаются в production.
* Запущенный из package root `bun run build` обязан самостоятельно выполнить
  строгую проверку и создать непустой `dist/index.js`. Это одинаковый договор
  первоначальной и повторной сборки.
* Host находит package root по каноническому имени и запускает в нём только
  `bun run build`. Параметры конкретного artifact не дублируются в route,
  macro или runtime build helper.

### Требования к development build endpoint

* Единственный endpoint browser code — `/code`; query parameter `module`
  содержит каноническое package name. `GET` читает package-owned
  `dist/index.js`: если файл существует и непуст, host отдаёт его без сборки;
  если файл отсутствует или пуст, host лениво запускает `scripts.build` и
  отдаёт artifact только после успешного завершения. Одновременные первые
  запросы одного package используют один pending build; после ошибки следующий
  `GET` может повторить попытку.
* Явная повторная сборка выполняется через `POST /code` с единственным JSON-body
  `{modules: string[]}`. Query parameters для POST не поддерживаются. Host
  принимает только непустой массив сменяемых `@import/*` и `@internal/*`
  packages, удаляет повторы и параллельно запускает их `scripts.build`.
* Ответ `POST /code` имеет один формат `{success, results}`; каждый result
  содержит каноническое имя, exit code, полные `stdout`/`stderr` package process
  и metadata artifact. Failed group возвращает `422`, сохраняет прежние готовые
  artifacts и не публикует уведомление.
* После успеха всей выбранной группы host публикует одно RPC WebSocket message
  `{type: "build", modules: [...]}`. Service Worker получает bytes каждого
  artifact отдельным последующим `GET`; WebSocket code bytes не переносит.
* Service Worker принимает только непустой массив известных importer package
  names, заранее загружает и проверяет весь набор, затем заменяет принадлежащие
  packages cache entries. Ошибка fetch не меняет caches, а ошибка записи
  восстанавливает уже заменённые entries и не запускает browser restart.
* Общая сборка всех Hamiltonian packages является только локальной командой и
  не образует update contract. После успешной замены выбранной группы Worker
  закрывает прежний RPC transport, снимает active registration и один раз
  навигирует каждый управляемый Window на его текущий URL. Startup заново
  регистрирует Worker, а новая инкарнация запускает service-side modules из
  сохранённых caches.

## Целевое поведение

1. Host успешно собирает полный сменяемый набор после startup, необходимый для
   рождения нового browser realm, и публикует один manifest с identity выпуска,
   cache owner и SHA-256 каждого artifact.
1. Service Worker обнаруживает новый manifest, скачивает все файлы выпуска и
   проверяет каждый файл до публикации готовности.
1. Неполная загрузка, неверный hash или failed build не меняют активный выпуск;
   страницы продолжают получать прежний полностью готовый набор.
1. Неизменяемые startup scripts, включая executable Service Worker startup, не
   заменяются этим release transition. Новая инкарнация Worker восстанавливает
   активные importers/modules из origin-bound caches.
1. После готовности полного выпуска Service Worker атомарно назначает его
   активным и сообщает каждой управляемой странице exact release identity.
1. Каждая страница принимает одну новую identity один раз, выполняет reload и
   после него получает все сменяемые artifacts одного выпуска через Service
   Worker, сохраняя тот же startup.
1. Закрытая или поздно подключившаяся страница при следующем открытии сразу
   получает текущий активный выпуск без ручной версии host.

## Поведение процесса

Локальные development update-срезы выполняются поверх уже доказанного loader
checkpoint и не объявляются полным release-механизмом. Frozen manifest,
атомарная публикация полного выпуска и удаление прежних prototype paths позже
получают отдельные последовательные срезы. Каждый новый механизм получает
собственную подзадачу и checkpoint; остановленная `.1` не возобновляется.

## Подзадачи

### UPD-002.1 — Зафиксировать полный состав клиентского выпуска

Статус и исполнитель: `STOPPED BEFORE IMPLEMENTATION`; product-исполнитель не
запускался, runtime result отсутствует.

Классификация: исторический contract-срез прежнего prototype release. Он был
остановлен до implementation и не является текущей формой будущего manifest.

Прежнее требование: один browser-safe manifest contract перечислял каждый
канонический файл нового Window realm вместе с SHA-256. После `LOAD-001` эта
граница заменена: будущий manifest должен описывать только сменяемые artifacts
`import`/`internal`/`metafor` после startup и сохранять их cache ownership.

Основание и связанная история: закрытая `MF-412` доказала exact bytes только
синтетического versioned module; `MF-428.3`–`MF-428.8`, result `1579d11cd` и
closing `63be214c6` доказали отдельный lifecycle executable Service Worker;
`UPD-001.1` inventory `f8f18e75f`, result `97442bb8b` и closing `6d07a5046`
собрали update-код в `hamiltonian/update`, но намеренно сохранили оба browser
transition. Новый срез не восстанавливает закрытые задачи, а заменяет их
раздельные release shapes одним договором текущего родителя.

Наблюдаемое расхождение: действующий `HamiltonianBrowserManifest` описывает
только ручной `version/moduleUrl/sha256` и отдельный `serviceWorker`, тогда как
настоящий browser source fingerprint отдельно охватывает bundles и напрямую
served text, исключает бинарный font и не является manifest, проверяемым
Service Worker.

Причина: standalone proof `MF-412`, Worker lifecycle `MF-428` и общий page
source reload развивались как три разных механизма; structural `UPD-001` по
решению владельца не меняла их поведение и договоры.

Разрешённое изменение одного механизма: отсутствует, пока `LOAD-001` не
завершена. После зависимости руководитель регистрирует новый последовательный
contract-срез, не продолжая этот остановленный artifact inventory.

Regression или опровергающее доказательство: будущая frozen проверка должна
отделять неизменяемые HTML/`@startup/main`/`@startup/service` от сменяемых
importers, modules и ресурсов, фиксировать cache owner каждого artifact и
отвергать возврат WSS code delivery. Точный inventory выбирается новым срезом.

Среда и критерий приёмки: canonical `main` из подготовительного commit этого
среза; focused shared/host tests, существующие browser release tests, строгий
host/WebWorker TypeScript check и `git diff --check` проходят, а runtime diff
отсутствует.

Фактические действия:

Результат и вывод:

Подготовительный commit: этот commit.

Result checkpoint:

Решение о продолжении: срез остановлен после owner-решения сначала определить
минимальный browser loader в `LOAD-001`. Его прежний contract сохранён только
как история отвергнутого порядка. После завершения зависимости руководитель
зарегистрирует новый последовательный срез многосоставного выпуска на основе
`startup → import → internal/metafor`.

### UPD-002.2 — Обновлять выбранные модули и один раз перезапускать браузер

Статус и исполнитель: `REVIEW`; текущая задача без субагента.

Классификация: локальный development update выбранного набора modules, а не
полный versioned release и не общая сборка всех Hamiltonian packages.

Требование: `POST /code` принимает один внешний формат для одного и нескольких
modules — один или несколько повторяемых query parameters `module`. Host
отвергает пустой запрос и любое неизвестное или неизменяемое package name до
сборки, удаляет повторы и параллельно запускает package-owned `scripts.build`
всего выбранного набора.

Наблюдаемое расхождение: действующий endpoint собирает и сообщает только один
module, Service Worker обновляет одну cache entry, но уже исполняющиеся Window
и Service Worker modules продолжают работать до новой execution incarnation.

Причина: build notification пока содержит единственное поле `module`, а после
успешного `cache.put` отсутствует transition текущих browser clients и active
Service Worker registration.

Разрешённое изменение одного механизма: если хотя бы одна сборка неуспешна,
host возвращает общий unsuccessful response и не публикует уведомление. После
успеха всего набора host публикует одно сообщение
`{type: "build", modules: [...]}`. Service Worker принимает только этот массив,
заранее получает и проверяет каждый artifact, затем заменяет выбранные cache
entries. После успешной замены всего набора Worker один раз снимает текущую
registration и навигирует каждый управляемый Window client на его текущий URL.
Startup заново регистрирует Service Worker; новая инкарнация загружает
обновлённые service-side modules из сохранённых caches. Startup packages и
Cache Storage при этом не удаляются.

Regression или опровергающее доказательство: один browser test передаёт два
modules, получает один host notification, наблюдает изменение обеих cache
entries, ровно одну navigation каждого из двух управляемых Window clients,
новую Service Worker execution incarnation и восстановленный RPC WebSocket.
Failed group не публикует сообщение, не снимает registration и не перезагружает
страницы.

Среда и критерий приёмки: canonical `main` от baseline `f520aea4a`; package
builds, строгий host/WebWorker TypeScript, focused boundary test, изолированный
Puppeteer browser regression и `git diff --check` проходят. Полный manifest,
hash identity и production release атомарность не входят в этот срез.

Фактические действия: host POST переведён с singular `module` на дедуплицированный
массив повторяемых query parameters и параллельный `Promise.all` package builds.
RPC notification и injected importer binding переведены на единственный массив
`modules`. Importer заранее fetch/verify всего набора, последовательно заменяет
entries с rollback уже применённых записей при ошибке, затем RPC закрывает
старый socket, а importer выполняет `registration.unregister()` и
`WindowClient.navigate(client.url)` для каждого controlled Window.

Результат и вывод: реальный production POST собрал `@import/main` и
`@internal/rpc` параллельно, дедуплицировал повтор и вернул два successful
result. Изолированный Puppeteer regression сначала получил failed group без
изменения caches/navigation, затем обновил оба artifacts, навигировал две
страницы ровно по одному разу, наблюдал новую Service Worker target/incarnation
и восстановленный RPC WebSocket. Focused test прошёл `1 pass`, boundary test —
`1 pass`; package/server builds, test TypeScript и `git diff --check` прошли.
Полный `test:load` дал `3 pass, 1 fail`: остаётся прежнее несвязанное ожидание
удалённого console message `main importer` в LOAD-001 cold-сценарии.

Подготовительный commit: этот commit.

Result checkpoint: этот commit.

### UPD-002.3 — Передавать группу обновления JSON-массивом

Статус и исполнитель: `REVIEW`; текущая задача без субагента.

Классификация: исправление внешнего development API без изменения принятой в
`.2` групповой сборки, уведомления Service Worker и browser restart.

Требование: `POST /code` принимает только `Content-Type: application/json` и
точный body `{modules: string[]}` с непустым массивом. Один module передаётся
массивом из одного элемента, зависимая группа — одним массивом всех package
names. Query parameters, malformed JSON, другая shape и пустой массив
отвергаются до сборки; неизвестные и неизменяемые packages также отвергаются.
Повторы по-прежнему удаляются с сохранением первого порядка.

Основание и связанная история: `UPD-002.2`, подготовительный `f07315138` и
result `57776b713` приняли один групповой `POST /code`, parallel package builds,
одно WebSocket notification и ровно один restart всех Window. Новый owner
review признал повторяемые query parameters неудобным внешним представлением
для агента: package names требуют URL encoding, а группа не выражена одной
структурированной value. Принятый механизм после parsing boundary не меняется.

Наблюдаемое расхождение: production route, test fixture, browser helper и
инструкция `$metafor-dev` кодируют группу повторяемыми `module` в URL, хотя
POST уже представляет одну структурированную команду сборки.

Причина: `.2` механически продолжила query-представление одиночного `GET /code`
на групповой POST вместо отдельного command body.

Разрешённое изменение одного механизма: заменить только разбор входа POST и
его callers на JSON `{modules}`. `GET /code?module=<package-name>`, build result,
parallel execution, RPC message, cache replacement и browser restart оставить
без изменения. Совместимость со старой query-формой не сохранять.

Regression или опровергающее доказательство: один и групповой JSON POST проходят
действующий failed/success browser-сценарий; legacy query POST отклоняется до
build; неизвестный, startup и пустой массив не публикуют update. Успешная группа
по-прежнему навигирует каждое Window ровно один раз и рождает новую active
Service Worker incarnation.

Среда и критерий приёмки: canonical `main` от baseline `a2bbcb897`; strict
TypeScript, focused boundary/browser regressions, live JSON POST в постоянном
`$metafor-dev` contour и `git diff --check` проходят. Query POST live-проверкой
отклонён; server остаётся запущен.

Фактические действия: общий production parser в `build.ts` принимает только
exact JSON `{modules}` с `application/json`, отвергает POST query, неверную
shape, пустой массив, неизвестные и startup packages до build. Production route
и browser fixture используют этот один parser. Browser request helper и русский
guide `$metafor-dev` переведены на JSON body без query compatibility.

Результат и вывод: accepted `test:load` прошёл `4 pass`, `0 fail`, `119 expect()`
и сохранил failed/success group, две Window navigation и Worker replacement.
Live canonical contour вернул legacy query `415`, пустому массиву `400`,
`@startup/main` — `404`; JSON `{"modules":["@internal/rpc"]}` вернул `200`,
один successful result и автоматически навигировал Window ровно один раз с
новым page realm. Server bundle собран, skill validation и `git diff --check`
прошли; Hamiltonian, iTerm и CDP Chrome оставлены работающими.

Подготовительный commit: `79733af92`.

Result checkpoint: `a002c3697`.

### UPD-002.4 — Разделить development- и production-сборку модулей

Статус и исполнитель: `IN_PROGRESS`; текущая задача без субагента по прямому
решению владельца работать совместно в одном чате.

Классификация: новый package-build contract того же локального development
update, без изменения `/code`, группового уведомления Service Worker и browser
restart.

Требование: development host запускается с `NODE_ENV=development`; каждый
package-owned browser artifact в этом режиме минифицируется, сохраняет
`console.debug` и получает inline source map. Production build минифицируется,
запускает Bun с `--production --drop console.debug`, не создаёт source map и не
содержит сам вызов, его литералы или вычисление аргументов. Временные
клиентские diagnostics используют `console.debug`; обязательная логика не
помещается в его аргументы.

Основание и связанная история: lazy package build и единый `dist/index.js`
приняты коммитом `69b7425f6`; `UPD-002.2` и `UPD-002.3` сохранили package-owned
`scripts.build` за parsing boundary группового update. Владелец отдельно
проверил на Bun `1.3.14`, что `--drop console.debug` удаляет вызов, строковые
литералы, вычисление аргументов и ставший мёртвым код, и выбрал этот прямой
механизм вместо feature flag.

Наблюдаемое расхождение: buildable packages сейчас имеют разные параметры:
только `@import/service` минифицируется, source maps clean-room artifacts не
выпускаются, `console.info` development diagnostics остаются в production, а
постоянный `$metafor-dev` contour не задаёт режим сборки.

Причина: package scripts были введены только как точные команды первоначальной
и повторной сборки до выбора двух build profiles и единого правила временных
логов.

Разрешённое изменение одного механизма: дать каждому buildable package точные
development/production build scripts и один выбор по `NODE_ENV`; корневые
Hamiltonian scripts и `$metafor-dev` должны явно запускать соответствующий
режим. Перевести одноразовые client diagnostics собираемых entrypoints на
`console.debug`, не меняя error reporting и update protocol.

Regression или опровергающее доказательство: проверка package contract и
фактическая сборка одного artifact в обоих режимах доказывают minification,
наличие debug marker и inline source map только в development и отсутствие
`console.debug`, его marker и source map в production. Все buildable packages
успешно собираются в обоих режимах; действующий `test:load` остаётся зелёным.

Среда и критерий приёмки: canonical `main` от baseline `a002c3697`; package
builds в обоих режимах, focused regression, `test:load`, skill validation и
`git diff --check` проходят. Постоянный `$metafor-dev` contour после одного
явного restart работает через `bun run dev`; последующий JSON update не требует
перезапуска server.

Фактические действия:

Результат и вывод:

Подготовительный commit: этот commit.

Result checkpoint:

## Границы

* Не обновлять Bun process, production-домены или другие устройства.
* Не включать HTML, `@startup/main` или `@startup/service` в сменяемый release.
* Не переносить code bytes через WSS; source address может прийти через internal
  RPC, а manifest/artifacts загружаются через `fetch`.
* Не удалять server-side использование `HAMILTONIAN_VERSION`, пока для него не
  создан отдельный договор замены.
* Не передавать код через Web Push; он остаётся только способом пробуждения и
  начала проверки.
* Не использовать `registration.update()` как механизм смены загружаемого
  functional release; executable startup Service Worker этой задачей не
  заменяется.
* Не смешивать файлы двух выпусков и не удалять прежний готовый cache до
  подтверждения нового.
* Не вводить remote registry, подпись выпусков, trust root, anti-rollback,
  межмашинную доставку или общий rollback.
* Не менять logical identity Service Worker, profile ownership, topology,
  peer, Oracle/Force transport или обязанности Dark, Boundary, Matrix, Energy,
  Bulk и Force.
* `skipWaiting()` и `clients.claim()` принадлежат неизменяемому startup и не
  меняются этой задачей.

## Критерии готовности

* Один manifest однозначно описывает весь сменяемый post-startup выпуск,
  cache owner и точный hash каждого artifact; identity выпуска детерминирована
  его содержимым.
* Service Worker загружает и проверяет полный сменяемый выпуск до переключения и
  затем обслуживает importers, modules и resources только из одного active
  release, не заменяя startup.
* Page больше не принимает host `source-update` как отдельное решение об
  обновлении и не загружает синтетический versioned module по ручной browser
  version.
* Browser-путь больше не зависит от `HAMILTONIAN_VERSION`; отдельный маленький
  module и его `/versions/...` cache path удалены из client update.
* Изменение обычного клиентского файла без изменения Service Worker также
  приводит к подготовке нового полного выпуска и одному reload всех текущих
  страниц.
* Изменение Service Worker functionality выполняется заменой загружаемого
  service-side module, а не browser-managed заменой startup script.
* Failed build, network interruption, неверный hash и закрытие страницы во
  время подготовки сохраняют прежний active release и допускают повтор.
* Два одновременно запущенных Chrome-профиля независимо переходят на один exact
  release, сохраняют разные profile/Worker identity и не смешивают caches,
  lifecycle или transport.
* Cold restart host с теми же browser bytes не создаёт reload, а поздно
  подключившийся профиль получает уже текущий выпуск.
* Испытательные server embodiments продолжают работать по прежнему договору до
  отдельной задачи обновления серверных процессов.

## Проверка результата

* Frozen manifest и release-identity tests по точному набору файлов.
* Tests атомарной подготовки, hash mismatch, partial fetch, cache retention и
  retry.
* Service Worker fetch tests для importers, modules и ресурсов одного active
  release, а также regression неизменности startup/navigation bytes.
* Page tests для одного `release-ready → reload`, duplicate message и cold host
  с неизменным release.
* Integration tests совместной смены service-side module и Window assets без
  mixed release и без изменения startup bytes.
* Строгие host и WebWorker TypeScript checks, реальные browser/Worker builds и
  полный `bun test hamiltonian`.
* Живой сценарий двух Chrome-профилей: исходный выпуск, новый выпуск с обычным
  client asset, новый выпуск вместе с service-side module, failed preparation
  и восстановленный exact final release без cross-profile edges.
* `git diff --check`, полный import/URL audit, отсутствие старого browser-path
  `HAMILTONIAN_VERSION`, `/versions/...`, самостоятельного page `source-update`
  и WSS code delivery.
