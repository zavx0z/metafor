# DOC-001 — Вернуть единые правила документации MetaFor

## Коротко

Документация снова становится одной связной системой без дублирования и
технического шума: общие документы дают короткий закон и карту владельцев,
package README объясняют собственный смысл и жизненный цикл, а техническая
механика живёт во внутрикодовом TSDoc рядом с реальным public code.

## Зачем

После закрытых документационных срезов `MF-411` правила остались распределены
по истории, текущим docs и карточке. Последняя Cosmos-правка вынесла часть из
них в отдельный `cosmos-development` skill и снова добавила в обычные README
cache names, build targets, source paths, DTO/type names и transaction details.
Это повторяет уже исправленную причину прежнего раздутого Hamiltonian README.

## Связанные задачи и история

* Thread `01a018eb-c0c4-7ad3-8572-4a7d5a99ac66` содержит owner corrections и
  документационные срезы `MF-411`.
* Commit `5b02f0f9c` разделил 1097-строчный Hamiltonian README между root,
  package owners, prototype и development documentation.
* Commit `7f664d156` переписал package README понятным языком и убрал статусы,
  caches, transactions, RPC/HTTP и implementation reports.
* Commit `1339a381c` закрепил единые термины, package aliases и контекстные
  ссылки во всех owner docs.
* Commit `7aaaeaddd` удалил TypeDoc generation/config/scripts и оставил TSDoc
  только внутри source/IDE.
* `MF-411.4` требует полезный TSDoc только у существующих public entrypoints и
  contracts: owner, lifecycle, side effects, errors и links to tests. Очевидные
  signatures и internal helpers не пересказываются.

## Решения владельца

* Общие правила ведения документации принадлежат `$metafor-dev`; отдельный
  `$cosmos-development` сохраняется для предметной специфики Cosmos.
* Документация образует одну связную систему, распределённую по реальным
  владельцам, а не набор повторяющих друг друга обзоров.
* Корневой README содержит короткий общий закон, пользовательскую картину и
  карту ответственности; package README описывает только собственный смысл,
  lifecycle, случаи исполнения и наблюдаемый результат.
* Общий факт находится у одного владельца и не копируется в package docs.
* Обычная документация использует понятный язык, положительные законы и один
  термин для одной сущности. Технические формы, status/evidence и история задач
  в неё не входят.
* Первое scoped package name объявляет короткий alias; дальше используется он.
  Namespace описывается как множество packages.
* Каждая ссылка встроена в проверяемое предложение, называет предмет закона и
  его владельца; предложение сохраняет смысл без Markdown.
* Внутрикодовый TSDoc описывает только существующий public code: owner,
  contract, lifecycle, invariants, side effects, errors и связанные tests. Он
  не генерируется в HTML/site/output и не пересказывает очевидные signatures.
* Когда browser и Bun имеют одинаковую ответственность, общий механизм
  описывается через public interface, а различия — через platform adapters.

## Подзадачи

### DOC-001.1 — Закрепить правила в metafor-dev

Статус и исполнитель: `COMPLETED`; руководитель текущей задачи.

Требование: добавить в `$metafor-dev` обязательный documentation workflow и
подробную reference с принятой системой; удалить отдельный
`cosmos-development` skill и ошибочный routing из root `AGENTS.md`.

Критерий приёмки: skill routing однозначен, reference связывает owner docs,
package docs, TSDoc и tests без дублирования project/runtime instructions;
Skill Creator validation и `git diff --check` проходят.

Подготовительный commit: `3cf267143`.

Фактические действия: `$metafor-dev` теперь применяется к реализации,
документации и проверке MetaFor. Его entrypoint маршрутизирует документационную
работу в новую русскую reference `references/documentation.md`.

Reference восстанавливает принятую линию `MF-411`: иерархию owner/root/package/
development/task/TSDoc, положительные законы, единые термины и aliases,
контекстные links, запрет technical/status noise, внутрикодовый TSDoc без
generation и правило common interface/platform adapters.

Ошибочный `cosmos-development` skill удалён, root `AGENTS.md` направляет всю
документационную работу в `$metafor-dev`. Runtime lifecycle и browser workflow
остались без изменения.

Проверки: Skill Creator `quick_validate.py` вернул `Skill is valid!`; routing и
reference paths существуют, отдельный Cosmos skill отсутствует,
`git diff --check` прошёл.

Result checkpoint: текущий `DOC-001.1` commit.

### DOC-001.2 — Привести текущую документацию Cosmos к правилам

Статус и исполнитель: `COMPLETED`; руководитель текущей задачи.

Требование: проверить root/startup/release/internal README по source, package
exports, tests и принятым owner laws; убрать technical/status duplication,
сохранить знания у TSDoc/development owners и восстановить контекстные links.

Критерий приёмки: root остаётся кратким общим законом и картой; package docs
описывают только собственную ответственность; техническая механика не теряется
и находится у существующих public declarations/TSDoc или development owner.
Локальные links/anchors, terminology audit и `git diff --check` проходят.

Подготовительные commits: `a00ea071e`, `44b6d8920` после owner correction.

Фактические действия: корневой Cosmos README сокращён до общего lifecycle,
environment law, карты владельцев и границы Quantum. Startup, release, internal
и visual README теперь описывают только собственный смысл, событие, lifecycle,
наблюдаемый результат и соседних владельцев; source paths, cache/transaction,
HTTP/RPC, build inventory, prototype history и live status удалены.

`docs/README.md` связывает Cosmos owners одной контекстной картой. Полезный
внутрикодовый TSDoc добавлен или исправлен у public environments, общего
`PackageExecutor`, startup/release/visual entrypoints; устаревшее обещание
будущего server adapter заменено описанием существующего Bun process adapter.
Visual TSDoc фиксирует реальные side effects, ошибку отсутствующего canvas и
существующую marker-only server part без обещания server-визуализации.

Проверки: все локальные links/anchors в 16 изменённых README/TSDoc owners
разрешаются; package typecheck startup/release/visual и Cosmos tests typecheck
прошли; lifecycle/boundary tests — `16 pass`, build-profile tests без отдельно
вынесенного convergence — `12 pass`, `1 filtered`, `0 fail`; terminology/noise
audit и `git diff --check` прошли. Source-map convergence передан в заранее
зарегистрированный `DOC-001.4`, потому что он требует отдельной package
publication и изменения manifests.

Result checkpoint: текущий `DOC-001.2` commit.

### DOC-001.3 — Разделить общие и Cosmos-specific skill rules

Статус и исполнитель: `COMPLETED`; руководитель текущей задачи.

Требование: сохранить общую documentation reference внутри `$metafor-dev`,
вернуть `$cosmos-development` и оставить в нём только Cosmos-specific правила:
environment terminology, package/release/artifact ownership, общий interface и
platform adapters. Cosmos skill обязан ссылаться на общую reference, а не
копировать её.

Основание и связанная история: первый результат `DOC-001.1` ошибочно удалил
Cosmos skill. Владелец уточнил, что отдельный skill нужен из-за предметной
специфики; неверным было только размещение в нём общих правил документации.

Критерий приёмки: root routing различает общую documentation work и
Cosmos-specific work; оба skills валидны, ссылки между ними разрешаются,
дублирования общих правил нет, `git diff --check` проходит.

Подготовительный commit: текущий project-коммит owner correction.

Фактические действия: `$cosmos-development` восстановлен в
`cosmos/.agents/skills` и ограничен предметной спецификой Cosmos: точными
единицами package/release/platform part/environment, владельцами
startup/release/internal/metafor, browser/Bun env, artifacts, composition,
placement и platform adapters.

Cosmos skill первой ссылкой подключает общую documentation reference
`$metafor-dev` и не копирует её правила. Root `AGENTS.md` требует оба уровня:
общий `$metafor-dev` для документации и `$cosmos-development` для Cosmos.

Проверки: оба skills прошли Skill Creator validation; cross-skill link и routing
paths существуют, активных TypeDoc-generation instructions нет,
`git diff --check` прошёл.

Result checkpoint: текущий `DOC-001.3` commit.

### DOC-001.4 — Свести внутрикодовую документацию с immutable artifacts

Статус и исполнитель: `IN_PROGRESS`; руководитель текущей задачи.

Требование: после изменения TSDoc опубликовать новые package-wide patch versions
`@cosmos/release`, `@internal/visual` и `@cosmos/startup`, потому что development
source maps содержат исходный текст и обязаны совпадать с текущими public
entrypoints. Startup должен зависеть от новой exact-compatible release version.

Подтверждённая причина: focused convergence test пересобирает каждую environment
во временную директорию и сравнивает SHA-256/size с immutable versioned artifact.
После полезной TSDoc-правки source map закономерно отличается от ранее
опубликованной версии, хотя runtime-поведение не менялось.

Критерий приёмки: release и visual опубликованы штатной атомарной publication,
startup опубликован полным набором `main/service/server` и maps; manifests
указывают новые версии, source-to-artifact convergence проходит для каждой
объявленной environment, managed runtime использует exact новый release.

Граница: не менять runtime, public API, package composition или поведение;
изменяются только package versions, совместимая startup dependency и
производные immutable artifacts/maps текущего TSDoc.

Подготовительные commits: `ac90b09a0`, текущий project-коммит начала после
source checkpoint `e02bb03de`.

## Границы

* Не менять runtime, public API или tests поведения; package manifests меняются
  только в `DOC-001.4` ради versioned materialization текущего TSDoc.
* Не восстанавливать TypeDoc generation/toolchain.
* Не переписывать документы из вкуса: каждое удаление или перенос имеет
  подтверждённого владельца.
* Не включать prototype evidence, task history и live status в постоянные docs.

## Критерии готовности

* `$metafor-dev` владеет общими правилами документации, а
  `$cosmos-development` — предметной спецификой Cosmos.
* Текущая documentation hierarchy соответствует принятой линии `MF-411`.
* Owner docs кратки, package-specific и связаны контекстными ссылками.
* Техническая документация остаётся внутрикодовой TSDoc без generation.
* Независимый review не находит потерянных законов или дублирующих owners.

## Проверка результата

* Skill Creator validation.
* Audit current source/package exports against changed docs.
* Проверка локальных links/anchors и terminology/noise gates.
* `git diff --check`.
