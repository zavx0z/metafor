# UI-010 — Сделать полный набор универсальных полей по Blender

## Коротко

Обычные панели и Node Editor получают один полный набор точных input controls:
числа, меню, цвета, ramp, curves, ссылки и изменяемые коллекции. Одни и те же
компоненты работают внутри Node Parameter и вне нодовой системы.

## Зачем

Текущий `Field` покрывает только первый набор value kinds и собран крупным
условным renderer. Blender catalog показал отдельные interactions и состояния,
которые сейчас либо упрощены до кнопки/текста, либо отсутствуют. Если добавить
их непосредственно в Node renderer, появятся копии и расходящиеся contracts.

## Связь с дорожной картой

Задача продолжает универсальную UI/Node component line параллельно UI-011:
production controls и playground имеют разных исполнителей и непересекающиеся
границы. Направление зависимостей остаётся
`Engine → Elements → Components → Node UI`.

## Связанные задачи и история

* NODES-016 создала первый universal `Field` и Blender component boundary:
  preparation `4835ae1f5`, Field result `316a16e37`, playground result
  `b867bd546`.
* NODES-017 доказала reuse Fields внутри retained Node, выявила расхождения
  compact controls и исправила первый visual rhythm коммитом `f8f27dbb3`.
* UI-001 восстанавливает Components playground и переносит его на shared
  package-owned stories. Следующее прямое решение владельца передало execution
  и closing UI-001 руководителю UI-011, а UI-010 разрешило развивать production
  Components параллельно без изменений playground.
* Самостоятельный
  [Blender Node Catalog](/Users/zavx0z/repozitarium/blender-node-catalog/README.md)
  на commit `62bf479` собрал 36 socket presentations, 63 node-owned property
  presentations и 49 source UI mechanisms. Это reference inventory, а не
  автоматически выбранный API MetaFor.
* Исследованы обсуждения
  [Node component library](codex://threads/01a01742-3e9d-7393-aacf-affe36787ba0)
  и [Blender parameter catalog](codex://threads/01a01c7c-9bff-72f2-859c-d6626c7e40ab).
  Прежняя минимальная выборка не является полным набором параметров.

## Подтверждённые факты

* Уже можно переиспользовать `TextField`, `SliderControl`, `Switcher`,
  `Button`/`IconButton`, `Typography`, `List`, `Table`, FlexBox, retained
  `UiSurface`, icons, clipping и input lifecycle.
* Отсутствуют готовые public select/popover, color picker/ramp, curve editor,
  reference/file/search picker и collection mutation/reorder controls.
* Текущий enum циклически меняется кнопкой, color редактируется как hex,
  reference вызывает внешний action, а topology-changing state не выражен
  универсальным control contract.

## Решения владельца

1. `Field` остаётся единым semantic facade, но конкретные controls становятся
   самостоятельными public Components.
2. `@nodes/ui` только компонует controls в Parameter и владеет Node/Socket
   topology; Node-копии universal controls запрещены.
3. Поведение, состояния и compact-пропорции берутся из Blender. Материалы,
   проектный шрифт и общая тема остаются MetaFor.
4. Принятые расхождения сохраняются: `Switcher` вместо checkbox, округлённая
   Node header и ортогональные Links.
5. Пропорции, padding и положения измеряются по exact Blender 4.5.5 reference,
   а не подбираются на глаз.
6. Blender source, assets и полный catalog не копируются в MetaFor.

## Целевые группы controls

* numeric input и slider с subtype, units, hard/soft range, step и precision;
* select/menu, expanded choices и error/undefined states;
* color swatch/picker и ordered color ramp;
* vector/rotation component editor и curve mapping;
* text, file path и contextual search;
* opaque reference picker с browse/clear и owner-supplied actions;
* collection editor с selection, add/remove/reorder;
* disabled, read-only, linked/hidden и compact/regular presentations.

Конкретная группа получает собственную последовательную подзадачу перед
реализацией. Наличие группы в target catalog не означает, что все её варианты
реализуются одним patch.

## Подзадачи

### UI-010.1 — Вынести числовой ввод в public NumberInput

Статус и исполнитель: `COMPLETE` на package и automated live contract; owner
visual acceptance остаётся parent gate.

Классификация: первый production-срез нового parent. Он изменяет один механизм:
скалярный numeric input, который сейчас скрыт внутри монолитного `Field`.

Требование: `NumberInput` является самостоятельным public Component с
controlled number value, integer/float contract, min/max/step/unit,
disabled/read-only и regular/compact presentation. `Field` использует этот же
Component; `SliderControl` остаётся отдельным уже существующим presentation.

Основание и история: NODES-016 ввела scalar normalization и два локальных пути
отрисовки в `Field.ts`; UI-010 owner law требует public controls без Node-копий.
Float/Integer являются самыми частыми value presentations Blender catalog и
дают минимальный проверяемый первый срез.

Наблюдаемое расхождение: regular и compact scalar number напрямую собирают
`TextField`, сами парсят unit и вызывают `normalizeNumberFieldValue`. Отдельного
leaf import и самостоятельного Component contract нет.

Причина: первый набор Fields реализовывался как минимальный единый renderer до
решения разделить concrete controls.

Разрешённое изменение одного механизма: добавить public `NumberInput`, его pure
normalization/parsing tests и exact subpath; перевести только scalar number
ветки `Field` на него. Vector, matrix, slider, playground и Node renderer не
меняются в этом срезе.

Regression: standalone NumberInput и Field возвращают одинаковое normalized
value; integer, min/max, step, unit, invalid input, disabled/read-only и compact
style проверяются focused tests. Exact leaf build не содержит Node/playground.

Среда и приёмка: package tests/typecheck и delivery exact-import proof. Live
Components/Node evidence выполняется совместно с UI-011 после её route patch;
до этого fresh build не выдаётся за loaded playground acceptance.

Подготовительный commit: записывается до implementation patch.

Фактические действия и результат: commit `99f80c0e2` добавил public
`NumberInput` и exact subpath `@ui/components/number-input`. Regular/compact
scalar Field делегируют ему render/parse/normalization; существующий
`normalizeNumberFieldValue` стал однострочным делегатом единственному owner.
Slider, vector, matrix, Node и playground не менялись.

Regression и проверки: Components `17/17`, delivery `8/8`, Components и
delivery typechecks, exact leaf build без Node/playground symbols и diff-check
прошли. Первый delivery run внутри restricted sandbox дал ложный
`Unexpected reading file`; тот же read-only Bun.build suite вне ограничения
прошёл `8/8`.

Live boundary: `socket/float/input` на singleton target UI-011 был DOM-ready,
console `0` и non-black, но detail story показывает только Socket и не вызывает
Field/NumberInput. Этот кадр правильно не сохранён как ложное UI-010 evidence.
Standalone `Field → NumberInput` принимает UI-011.3 Components story; открытый
live gate не блокирует следующие production slices по прямому решению владельца
вести Components и playground параллельно.

### UI-010.2 — Вынести цветовой ввод в public ColorInput

Статус и исполнитель: `COMPLETE` на package и automated live contract; owner
visual acceptance остаётся parent gate.

Классификация: следующий production control того же parent. Он изменяет один
механизм: scalar RGBA input, сейчас скрытый внутри `Field`.

Требование: `ColorInput` становится самостоятельным public Component с
controlled normalized RGBA, swatch + exact hex input, alpha, disabled/read-only
и regular/compact presentation. `Field` использует тот же Component.

Основание и история: current regular/compact color branches отдельно собирают
одинаковые swatch/TextField и владеют conversion helpers. UI-010 требует один
public control owner до добавления popup picker и ColorRamp.

Наблюдаемое расхождение и причина: `Field.ts` напрямую рисует color swatch,
парсит/форматирует hex и повторяет эту composition для двух density. Это
наследие минимального монолитного NODES-016 renderer; standalone leaf import
отсутствует.

Разрешённое изменение одного механизма: добавить public `ColorInput`, перенести
к нему scalar RGBA normalization/hex conversion и перевести только regular и
compact color Field branches. Popup ColorPicker, ramp stops, overlay, остальные
Field kinds, Node и playground не меняются.

Regression: standalone ColorInput и обе Field density возвращают одинаковое
RGBA; clamp/alpha/hex invalid input, disabled/read-only, swatch и compact
geometry проверяются focused tests. Exact leaf build исключает Node/playground.

Среда и приёмка: package tests/typecheck/delivery exact build. Live evidence
принадлежит будущей UI-011.3 Components story и остаётся parent gate.

Подготовительный commit: записывается до implementation patch.

Фактические действия и результат: commit `c935de436` добавил public
`ColorInput` и exact subpath `@ui/components/color-input`. Normalized immutable
RGBA, RGB/RGBA hex, swatch, disabled/read-only и regular/compact composition
имеют одного owner; обе color Field branches делегируют ему. Прежние
`FieldColor`, `normalizeFieldColor`, `fieldColorToHex` и `parseFieldColor`
сохранены прямыми aliases того же implementation.

Regression и проверки: production Components `20/20` (`408` assertions),
Components typecheck, delivery `9/9` (`365` assertions), delivery typecheck,
exact ColorInput leaf без Field/Node/playground и diff-check прошли. Regression
red включал ожидаемые missing leaf/manifest и restricted-read шум; финальный
Bun.build suite прошёл как в sandbox, так и повторно вне ограничения.

Live boundary: ColorInput не выдаётся за loaded UI до Components stories
UI-011.3. Package checkpoint не блокирует следующие production controls по
прямому решению владельца вести две линии параллельно.

### UI-010.3 — Вынести vector и rotation ввод в public VectorInput

Статус и исполнитель: `COMPLETE` на package и automated live contract; owner
visual acceptance остаётся parent gate.

Классификация: следующий production control того же parent. Он изменяет один
механизм: multi-axis numeric input, сейчас продублированный в regular/compact
Field branches.

Требование: `VectorInput` становится самостоятельным public Component для
2–4 controlled numeric axes с labels, integer/float-compatible
min/max/step/unit, disabled/read-only и regular/compact presentation. Rotation
остаётся отдельным semantic Field kind, но использует тот же control и может
задать собственные axes/units.

Основание и история: Blender catalog различает vector subtypes и Rotation
semantic Socket, но UI Components уже приняли один общий axis editor.
UI-010 требует сделать этот editor public без Node-копии.

Наблюдаемое расхождение и причина: `Field.ts` дважды строит nested Flex rows,
axis Typography, scalar TextField parsing и normalization. Это осталось внутри
первого монолитного renderer; standalone leaf import отсутствует.

Разрешённое изменение одного механизма: добавить public `VectorInput`, перенести
единственному owner axis normalization/composition и перевести только vector и
rotation Field branches. Matrix, slider, popup, Node и playground не меняются.

Regression: 2/3/4 dimensions, axis labels, unit, min/max/step, invalid input,
disabled/read-only и обе density; standalone VectorInput и vector/rotation Field
возвращают одинаковое immutable value. Exact leaf исключает Node/playground.

Среда и приёмка: package tests/typecheck/delivery exact build. Live evidence
принадлежит UI-011.3 Components story и остаётся parent gate.

Подготовительный commit: записывается до implementation patch.

Фактические действия и результат: commit `b926baf00` добавил public
`VectorInput` и exact subpath `@ui/components/vector-input`. Один component
владеет 2–4 numeric axes, default/custom labels, min/max/step/unit,
disabled/read-only и horizontal/compact vertical composition. Vector и Rotation
Field сохраняют разные semantic kinds, но делегируют тому же control;
`normalizeVectorFieldValue` является direct alias единственного owner.

Regression и проверки: Components `36/36` (`524` assertions), Components
typecheck, delivery `10/10` (`380` assertions), delivery typecheck, exact leaf
без Field/Node/playground и diff-check прошли. Руководитель повторил focused
Vector/Field/retained `15/15` (`376` assertions), оба typecheck и delivery
`10/10` вне restricted sandbox.

## Общий live gate UI-010.1–UI-010.3

UI-011.3 выполнила один `$ui-dev` restart Components: PID `44440`, exact target
`D0775AE44CFF0E299A0C28EECB3872D2`, native `1920×1088 @2`, backing
`3840×2176`, console `0`; target возвращён `/field/number/input`.

* Number `/field/number/input`: exact kind `number`, presentation `input`,
  value `0.625`; capture
  [`components-field-number.png`](../artifacts/UI-010/components-field-number.png),
  `647101` bytes, SHA-256
  `212ddf69bdbe67bb7215740e3560a75cb178ed741777bf8af8105207b13d73fe`.
* Color `/field/color/input`: exact RGBA; первый starting capture был чёрным и
  отклонён, повторный ready capture
  [`components-field-color.png`](../artifacts/UI-010/components-field-color.png),
  `635819` bytes, SHA-256
  `ed1610346a91630c1df48a0e822db09f7f1eb977b5c783b2b4ce0b3de842361d`.
* Vector `/field/vector/default`: exact `[1,2,3]`, dimensions `3`; capture
  [`components-field-vector.png`](../artifacts/UI-010/components-field-vector.png),
  `634410` bytes, SHA-256
  `e7ddf87392c982d683c952f2b685ca5263ae524155f66b0054db0a4603d2b838`.

Все три non-black desktop captures показывают actual
`Field → NumberInput/ColorInput/VectorInput`, TypeScript/Copy/controls справа и
`nativeMetricsRestored=true`. Это automated live evidence, не owner acceptance.

### UI-010.4 — Вынести matrix ввод в public MatrixInput

Статус и исполнитель: `COMPLETE` на package и automated live contract; owner
visual acceptance остаётся parent gate.

Классификация: следующий production control того же parent. Он изменяет один
механизм: square numeric matrix, сейчас целиком скрытую внутри `Field`.

Требование: `MatrixInput` становится самостоятельным public Component для
controlled square `2×2`–`4×4` numeric values, identity fallback, immutable cell
update, disabled/read-only и regular/compact presentation. Каждая cell использует
public NumberInput; `Field` остаётся semantic wrapper.

Основание и история: Blender catalog выделяет Matrix как connection/value
semantic, а current Components уже имеют минимальный matrix editor. UI-010
требует вынести его без Node-копии и без смешения с VectorInput.

Наблюдаемое расхождение и причина: `Field.ts` самостоятельно нормализует square
matrix, строит nested Flex grid, форматирует и парсит каждую TextField cell в
regular/compact branches. Это осталось от монолитного первого renderer.

Разрешённое изменение одного механизма: добавить public `MatrixInput`, перенести
единственному owner normalization/measurement/grid composition и перевести
только Matrix Field branches. Popup, transform semantics, Node и playground не
меняются.

Regression: 2×2/3×3/4×4, identity fallback, invalid cell, immutable update,
disabled/read-only, regular/compact geometry и standalone/Field equality. Exact
leaf использует NumberInput и исключает Node/playground.

Среда и приёмка: package tests/typecheck/delivery exact build. Live evidence
принадлежит Matrix story UI-011.3 и остаётся automated parent gate.

Подготовительный commit: записывается до implementation patch.

Фактические действия и результат: commit `4978b3d97` добавил public
`MatrixInput` и exact subpath `@ui/components/matrix-input`. Один component
владеет square `2×2`–`4×4` normalization, identity fallback, immutable cell
update и regular/compact grid; каждая cell использует public `NumberInput`, а
Matrix Field делегирует тому же control. Correction `a54f54495` восстановила
прежнюю production presentation ячеек: `1.00/0.00`, regular `9px`, compact
`11px`, не меняя parser и остальные NumberInput defaults.

Regression и проверки: Components `42/42` (`564` assertions), delivery
`11/11` (`396` assertions), оба typecheck, exact MatrixInput leaf с
NumberInput и без Field/Node/playground, а также diff-check прошли. Независимый
focused rerun Matrix/Number/Field/retained `20/20` и связанные package
typechecks зелёные.

Live boundary: UI-011 подготовила non-black exact Matrix story capture на
correction `a54f54495`: actual `Field → MatrixInput`, values `1.00/0.00`,
console `0`, native metrics восстановлены. Первый starting/black capture
правильно отклонён. Artifact и общий live verdict сериализует единственный
playground owner UI-011; automated capture не является owner acceptance.

### UI-010.5 — Вынести ссылочный ввод в public ReferenceInput

Статус и исполнитель: `COMPLETE` на production package/live contract; owner
acceptance остаётся parent gate.

Классификация: следующий production control того же parent. Он изменяет один
механизм: opaque reference browse/clear input, сейчас скрытый и неполный внутри
`Field`.

Требование: `ReferenceInput` становится самостоятельным public Component для
controlled `FieldReference | null` с выбранным/пустым состоянием, placeholder,
owner-supplied activate и clear actions, disabled/read-only и regular/compact
presentation. `Field` остаётся semantic facade и использует тот же Component.

Основание и история: UI-010 target catalog отдельно фиксирует opaque reference
picker с browse/clear, но выбор конкретной сущности принадлежит внешнему owner.
NODES-016 уже добавила `ReferenceFieldDefinition.onActivate` и `onClear`, не
создав самостоятельный Component или popup lifecycle.

Наблюдаемое расхождение: regular и compact reference branches отдельно
собирают одну Button. Объявленный public `onClear` не вызывается ни одной из
них; standalone exact leaf отсутствует.

Причина: первый монолитный Field выражал reference как минимальную внешнюю
action-кнопку до решения вынести concrete controls и не реализовал вторую
clear interaction region.

Разрешённое изменение одного механизма: добавить public `ReferenceInput`,
собрать его из существующих Button/IconButton primitives и перевести только
обе reference Field branches. Selector, popup/overlay, file/search browser,
асинхронная загрузка, Node topology, playground и общий Elements lifecycle не
меняются; конкретный picker остаётся owner-supplied action.

Regression: null/selected value, label/kind/placeholder, activate и отдельный
clear hit path, отсутствие clear при пустом value, disabled/read-only и обе
density. Clear не вызывает activate; standalone ReferenceInput и reference
Field имеют один callback contract. Exact leaf исключает Field/Node/playground.

Среда и приёмка: package tests/typecheck/delivery exact build. Compact padding,
label и clear affordance сверяются с exact Blender 4.5.5 reference; MetaFor
materials/font сохраняются. Live story принадлежит UI-011 и остаётся automated
parent gate.

Подготовительный commit: этот project checkpoint, до implementation patch.

Фактические действия и результат: commit `663a81c0b` добавил public
`ReferenceInput` и exact subpath `@ui/components/reference-input`. Один
Component владеет opaque value/null, placeholder, отдельными activate/clear hit
paths, disabled/read-only и regular/compact composition; обе reference Field
ветки делегируют ему. Selector/popup/async и Node topology не добавлялись.

Regression и проверки: Components `41/41` (`530` assertions), focused
Reference/Field/retained `15/15` (`379`), delivery `12/12` (`411`), Components
и delivery typechecks, exact leaf без Field/Node/playground и diff-check прошли.
Compact `22px` row и `3px` gap являются сохранённым MetaFor production rhythm,
а не заявленной Blender pixel parity; Blender reference подтверждает структуру
browse + отдельный unlink/clear.

Live result: UI-011.7 source `4c57b33c9` добавил exact lazy
`reference-input/basic/default`; package/playground/delivery gates зелёные.
Loaded PID `1163`, target `D0775AE44CFF0E299A0C28EECB3872D2`: route ready,
source/args exact, console `0`, selected resource и owner activate/clear events
видны, native `1920×1088 @2` восстановлен. Capture SHA-256
`27d25b64062dab87815339a309c33d57e13653742668aea0031005a1bdc0781e`
сохранён в Git history UI-011 result/closing `39f6f4ea2`/`2ad6fffbd`;
automated evidence не является owner acceptance.

### UI-010.6 — Вынести enum в public EnumInput

Статус и исполнитель: `COMPLETE` на production package/live contract; owner
acceptance остаётся parent gate.

Классификация: следующий production control того же parent. Он изменяет один
механизм: stable enum choice, сейчас продублированный циклической Button внутри
regular/compact Field.

Требование: `EnumInput` становится самостоятельным public Component для
controlled string value и immutable options. Первый slice публикует `cycle` и
inline `expanded` presentations, selected description, disabled/read-only и
regular/compact density, а также явные `No Items`, `Menu Undefined` и
`Menu Error` states. `Field` использует тот же Component в сохранённой cycle
presentation.

Основание и история: Blender catalog различает dropdown и expanded choices, а
`NodeSocketMenu` отдельно показывает empty/undefined/error states. Current
Components имеют Button/Flex primitives, достаточные для cycle/expanded, но не
имеют retained overlay owner для честного popup menu. История проекта не
содержит отдельного EnumInput/SelectInput result; первый enum path остался в
монолитном NODES-016 Field.

Наблюдаемое расхождение: обе density отдельно ищут selected option, собирают
Button и вызывают Field-owned `nextEnumFieldValue`. Standalone exact leaf,
expanded choices и наблюдаемые exceptional states отсутствуют; options `[]`
просто отключают кнопку.

Причина: первый Field сохранял минимальный циклический interaction до решения
разделить concrete controls и до появления общего overlay lifecycle.

Разрешённое изменение одного механизма: добавить public `EnumInput`, перенести
ему option lookup/cycle helper и перевести только enum Field branches. Inline
expanded choices строятся существующими Button/Flex primitives. Dropdown,
popover/portal, contextual search, dynamic item mutation, Socket topology,
playground и Elements lifecycle не меняются; будущий `SelectInput` требует
отдельного owner slice.

Regression: forward/backward cycle, selected/invalid value, empty/undefined/
error state labels, expanded single-choice callbacks, disabled/read-only и обе
density. Exceptional states не публикуют скрытый value change. Standalone и
Field cycle возвращают один результат; exact leaf исключает Field/Node/
playground.

Среда и приёмка: package tests/typecheck/delivery exact build. Compact row,
button groups и state labels сверяются с exact Blender 4.5.5 reference при
сопоставимом масштабе; конкретные MetaFor materials/font сохраняются. Live
story принадлежит UI-011 и остаётся automated parent gate.

Подготовительный commit: этот project checkpoint, до implementation patch.

Фактические действия и результат: commit `754a6d616` добавил public
`EnumInput`, exact `@ui/components/enum-input`, cycle + inline expanded,
selected description, preserved invalid legacy cycle, explicit No Items/Menu
Undefined/Menu Error и disabled/read-only. Field option type и helper являются
прямыми aliases единственного owner; обе enum Field density делегируют ему.
Delivery correction `1f6e8b394` добавила exact leaf в complete-import и root API
fixtures.

Regression и проверки: Components `48/48` (`577` assertions), delivery `13/13`
(`426`), оба typecheck, exact browser build и diff-check прошли. Независимый
focused Enum/Field/retained rerun `16/16` (`386`) и delivery `13/13` зелёные.
Regular `28px`/compact `22px` являются MetaFor production geometry, не
заявленной Blender pixel parity.

Live result: UI-011.8 source `0ea692dda` добавил четыре sections и `9` exact
routes. Loaded PID `11092`, тот же Components target: cycle и expanded ready,
source/args exact, console `0`, native restored. Capture SHA-256:
`f121f4c90d2aafc82ab03c4931fc6d0654424402ff9c9face3854c76d49db7ca`
и `af1587f2ff46dd21b9a9862ce11c91a09917762af5d8aaa9236f383b986b8035`.
UI-011 final matrix доказала completeness `6/6`; evidence закрыта commit-ом
`2ad6fffbd`, но не заменяет owner acceptance.

### UI-010.7 — Добавить public CollectionInput для rows/selection/add/remove

Статус и исполнитель: `COMPLETE` на production package contract; dependent
UI-010.8 story/live gate выполняется отдельно, owner acceptance остаётся parent.

Классификация: новый control group того же parent. Первый collection slice
меняет один механизм: controlled rows, active selection и соседние add/remove
actions. Reorder регистрируется отдельно после принятого interaction primitive.

Требование: `CollectionInput` становится самостоятельным public Component для
immutable items `{id,label,description?,disabled?}`, controlled
`selectedId | null`, bounded visible rows, empty state, owner-supplied select,
add/remove callbacks, disabled/read-only и regular/compact density. Новый
semantic `Field(kind:"collection")` использует тот же Component; Component не
владеет данными и не меняет Node topology.

Основание и история: permanent Components law требует public collection editor
и immutable owner callbacks. Blender `uiTemplateList` владеет rows/selection,
а mutation выполняют соседние operators. Existing public `List`, Button/
IconButton, Flex и scroll lifecycle уже являются достаточными primitives для
первого среза; отдельного CollectionInput в истории проекта нет.

Наблюдаемое расхождение: `FieldDefinition` не имеет collection kind, а consumer
может вручную собрать List и operators только вне semantic facade. Нет единого
value/callback contract, measurement, exact leaf или Node-reusable control.

Разрешённое изменение одного механизма: добавить public `CollectionInput`,
pure selection/measurement helpers, exact export/delivery и новый collection
Field delegate. Использовать существующие List и adjacent plus/minus actions.
Drag/drop, reorder, item editing, popup/menu, async data, virtualization changes,
Node topology и playground не входят.

Regression: immutable items, selected/missing/null id, row callback, add,
remove only for valid active item, empty state, bounded visible rows,
disabled/read-only, regular/compact geometry и standalone/Field equality.
Exact leaf включает List/Button primitives и исключает Field/Node/playground.

Среда и приёмка: production tests/typecheck/delivery exact build. Compact row и
operator dock сверяются с Blender 4.5.5 reference без заявления pixel parity;
MetaFor materials/font сохраняются. Подготовительный commit регистрирует также
зависимую UI-010.8 story до появления нового public leaf.

Подготовительный commit: этот project checkpoint, до production patch.

Фактические действия и результат: commit `059deffc8` добавил public
`CollectionInput`, exact `@ui/components/collection-input`, immutable rows,
controlled selection, bounded `1–8` visible rows, empty state, owner select/
add/remove, disabled/read-only и regular/compact density. Новый
`Field(kind:"collection")` делегирует exact control и измеряет label/hidden
compact presentation. Delivery complete-import/root/exact fixtures обновлены.

Parent correction `03b2c252b` сделала compact `24px` rows однострочными, чтобы
secondary description не выходил за row; regular `36px` сохраняет primary
`12px`/secondary `10px`. Height учитывает `max(rows, operator dock)` и не
обрезает add/remove при одном visible row. Geometry является MetaFor production
rhythm, не заявленной Blender pixel parity.

Regression и проверки: production Components `55/55` (`651` assertions),
delivery `14/14` (`443`), оба typecheck и diff-check прошли. Независимый
Collection/Field/retained rerun `16/16` (`413`) и Components typecheck зелёные.
Drag/reorder, item editing, async data и Node topology не добавлялись.

### UI-010.8 — Добавить CollectionInput в package-owned catalog

Статус и исполнитель: `COMPLETE` на package story/live contract; owner
acceptance остаётся parent gate.

Требование: после exact public leaf отдельный playground-only slice добавляет
concrete lazy `CollectionInput` story, selected/empty/disabled/read-only states,
rows и owner add/remove/select events, exact TypeScript/copy и controls.
Manifest completeness получает новый input leaf до следующего acceptance
snapshot.

Граница: только `pkg/ui/components/playground/**`; production Components,
shared Workbench и другие packages не меняются. После source checkpoint один
`$ui-dev` Components restart доказывает representative ready route, exact
source/args, events, console `0`, non-black capture и native restore. Automated
capture не является owner acceptance.

Production prerequisite: `059deffc8` + correction `03b2c252b`. Подготовительный
commit: этот state checkpoint, до playground patch.

Результат: source checkpoint `5df1327c5` добавил concrete lazy
`collection-input` с selected/empty/disabled/read-only/compact variants,
русскими metadata, exact source/controls и owner select/add/remove events.
Generic Field catalog получил collection kind, manifest completeness — `7/7`.
Focused playground `8/8` (`175` assertions), package/playground typechecks,
production Components `55/55` (`651`) и diff-check зелёные; story implementation
не входит initial split entry.

Live evidence: `$ui-dev` восстановил stopped Components selector как exact
skill-owned PID `61841`, origin `4017`; один прежний target
`D0775AE44CFF0E299A0C28EECB3872D2`. Route
`collection-input/value/selected` ready, source/args exact, console `0`.
Первый полностью чёрный canvas отклонён; после explicit same-route `open`
non-black capture
[`components-collection-input.png`](../artifacts/UI-010/components-collection-input.png)
имеет `645468` bytes, SHA-256
`254ea221ee3040a40a0fe82c47b1b77bd83b360466fd69991bd356cd78872277`,
canvas `3840×2176`, native `1920×1088 @2` восстановлен. Bridge callbacks
доказаны focused test без OS input; automated evidence не owner acceptance.

### UI-010.9 — Добавить owner-controlled reorder в CollectionInput

Статус и исполнитель: `IN_PROGRESS`, внутренний исполнитель текущей задачи.

Классификация: следующий механизм уже принятого CollectionInput. Первый slice
закрыл rows/selection/add/remove; reorder требует новых observable actions и не
может быть скрыто добавлен в UI-010.7.

Требование: CollectionInput принимает owner `onMove(id, "up" | "down")` и
показывает соседние Blender-like `↑/↓` operators только когда reorder включён.
Component вычисляет availability по controlled selected item и его current
index, но не переставляет immutable items самостоятельно. Field collection
делегирует тот же callback.

Основание: Blender `uiTemplateList` сохраняет controlled active row, а соседние
operators выполняют mutation/reorder. Existing Button/Flex dock достаточен для
явных up/down actions; drag/drop primitive и item model не нужны этому срезу.

Разрешённое изменение: только CollectionInput/Field/tests и при необходимости
delivery regression уже существующего exact leaf. Move actions не появляются,
если owner не передал `onMove`; legacy add/remove geometry сохраняется.
Drag/drop, keyboard reorder, new icons/Elements API, item editing, Node topology
и playground не меняются.

Regression: up/down boundaries first/last, missing/null/disabled selection,
disabled/read-only, immutable items, exact callback id/direction, optional dock
measurement и standalone/Field equality. Geometry учитывает фактическое число
operators без clipping и называется MetaFor production rhythm.

Подготовительный commit: этот project checkpoint, до production patch.

### UI-010.10 — Обновить CollectionInput story для reorder

Статус: `WAITING` на UI-010.9.

Playground-only slice обновляет concrete CollectionInput story: up/down
operators, immutable owner reorder, exact source и events. Existing public leaf
и completeness `7/7` не меняются. После source checkpoint supervising task
выполняет `$ui-dev ensure`, exact ready/console/non-black live check и сохраняет
artifact result. Граница — только `pkg/ui/components/playground/**` и evidence;
production/shared/другие packages не меняются.

## Границы

* Не переносить Socket, Link, Node preview, Node panels или topology semantics в
  `@ui/components`.
* Не создавать параллельный legacy/new Field API, compatibility aliases или
  Node-specific control bundle.
* Не менять layout solver, Hamiltonian либо product integration.
* Общую недостающую возможность Elements сначала оформить отдельным owner
  slice; не скрывать popup/overlay lifecycle внутри одного Field.
* Production leaf imports остаются независимыми и разделяют один Engine,
  Elements и product-owned UiRuntime.

## Критерии готовности

1. Каждый принятый control является отдельным public component и используется
   через единый Field facade.
2. Standalone и Node Parameter показывают один и тот же component/value state.
3. Compact geometry и interaction states сверены с exact Blender reference;
   MetaFor divergences перечислены явно.
4. Controlled immutable callbacks, disabled/read-only и linked/hidden states
   защищены focused tests.
5. Exact subpath builds не дублируют Engine/Elements и не включают playground.
6. Components и Node playground дают DOM/console/canvas evidence; owner visual
   acceptance остаётся отдельным gate.

## Состояние

`IN_PROGRESS`: по решению владельца production Components развиваются
через public leaf → dependent package story law. NumberInput, ColorInput,
VectorInput, MatrixInput, ReferenceInput и EnumInput завершили package/live
contracts. Текущий production-срез — UI-010.7 CollectionInput selection/
add/remove и UI-010.8 package-owned story/completeness/live завершены. Следующий
срез UI-010.9 добавляет owner-controlled reorder без нового public leaf;
UI-010.10 story зарегистрирована `WAITING`.
