# HAM-002 — Собрать визуальный слой Hamiltonian в одном контуре

## Коротко

Собрать в `hamiltonian/visual` весь Hamiltonian-specific код, который именно
отображает нодовую систему: projection, layout composition, presentation, HUD,
transport palette и visual entrypoint. Lifecycle, control и bootstrap остаются
в orchestration; универсальные законы node-system не копируются.

## Зачем

Сейчас визуальная реализация исторически распределена между
`hamiltonian/browser/orchestration.ts`, `browser/orchestration/*`, browser
entrypoints, lifecycle projection, HUD-панелями и тестами. Это затрудняет ответ
на два разных вопроса: что является причинным законом Hamiltonian, а что только
его визуальной проекцией и composition универсальных `@nodes/*` пакетов.

## Историческая линия и отличие

* `MF-415` создала живую orchestration scene.
* `MF-419`/`MF-420` и закрытые NODES-задачи вынесли и стабилизировали layout.
* `MF-424` принимает отдельные наблюдаемые дефекты живого вида вместе с
  владельцем; это не задача организации исходников.
* `HAM-001` владеет retained declarations и lifecycle согласованностью; это не
  presentation layer.
* `NODES-009.1`–`NODES-009.5` удалили Hamiltonian-specific catalog и HUD
  coupling из общей библиотеки, доказали package/browser boundaries и обновили
  живой Service Worker. Эти закрытые срезы удовлетворили подготовительную
  зависимость; открытый родитель `NODES-009` не блокирует этот refactor.
* Result `NODES-009` (`9c569e9c9`) вынес Hamiltonian transport catalog и
  palette из общего `@nodes/ui` в
  `hamiltonian/browser/orchestration/connection-color.ts`, Inspector — в
  `@nodes/hud`, а fixed adapter — в `@nodes/ui/fixed-card-layout`. Отдельный
  Hamiltonian visual package он не создавал; остальной consumer-код остался в
  `browser/orchestration*` и теперь является прямым входом HAM-002.
* Существующий `pkg/visual` — другой пакет, используемый Bulk. Он не участвовал
  в `NODES-009` и полностью исключён из границ HAM-002.
* `MF-415` оставила под WebGPU canvas отдельный legacy fallback screen. Снимок
  владельца подтвердил, что при ошибке WebGPU он становится параллельным UI со
  своими status fields, actions и event log. Владелец потребовал удалить этот
  экран и всю принадлежащую только ему DOM-функциональность; нодовая система
  остаётся единственным отображением Hamiltonian.

Поэтому работа получает новый стабильный номер, а не `MF-424.4` и не подзадачу
`HAM-001`.

## Предварительный инвентарь

До реализации повторно проверить актуальный tree, но начать с:

* `hamiltonian/browser/orchestration.ts` — bootstrap и composition сцены;
* `hamiltonian/browser/orchestration/lifecycle-projection.ts` — projection
  Hamiltonian declarations/lifecycle в `NodeSystemDocument`;
* `canvas-view.ts`, `workspace.ts`, `selection-retention.ts` — HUD и workspace;
* `responsive-layout.ts`, `layout-transition.ts` — consumer layout policy и
  presentation transition;
* `traffic-presentation.ts` — live message presentation;
* `spatial-runtime.ts` — наблюдаемая spatial state;
* `hamiltonian/browser/layout-worker.ts` — browser Worker entrypoint;
* связанные `*.spec.ts`, `browser-build.spec.ts`, `host.ts` bundle ownership и
  разделы `hamiltonian/README.md`.
* `hamiltonian/browser/orchestration/connection-color.ts` — уже вынесенные
  NODES-009 product catalog и palette;
* workspace registration, package exports, `bun.lock` и Hamiltonian imports —
  будущая физическая граница `hamiltonian/visual`;
* `hamiltonian/public/index.html`, `styles.css` и DOM-only writes/listeners в
  `public/app.js` — отдельный legacy fallback screen.

Каталог transport family, палитра и легенда являются Hamiltonian-specific и
должны войти в этот контур. Generic `NodeSystemDocument`, validation,
`@nodes/layout`, HUD-free renderer primitives и optional `@nodes/hud` остаются
в универсальных пакетах.

## Подтверждённая готовность к запуску

* Проверенный source baseline — `main@0f17d980ceafc2b9603af6a1de62dc23fc869608`.
* Коммит `9c569e9c9` физически разделил `nodes`, `@nodes/layout`, `@nodes/ui` и
  `@nodes/hud`, вынес Hamiltonian palette к consumer и сохранил browser builds
  и проверки.
* Коммит `e3d6b5960` опубликовал Service Worker `1.1.3`; package/browser/live
  closing review закрыт в `0f17d980c` с действующими Oracle/Force линиями и
  актуальной browser source revision.
* `NODES-009` по решению владельца остаётся `IN_PROGRESS` для дальнейшей
  универсальной работы, но будущие изменения этого родителя не являются
  неявной зависимостью `HAM-002`.
* Первый исполнитель начинает из подготовительного project-коммита этой
  карточки, повторно сверяет `HEAD` и текущий import graph и не считает
  исторический live evidence доказательством ещё не выполненного refactor.

## Целевая граница

`hamiltonian/visual/` владеет visual projection/composition Hamiltonian.
Владелец разрешил оформить его внутренним workspace-пакетом; точное package
name и exports фиксирует диагностический срез по фактическому import graph.
Структура должна различать как минимум:

* `projection/` — declarations/lifecycle -> generic node document;
* `layout/` — выбор universal adapter, responsive policy и transitions;
* `presentation/` — WebGPU surface, traffic, palette и selection;
* `hud/` — Inspector/canvas controls composition;
* browser visual entrypoint, который соединяет слои, но не содержит lifecycle,
  control или bootstrap реализации.

`hamiltonian/browser/orchestration*` сохраняет невизуальные lifecycle, control,
bootstrap и причинную агрегацию. Код переносится в `visual` только тогда, когда
его собственный результат — model или pixels отображения, а не потому, что его
данные позже показываются.

Названия являются направлением, а не разрешением механически переместить файлы
без проверки dependency graph.

## Границы

* Подготовительный gate закрыт срезами `NODES-009.1`–`NODES-009.5`; открытый
  родитель `NODES-009` не нужно закрывать перед началом.
* Не менять lifecycle, transport, declaration или Force semantics.
* Не переносить в `visual` невизуальный orchestration/control код.
* Не менять принятую нодовую геометрию попутно с перемещением.
* Единственное принятое изменение UI в этой задаче — полное удаление legacy
  fallback screen и принадлежащей только ему DOM-функциональности. При ошибке
  WebGPU остаётся доступное сообщение об ошибке, но не второй интерфейс.
* Не копировать код или типы из `nodes` в Hamiltonian.
* Не превращать `hamiltonian/visual` во второй node-system и не переносить в
  него generic model, validation, renderer primitives или layout laws.
* Каждый новый визуальный дефект по-прежнему регистрируется и принимается через
  `MF-424`, а не скрыто исправляется этим structural refactor.

## Первый срез после запуска

Получивший эту родительскую задачу агент сначала переводит `HAM-002` в
`IN_PROGRESS`, указывает себя исполнителем и регистрирует подзадачу
`HAM-002.1 — Зафиксировать ownership и import graph визуального слоя` отдельным
подготовительным project-коммитом. Этот диагностический срез:

1. снимает актуальный import graph и ownership inventory без перемещения кода;
2. фиксирует точную целевую структуру `hamiltonian/visual/`, public exports и
   полный список move-only файлов;
3. отделяет bootstrap entrypoints, причинный lifecycle contract и bundle
   ownership от projection, layout composition, presentation и HUD;
4. учитывает result `NODES-009`, включая consumer-owned palette, generic
   `@nodes/*` entrypoints и запрещённое обратное направление импорта;
5. задаёт независимые последующие срезы: удаление legacy fallback, перенос
   projection/layout/presentation/HUD и финальная сборка visual entrypoint;
6. останавливается, если обнаруживает необходимость менять другое поведение,
   другой owning contract или новый визуальный дефект: это не расширяется скрыто в
   structural refactor.

Только после checkpoint `HAM-002.1` начинается перенос по одному слою. После
структурной эквивалентности выполняется live Hamiltonian regression без
заявления новой эстетической приёмки.

## Критерии готовности

1. Product-specific visual code находится под одним явным owning contour.
2. Browser entrypoints содержат bootstrap, а не смешанную реализацию всех слоёв.
3. Projection, layout composition, presentation и HUD имеют односторонние
   зависимости и отдельные тесты.
4. Ни один universal package не импортирует Hamiltonian.
5. Browser bundles, package/root typecheck и Hamiltonian tests проходят.
6. Bulk и `pkg/visual` не изменены.
7. В DOM и browser bundle отсутствуют legacy fallback screen, его controls,
   status grid, identity dump и event log; единственное отображение Hamiltonian
   — нодовый canvas с HUD.
8. Живой нодовый пользовательский сценарий причинно эквивалентен baseline;
   новая эстетическая приёмка не приписывается structural refactor.

## Подзадачи

### HAM-002.1 — Зафиксировать ownership и import graph визуального слоя

Статус и исполнитель: `COMPLETE`, внутренний субагент текущей задачи Codex.

Классификация: диагностический срез результата NODES-009 и подтверждённого
legacy UI; код и файлы пока не перемещаются.

Требование или диагностический результат: зафиксировать действующий import
graph Hamiltonian visualization после NODES-009, затем определить одностороннюю
целевую структуру `hamiltonian/visual` и последовательные срезы.

Основание и связанная история: `MF-415`, `MF-419`/`MF-420`, закрытые
`NODES-009.1`–`NODES-009.5`, commit `9c569e9c9`, owner-уточнения и screenshot
13 августа 2026 года.

Наблюдаемое расхождение: NODES-009 вернул consumer-owned palette в Hamiltonian,
но Hamiltonian-specific visual code всё ещё разбросан по orchestration, а
legacy fallback остаётся отдельной веткой DOM UI.

Причина: историческое развитие трёх независимых слоёв до общего owner-решения.

Разрешённое изменение одного механизма: только карточка, import/ownership
inventory и точный план атомарных move-срезов; product source не изменяется.

Regression или опровергающее доказательство: source import graph, workspace
exports, browser entrypoints, DOM ownership и существующие package/browser
checks.

Среда и критерий приёмки: canonical `main@703d5224209c1b392aec770bba4c75dad83832b8`;
inventory покрывает все source consumers, различает nonvisual orchestration и
visual code и задаёт проверку каждого следующего среза.

Фактические действия: прочитаны документы-владельцы и project-контекст;
проверены canonical root/branch/HEAD/worktree; просмотрены result NODES-009
`9c569e9c9`, текущие imports, public DOM/bootstrap, Worker entrypoint,
Hamiltonian bundle owner, package manifests и все прямые tests/consumers. Код,
Bulk и `pkg/visual` не изменялись; runtime не запускался.

Результат и вывод: NODES-009 не создал Hamiltonian visual package. Он вернул
product-owned palette в `browser/orchestration/connection-color.ts`, вынес
generic Inspector в `@nodes/hud` и fixed adapter в `@nodes/ui`; весь остальной
Hamiltonian visual composition остался в `browser/orchestration*`. Целевая
граница подтверждена как новый private workspace `hamiltonian/visual` с именем
`@hamiltonian/visual`, а не как перенос или изменение `pkg/visual`.

Действующий import/ownership graph:

* `public/window-entry.js` сначала загружает `/core/monitor.js`, затем параллельно
  `/app.js` и host-built `/orchestration.js`. `public/app.js` владеет page,
  Service Worker, Dedicated Worker, WebRTC, Web Push, singleton/control и
  lifecycle emission; после удаления legacy DOM он остаётся nonvisual
  orchestration. `browser/orchestration.ts` сейчас смешивает lifecycle cursor,
  retained frontier/snapshot/declaration bootstrap с WebGPU scene, layout,
  selection, traffic, HUD и DOM evidence.
* `orchestration/lifecycle-projection.ts` читает только
  `core/lifecycle.js`, `core/orchestration.js` и generic `nodes/types`; его
  собственный результат — `NodeSystemDocument` и presentation message. Он и
  его spec принадлежат visual `projection`, а transport/declaration contracts,
  cursor, subscriptions и source retirement остаются в Hamiltonian
  orchestration.
* Visual leaf graph сейчас таков: `orchestration.ts -> workspace -> @ui/hud`;
  `orchestration.ts -> canvas-view -> workspace + connection-color`;
  `orchestration.ts -> connection-color -> @nodes/ui fallback`;
  `orchestration.ts -> responsive-layout | layout-transition |
  selection-retention | spatial-runtime | traffic-presentation`; main scene
  также напрямую использует `nodes/layout-worker`, `@nodes/ui`, `@nodes/hud`
  и `@ui/elements`.
* `browser/layout-worker.ts` является отдельным browser entrypoint и зависит
  только от `nodes/layout-worker` и `nodes/types`; это visual bundle boundary,
  но не lifecycle-нода. `host.ts` остаётся владельцем Bun.build, URL
  `/orchestration.js`/`/layout-worker.js`, source revision, static mapping и
  source watchers; он меняет только target paths и добавляет watch
  `hamiltonian/visual`.
* Unit specs следуют за своими файлами. `browser-build.spec.ts` и
  `host.spec.ts` остаются Hamiltonian integration tests; первый меняет Worker
  entry path, второй — import projection и bundle/static ownership. Ни один
  universal `nodes`, `@nodes/layout`, `@nodes/ui` или `@nodes/hud` source не
  импортирует Hamiltonian.

Точная целевая структура и exports:

```text
hamiltonian/visual/
├── package.json, tsconfig.json, bunfig.toml, index.ts
├── projection/{index,lifecycle-projection}.ts
├── layout/{index,responsive-layout,layout-transition}.ts
├── presentation/{index,connection-color,traffic-presentation,
│                 selection-retention,spatial-runtime}.ts
├── hud/{index,workspace,canvas-view}.ts
└── browser/{index,layout-worker}.ts + browser/styles.css
```

`package.json` публикует только `.`, `./projection`, `./layout`,
`./presentation`, `./hud` и `./browser/layout-worker`; root export — browser
visual controller над generic `NodeSystemDocument`, без lifecycle/control
реализации. Прямые зависимости: `@metafor/engine`, `nodes`, `@nodes/ui`,
`@nodes/hud`, `@ui/components`, `@ui/elements`, `@ui/hud`. `@nodes/layout`
остаётся транзитивной реализацией `nodes`/`@nodes/ui` и не объявляется без
прямого import. `hamiltonian/package.json` в итоге зависит от
`@hamiltonian/visual`, но visual browser/layout/presentation/HUD не импортируют
Hamiltonian orchestration. Разрешённое исключение — `projection` читает
Hamiltonian-owned lifecycle types/registry из соседнего `core`; обратного
import из `core` или universal packages нет.

Полный move-only inventory:

* в `projection/`: `lifecycle-projection.ts` и `.spec.ts`;
* в `layout/`: `responsive-layout.ts`/`.spec.ts` и
  `layout-transition.ts`/`.spec.ts`;
* в `presentation/`: `connection-color.ts`/`.spec.ts`,
  `traffic-presentation.ts`/`.spec.ts`, `selection-retention.ts`/`.spec.ts` и
  `spatial-runtime.ts`/`.spec.ts`;
* в `hud/`: `workspace.ts`/`.spec.ts` и `canvas-view.ts`/`.spec.ts`;
* в `browser/`: `browser/layout-worker.ts`; после удаления legacy CSS —
  оставшийся canvas/status `public/styles.css` как `browser/styles.css` с тем
  же URL через host static mapping.

`browser/orchestration.ts` нельзя перемещать целиком: из него отдельным
structural extraction создаётся `visual/browser/index.ts` с `UiRuntime`, graph,
HUD, Worker client, layout coalescing/cancellation, responsive transition,
selection, traffic, spatial evidence и dispose. Исходный entrypoint сохраняет
page bootstrap, lifecycle subscriptions/cursor/frontier, snapshot/declaration
acceptance и передаёт visual controller только projection documents,
presentation events и action callback. `public/index.html`, `window-entry.js`,
`app.js`, `core/*`, `service-worker.ts`, `page-lifecycle-declaration.ts` и
`host.ts` остаются Hamiltonian bootstrap/control/bundle owners.

Legacy fallback является отдельным уже одобренным behavior slice: удалить из
`index.html` весь `main.legacy-debug`; из CSS — его reveal и все main/header,
status-grid, identity, actions и event-log rules; из `app.js` — `elements` map,
DOM `log`, все `elements.*` writes и шесть дублирующих button listeners.
Сохранить canvas, доступный `#orchestration-status`, nonvisual runtime и
единственный `hamiltonian-orchestration-action` path от нодового HUD к
`runOrchestrationAction`.

External coordination gate: активный
`project/artifacts/NODES-008/benchmark.ts` хеширует exact paths
`hamiltonian/browser/orchestration.ts`,
`hamiltonian/browser/orchestration/lifecycle-projection.ts` и
`hamiltonian/browser/orchestration/responsive-layout.ts`. Его повторный запуск
уже отдельно невалиден после NODES-009 из-за отсутствующего
`pkg/nodes/layout-engine.ts`; HAM-002 не правит чужой artifact. Независимые
срезы ниже выполняются раньше, но projection/layout и extraction
`orchestration.ts` начинаются только после release/adaptation от владельца
NODES-008.

Последовательные срезы после checkpoint:

1. `HAM-002.2` — удалить legacy fallback DOM/CSS/app-only UI и защитить
   отсутствие его IDs, controls, identity dump и event log source-проверкой.
2. `HAM-002.3` — зарегистрировать `@hamiltonian/visual`, workspace/lock,
   exports/dependency boundary и перенести очищенный visual stylesheet.
3. `HAM-002.4` — move-only presentation и его unit specs.
4. `HAM-002.5` — move-only HUD/workspace и их unit specs.
5. `HAM-002.6` — move-only layout Worker entrypoint, host path/watch и browser
   bundle regression.
6. После external gate `HAM-002.7` — move-only lifecycle projection и полный
   projection/host test corpus.
7. `HAM-002.8` — move-only responsive layout/transitions и их regressions.
8. `HAM-002.9` — собрать visual browser entrypoint, сузить
   `browser/orchestration.ts`, очистить root Hamiltonian dependencies и
   выполнить bundle/package integration.

Проверки каждого среза: moved unit specs и `git diff --check`; package slices
добавляют `bun run --cwd hamiltonian/visual typecheck` и boundary regression,
запрещающий reverse Hamiltonian imports в universal packages; Worker/bundle
срезы — `bun test hamiltonian/browser-build.spec.ts hamiltonian/host.spec.ts`.
Финальная структурная эквивалентность требует `bun test hamiltonian`, package
и root `typecheck`, production browser bundles, отсутствия legacy strings в
DOM/bundle и только затем live Hamiltonian regression; она не доказывает новую
эстетическую приёмку.

Подготовительный commit: `703d5224209c1b392aec770bba4c75dad83832b8`.

Result checkpoint: этот commit.

### HAM-002.2 — Удалить резервный экран и оставить только нодовую систему

Статус и исполнитель: `COMPLETE`, внутренний субагент текущей задачи Codex.

Классификация: отдельное принятое изменение поведения одного UI-механизма;
оно не является move-only частью structural refactor.

Требование или диагностический результат: Hamiltonian имеет одно отображение —
нодовый canvas с HUD. Старый fallback screen, его status grid, identity dump,
кнопки, topology и event log полностью отсутствуют вместе с обслуживающей
только их DOM-функциональностью.

Основание и связанная история: fallback добавлен в `MF-415`; screenshot
владельца 13 августа 2026 года сохранён по provenance в
[`artifacts/HAM-002`](../artifacts/HAM-002/README.md). HAM-002.1 подтвердил
точные DOM/CSS/app owners и result checkpoint `b6a8286bb`.

Наблюдаемое расхождение: при ошибке WebGPU класс `orchestration-failed`
скрывает canvas и раскрывает `main.legacy-debug`, то есть запускает второй
полноценный интерфейс с дублирующими actions и runtime fields.

Причина: первоначальный HTML-стенд был сохранён как fallback после добавления
нодовой WebGPU-сцены, а `public/app.js` продолжил обновлять его скрытый DOM.

Разрешённое изменение одного механизма: удалить `main.legacy-debug`, его CSS,
DOM map/writes/log и прямые button listeners; сохранить nonvisual runtime,
нодовые actions через `hamiltonian-orchestration-action`, canvas и доступный
текст ошибки `#orchestration-status`.

Regression или опровергающее доказательство: host/bootstrap test проверяет
отсутствие legacy container, IDs и labels; browser source test — отсутствие
DOM map, direct button listeners и DOM event log при сохранённом action bridge.

Среда и критерий приёмки: canonical `main@b6a8286bb`; focused host/browser
tests, browser build и `git diff --check` проходят. Live-проверка после запуска
host должна показать нодовую систему единственным UI; искусственно ломать
WebGPU или подменять пользовательский профиль не требуется для этого source
regression.

Фактические действия: из `public/index.html` удалён весь `main.legacy-debug` с
status grid, identity dump, шестью кнопками, topology и event log. Из
`public/styles.css` удалены только принадлежащие этому экрану reveal/layout/
control rules; error-state по-прежнему скрывает canvas и раскрывает доступный
`#orchestration-status`. Из `public/app.js` удалены legacy DOM map, все
`elements.*` writes, DOM event log и шесть прямых button listeners; сохранены
page/Service Worker/Dedicated Worker/WebRTC/Web Push runtime, keyboard shortcut,
`runOrchestrationAction` и единственный
`hamiltonian-orchestration-action` bridge от нодового HUD. В существующие
`host.spec.ts` и `browser-build.spec.ts` добавлены точные отрицательные проверки
legacy container/IDs/labels/styles/DOM listeners и положительные проверки
canvas, доступного status и action bridge. Bulk и `pkg/visual` не изменялись.

Результат и вывод: HTML Hamiltonian содержит только нодовый canvas и доступный
status; второго fallback-интерфейса и обслуживающего его DOM-кода больше нет.
`bun test hamiltonian/browser-build.spec.ts hamiltonian/host.spec.ts` —
`51 pass, 0 fail`; production browser build `public/app.js` и
`public/embodiment-worker.js` — успешно; `git diff --check` — успешно. Live
host не запускался: карточка оставляет живую проверку единственного нодового UI
после запуска общего контура.

Подготовительный commit: `ba0a90bec`.

Result checkpoint: этот commit.

### HAM-002.3 — Создать внутренний пакет визуализации Hamiltonian

Статус и исполнитель: `COMPLETE`, внутренний субагент текущей задачи Codex.

Классификация: structural package-boundary slice; визуальное поведение и
геометрия не меняются.

Требование или диагностический результат: создать внутри `hamiltonian/visual`
приватный workspace-пакет `@hamiltonian/visual`, закрепить одностороннюю
dependency/export boundary и перенести в него уже очищенный stylesheet
единственного нодового интерфейса.

Основание и связанная история: owner-уточнение исключило Bulk и существующий
`pkg/visual`; HAM-002.1 зафиксировал точную целевую структуру и package name;
HAM-002.2 удалил legacy fallback до физического переноса CSS.

Наблюдаемое расхождение: Hamiltonian-specific visual contour ещё не является
workspace-пакетом, а visual stylesheet остаётся в bootstrap-owned
`hamiltonian/public`.

Причина: до NODES-009 visual consumer развивался внутри orchestration и public
assets; отдельной Hamiltonian package boundary не существовало.

Разрешённое изменение одного механизма: создать package shell, exports,
typecheck/config, workspace/lock и зависимость Hamiltonian; перенести только
`public/styles.css` в `visual/browser/styles.css`; сохранить URL `/styles.css`
через host static mapping и добавить `hamiltonian/visual` в source watch.

Regression или опровергающее доказательство: package-boundary spec проверяет
workspace/name/exports, отсутствие reverse Hamiltonian imports в universal
packages и отсутствие `public/styles.css`; host regression подтверждает тот же
HTTP URL и точное содержимое очищенного CSS.

Среда и критерий приёмки: canonical `main@29c9002bb`; visual package typecheck,
package-boundary spec, focused host/browser tests, production browser build и
`git diff --check` проходят. Bulk и `pkg/visual` не изменены.

Разрешённые файлы: root `package.json`, `bun.lock`, `hamiltonian/package.json`,
новый `hamiltonian/visual/**`, `hamiltonian/host.ts`, package/host integration
specs и эта карточка для фактического результата.

Подготовительный commit: этот commit.

Фактические действия: зарегистрирован private workspace
`hamiltonian/visual` с именем `@hamiltonian/visual`, root export, typecheck и
целевой односторонней dependency boundary. `@metafor/hamiltonian` получил
workspace dependency, а `bun.lock` — только соответствующие workspace-записи.
Очищенный stylesheet без изменения bytes перенесён из `public/styles.css` в
`visual/browser/styles.css`; host по-прежнему отдаёт его по `/styles.css`,
включает в browser source revision и следит за visual source root. CSS остался
внутренним host-served asset и не опубликован package export. Добавлен отдельный
`visual/package-boundary.spec.ts`: он проверяет workspace/name/существующий
root export/dependencies, новый physical CSS owner и отсутствие reverse
Hamiltonian imports во всех universal `pkg/nodes` source. Host regression
сверяет HTTP body с точным новым physical owner. Presentation, HUD, layout,
projection и browser entrypoint не перемещались; Bulk, `pkg/visual` и
`project/artifacts/NODES-008` не изменялись.

Результат и вывод: внутренняя package boundary создана без изменения
визуального поведения и URL. `bun run --cwd hamiltonian/visual typecheck` —
успешно; `bun test hamiltonian/visual/package-boundary.spec.ts` — `3 pass,
0 fail`; `bun test hamiltonian/browser-build.spec.ts hamiltonian/host.spec.ts`
— `51 pass, 0 fail`; production browser build `public/app.js` и
`public/embodiment-worker.js` — успешно; byte-for-byte сравнение stylesheet,
`bun install --frozen-lockfile --lockfile-only` и `git diff --check` — успешно.

Result checkpoint: этот commit.

### HAM-002.4 — Перенести presentation Hamiltonian в visual

Статус и исполнитель: `COMPLETE`, внутренний субагент текущей задачи Codex.

Классификация: move-only presentation slice; поведение, palette и observable
spatial/traffic/selection semantics не меняются.

Требование или диагностический результат: перенести Hamiltonian-specific
palette, traffic presentation gate, selection retention и spatial runtime со
всеми их unit specs из `browser/orchestration` в
`hamiltonian/visual/presentation` и опубликовать один code subpath
`@hamiltonian/visual/presentation`.

Основание и связанная история: NODES-009 вернул product-owned connection color
в Hamiltonian; HAM-002.1 отнёс все четыре leaf-модуля к presentation, а
HAM-002.3 создал внутреннюю package boundary checkpoint `0f5056ef5`.

Наблюдаемое расхождение: presentation leaf-код остаётся физически под
orchestration, хотя не владеет lifecycle, control или bootstrap.

Причина: ранее у Hamiltonian не было собственного visual package.

Разрешённое изменение одного механизма: move-only четырёх `.ts` и четырёх
`.spec.ts`, новый `presentation/index.ts`, package export и замена imports в
browser orchestration на package subpath. Никаких иных source refactor.

Regression или опровергающее доказательство: все перенесённые unit specs,
visual package typecheck и package-boundary export existence; focused browser
bundle regression доказывает разрешение нового subpath.

Среда и критерий приёмки: canonical `main@0f5056ef5`; moved specs, visual
typecheck, boundary/browser checks, production orchestration build и
`git diff --check` проходят. Bulk, `pkg/visual` и NODES-008 без diff.

Разрешённые файлы: четыре presentation-модуля и specs по inventory,
`hamiltonian/visual/presentation/index.ts`, visual manifest/boundary spec,
`hamiltonian/browser/orchestration.ts`, а также
`hamiltonian/browser/orchestration/canvas-view.ts` исключительно для
механической замены его import `connection-color`; эта карточка. Сам HUD в этом
срезе не переносится и не изменяется. Разрешён также один локальный
`hamiltonian/visual/wgsl-shim.d.ts` с ambient declaration `*.wgsl`: перенос
palette впервые включает engine shader imports в package typecheck; runtime,
shader source и loader не меняются.

Подготовительный commit: этот commit.

Result checkpoint: готов к checkpoint-коммиту руководителя. Четыре source и
четыре unit spec byte-for-byte перенесены в `visual/presentation`; package
экспортирует `@hamiltonian/visual/presentation`, а orchestration и canvas-view
разрешают palette, traffic, selection и spatial presentation только через этот
subpath. Для package typecheck добавлен только локальный ambient `*.wgsl`
declaration по действующей workspace-конвенции; loader и shader sources не
изменены.

Проверка результата: moved unit specs вместе с package-boundary — `17 pass`,
`0 fail`, `65 expect`; `hamiltonian/visual` typecheck проходит; focused
browser build — `1 pass`, `0 fail`, `86 expect`; production orchestration
bundle собрал `148 modules`. `git diff --check` проходит, а Bulk, `pkg/visual`
и NODES-008 не имеют diff.

### HAM-002.5 — Перенести HUD и workspace Hamiltonian в visual

Статус и исполнитель: `COMPLETE`, внутренний субагент текущей задачи Codex.

Классификация: move-only HUD composition slice; frame laws, controls, legend,
actions и observable HUD behavior не меняются.

Требование или диагностический результат: перенести `workspace.ts`,
`canvas-view.ts` и их unit specs из `browser/orchestration` в
`hamiltonian/visual/hud`, добавить единый subpath `@hamiltonian/visual/hud` и
перевести orchestration consumer на него.

Основание и связанная история: HAM-002.1 отнесла оба модуля к Hamiltonian HUD;
HAM-002.4 уже перенесла используемую панелью palette в presentation checkpoint
`c9b40e09d`.

Наблюдаемое расхождение: Hamiltonian-owned workspace geometry и canvas HUD
panel всё ещё физически лежат под orchestration.

Причина: до HAM-002 не существовало собственного visual contour.

Разрешённое изменение одного механизма: byte-for-byte move двух source и двух
spec, новый `hud/index.ts`, package export, механическая замена imports в
orchestration. Никаких иных refactor или UI-изменений.

Regression или опровергающее доказательство: moved unit specs, package
boundary, visual typecheck и browser bundle regression.

Среда и критерий приёмки: canonical `main@c9b40e09d`; moved/boundary tests,
typecheck, production orchestration build и `git diff --check` проходят; Bulk,
`pkg/visual`, NODES-008 без diff.

Разрешённые файлы: `workspace.ts`/spec, `canvas-view.ts`/spec, новый
`visual/hud/index.ts`, visual manifest/boundary spec,
`browser/orchestration.ts` и эта карточка.

Подготовительный commit: этот commit.

Result checkpoint: готов к checkpoint-коммиту руководителя. `workspace` и
`canvas-view` вместе с unit specs byte-for-byte перенесены в `visual/hud`;
package экспортирует `@hamiltonian/visual/hud`, а orchestration разрешает всю
Hamiltonian HUD/workspace composition только через этот subpath. UI, frame
geometry, controls, legend и actions не изменены.

Проверка результата: moved unit specs вместе с package-boundary — `12 pass`,
`0 fail`, `60 expect`; `hamiltonian/visual` typecheck проходит; focused
browser build — `1 pass`, `0 fail`, `86 expect`; production orchestration
bundle собрал `149 modules`. Byte-for-byte comparison и `git diff --check`
проходят, а Bulk, `pkg/visual` и NODES-008 не имеют diff.

## Состояние

`IN_PROGRESS`: HAM-002.1 завершена checkpoint `b6a8286bb`, HAM-002.2 —
checkpoint `29c9002bb`, HAM-002.3 — checkpoint `0f5056ef5`, HAM-002.4 —
checkpoint `c9b40e09d`; HAM-002.5 реализована и проверена, checkpoint-коммит
ожидает руководителя.
