{
  "name": "@zavx0z/git-history",
  "version": "0.1.0",
  "description": "Git history — команды управления историей (switch, checkout, commit, reset, revert)",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/meta.ts",
    "./monad": "./git-history.json"
  },
  "devDependencies": {
    "@metafor/build": "workspace:*"
  },
  "scripts": {
    "build": "bun ../../build/cli.ts src/meta.ts --out git-history.json"
  }
}