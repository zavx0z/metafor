{
  "name": "@zavx0z/git-history",
  "version": "0.1.0",
  "description": "Git history — команды управления историей (switch, checkout, commit, reset, revert)",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/meta.ts",
    "./monad": "./git-history.json"
  },
  "devDependencies": {
    "@metafor/meta": "workspace:*",
    "@metafor/build": "workspace:*"
  },
  "scripts": {
    "build": "metafor-build src/meta.ts --out git-history.json"
  },
  "dependencies": {
    "@metafor/build": "link:@metafor/build"
  }
}