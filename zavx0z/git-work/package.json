{
  "name": "@zavx0z/git-work",
  "version": "0.1.0",
  "description": "Git work — команды работы с файлами (add, mv, restore, rm, clean)",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/meta.ts",
    "./monad": "./git-work.json"
  },
  "devDependencies": {
    "@metafor/meta": "workspace:*",
    "@metafor/build": "workspace:*"
  },
  "scripts": {
    "build": "metafor-build src/meta.ts --out git-work.json"
  },
  "dependencies": {
    "@metafor/build": "link:@metafor/build"
  }
}