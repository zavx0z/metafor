{
  "name": "@metafor/space",
  "author": "zavx0z",
  "version": "0.4.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "boundary/*",
    "force/*",
    "dsl/*",
    "fixture",
    "bulk/*"
  ],
  "devDependencies": {
    "@types/node": "^25.3.0",
    "dts-bundle-generator": "^9.5.1",
    "typedoc": "^0.28.17",
    "typedoc-plugin-merge-modules": "^7.0.0",
    "bun-webgpu": "^0.1.5",
    "@types/bun": "^1.1.11"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "bun index",
    "space": "bun space/server",
    "upd": "bun update --latest && bun i",
    "clear": "rm -rf node_modules && bun run --parallel --filter '*' clear",
    "typegen": "bun --filter @metafor/meta typegen",
    "build": "bun --filter @metafor/meta build",
    "space:build": "bun build metafor.ts --outdir ./dist --sourcemap=linked",
    "zavx0z:build": "bun run --parallel --filter '@zavx0z/*' build",
    "zavx0z:watch": "bun run --parallel --filter '@zavx0z/*' build --watch",
    "zavx0z:clean": "for dir in zavx0z/*/; do rm -f \"$dir\"/git*.json; done"
  }
}
