{
  "name": "@metafor/space",
  "author": "zavx0z",
  "version": "0.4.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "./meta",
    "./atom",
    "./infra/*"
  ],
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "dts-bundle-generator": "^9.5.1",
    "typedoc": "^0.28.13",
    "typedoc-plugin-merge-modules": "^7.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "upd": "bun update --latest && bun i",
    "build": "bun --filter @metafor/meta build && bun --filter @metafor/atom build && bun --filter @metafor/inspect build",
    "typegen": "bun --filter @metafor/meta typegen && bun --filter @metafor/atom typegen && bun --filter @metafor/inspect typegen",
    "clear": "bun --filter @metafor/meta clear && bun --filter @metafor/atom clear && bun --filter @metafor/inspect clear"
  }
}
