// Заглушка пакета `@surface/meta`. Реальная декларация существовала в более ранних
// версиях репозитория, но subset, на который ссылаются submodule-пакеты в `github/`,
// уже не используется в production-коде. Пустой пакет нужен только чтобы bun install
// корректно разрешал workspace-зависимости submodule'ов до их обновления.
export {}
