{
  "name": "nodes",
  "version": "0.0.1",
  "description": "Workspace container for the independent Nodes packages and their integration playground.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false"
  },
  "devDependencies": {
    "@nodes/core": "workspace:*",
    "@nodes/layout": "workspace:*",
    "@nodes/layout-worker": "workspace:*",
    "@nodes/ui": "workspace:*"
  }
}
