{
  "name": "space",
  "private": true,
  "devDependencies": {
    "@metafor/build": "workspace:*",
    "@metafor/meta": "workspace:*"
  },
  "scripts": {
    "build": "metafor-build src/meta.ts --out meta.json",
    "build:watch": "metafor-build src/meta.ts --out meta.json --watch",
    "space": "bun space/server"
  }
}
