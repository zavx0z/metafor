{
  "name": "@metafor/boundary",
  "description": "Граница (Boundary) — движок квантовых полей на WebGPU: компиляция правил суперпозиции в байт-код и эволюция полей на GPU.",
  "author": "zavx0z",
  "license": "MIT",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "devDependencies": {
    "@types/bun": "^1.1.11",
    "@types/puppeteer": "^5.4.7",
    "@webgpu/types": "^0.1.32",
    "@zavx0z/context": "^2.3.1",
    "bun-webgpu": "^0.1.5",
    "puppeteer": "^24.37.2"
  },
  "peerDependencies": {
    "bun": "^1.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "example": "bun run ./examples/server.ts",
    "setup:browsers": "npx puppeteer browsers install chrome",
    "build": "bun build ./src/index.ts --outdir ./dist --target browser",
    "test": "bun test",
    "test:logic": "bun test tests/field.logic.test.ts",
    "docs": "typedoc"
  },
  "type": "module"
}
