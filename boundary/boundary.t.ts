/**
 * Типы для @boundary/boundary.
 *
 * @packageDocumentation
 */

import type { BoundaryData } from "./store.t"

/** Плоский подготовленный снимок, который Boundary записывает в канонический store. */
export type PreparedData = BoundaryData
