---
name: table-format
description: Форматирование ширины колонок таблиц в Markdown файлах. Использовать при редактировании markdown файлов с таблицами для выравнивания колонок и pipe-символов.
---

# Table Format Skill

Форматирование ширины колонок таблиц в Markdown-файлах.

## Описание

Этот skill выравнивает:

1. Колонки таблиц по ширине для читаемости
2. Pipe-символы `|` по вертикали (MD060)
3. Разделители с пробелами (`| --- |`)

## Использование

``` bash
bun run .qwen/skills/table-format/format.ts <файл>
```

Или для всех markdown файлов в директории:

``` bash
bun run .qwen/skills/table-format/format.ts .
```

## Правила форматирования

1. **Разделители таблиц** обязаны иметь пробелы с обеих сторон от дефисов
2. **Контент в ячейках** должен быть отделен от границ (pipe) пробелами
3. **Колонки таблиц** должны быть выровнены по ширине для читаемости
4. **Pipe-символы** должны быть выровнены по вертикали (MD060)

## Пример

**До:**

``` markdown
| Тип | Где | Для чего |
| --------- | -------------------- | ---------------------------------- |
| Локальное | `@{pkg}/store.ts` | Данные для одного пакета |
| Общее | `@{domain}/store.ts` | Данные для нескольких пакетов domain |
```

**После:**

``` markdown
| Тип       | Где                  | Для чего                             |
| --------- | -------------------- | ------------------------------------ |
| Локальное | `@{pkg}/store.ts`    | Данные для одного пакета             |
| Общее     | `@{domain}/store.ts` | Данные для нескольких пакетов domain |
```

## Скрипт

Скрипт форматирования находится в `.qwen/skills/table-format/format.ts`

## Зависимости

* Bun runtime
* Нет внешних зависимостей
