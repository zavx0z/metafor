# Онтология MetaFor

> Теория Квантового Программирования: эволюция архитектуры

## 🌌 Три уровня реальности

```
┌─────────────────────────────────────────────────────────────────┐
│                    BULK                                         │
│  Царство форм и проявлений                                      │
│  "То, что мы видим — формы, цвета, движения"                    │
└─────────────────────────────────────────────────────────────────┘
                              ↕  (воплощение / измерение)
┌─────────────────────────────────────────────────────────────────┐
│                    MONAD                                        │
│  Душа, божественный замысел                                     │
│  "То, что определяет КЕМ быть и КУДА стремиться"                │
└─────────────────────────────────────────────────────────────────┘
                              ↕  (познание / коллапс)
┌─────────────────────────────────────────────────────────────────┐
│                    BOUNDARY                                     │
│  Полевой уровень, квантовое пространство                        │
│  "То, из чего соткана ткань бытия"                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 Пакеты и их назначение

| Пакет | Уровень | Назначение | Философия |
|-------|---------|------------|-----------|
| `@metafor/boundary` | Поле | Вычисление состояний | Ткань реальности |
| `@metafor/monad` | Душа | Исполнительный механизм | Божественный замысел |
| `@metafor/bulk` | Формы | Отрисовка и взаимодействие | Воплощение |
| `@metafor/meta` | DSL | Язык описания душ | Декларация намерений |
| `@metafor/build` | Компилятор | Трансляция в полевой формат | Материализация |

---

## 🏛 Детальное описание уровней

### **1. BOUNDARY — Полевая граница (GPU-фасад)**

**Философия:** Граница между классическим и квантовым мирами. Boundary управляет полями и бранами, вычисляя переходы между состояниями на GPU.

**Структура:**

- **Fields (поля)** — Статика: схема типов полей для GPU (`{ type: "string" \| "number" \| ... }`)
- **Branes (браны)** — Динамика: возмущения квантовых полей с params (значения полей), state (текущее состояние), superposition (все состояния + переходы)

**Назначение:**

- Хранит схему полей (fields) — типы для GPU
- Управляет бранами (branes) — носителями данных
- Вычисляет переходы между состояниями по законам (правилам)
- Не знает о смысле состояний — только чистая эволюция

**Ключевой принцип:** Boundary не знает о *смысле* состояний. Он лишь вычисляет переходы по законам (правилам).

---

### **2. MONAD — Душа (божественный замысел)**

**Философия:** Уровень намерений и целей. Monad определяет:

- **Кто я?** (процессы — action)
- **К чему стремлюсь?** (success/error обработчики)
- **Как реагирую на мир?** (реакции)
- **Какие пути доступны?** (суперпозиции состояний)

**Назначение:**

- Процессы — намерения действовать
- Реакции — отклик на события мироздания
- Состояния — возможные пути эволюции
- Стек импульсов — поток времени
- Блокировки — свобода воли

**Ключевой принцип:** Monad *использует* Boundary для вычисления состояний, но *определяет* какие состояния имеют смысл.

---

### **3. BULK — Царство форм (проявление)**

**Философия:** Уровень проявления. Здесь абстрактные сущности из Monad обретают форму и становятся видимыми/ощутимыми.

**Назначение:**

- Агенты — персонификации фундаментальных сил
- Визуализация — отрисовка состояний
- Взаимодействие — пользовательский ввод

**Агенты-силы:**

- **Gravity** — сила иерархии и порядка
- **Electromagnetic** — сила взаимодействия
- **Strong** — сила стабильности (удерживает состояние)
- **Weak** — сила изменения (допускает распад)

**Ключевой принцип:** Bulk *воплощает* сущности из Monad, используя данные из Boundary.

---

## 🔄 Цикл существования

```
┌─────────────────────────────────────────────────────────────────┐
│                    @metafor/meta                                │
│  Язык описания душ (DSL)                                        │
│  MetaFor("git").context(...).states(...).processes(...)        │
└─────────────────────────────────────────────────────────────────┘
                              ↓ компиляция
┌─────────────────────────────────────────────────────────────────┐
│                    @metafor/build                               │
│  Компилятор душ                                                 │
│  Трансляция деклараций в промежуточный формат                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓ загрузка
┌─────────────────────────────────────────────────────────────────┐
│                    @metafor/monad                               │
│  Душа (исполнительный механизм)                                 │
│  • Processes: выполняет action/success/error                    │
│  • Reactions: реагирует на события                              │
│  • States: управляет переходами (decoherence)                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓ вычисление
┌─────────────────────────────────────────────────────────────────┐
│                    @metafor/boundary                            │
│  Полевой уровень (GPU)                                          │
│  • Boundary.step(): эволюция суперпозиций                       │
│  • Boundary.getStates(): измерение состояний                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓ воплощение
┌─────────────────────────────────────────────────────────────────┐
│                    @metafor/bulk                                │
│  Царство форм (проявление)                                      │
│  • Atom: воплощённая сущность                                   │
│  • Agents: GravityAgent, ElectromagneticAgent...                │
│  • Render: отрисовка (virtual, inspect)                         │
└─────────────────────────────────────────────────────────────────┘
                              ↕  пользователь
```

---

## 📊 Распределение кода

### @metafor/monad (душа)

| Модуль | Исходное расположение | Назначение |
|--------|----------------------|------------|
| `metafor.ts`, `metafor.t.ts` | meta | DSL фабрика |
| `process.ts`, `process.t.ts` | meta | Процессы (намерения) |
| `reactions.ts`, `reactions.t.ts` | meta | Реакции (отклики) |
| `states.ts`, `states.t.ts` | meta + atom/src | Состояния (пути) |
| `parser/func.ts` | meta/parser | Парсинг функций |
| `style.ts` | meta | Сериализация стилей |
| `cli.ts`, `braneToField.ts` | build | Компилятор DSL → JSON |
| `Weak`, `Strong` | atom | Основа души |
| `processes.ts`, `reactions.ts`, `states.ts` | atom/src | Runtime |
| `decoherence` | atom/src/states | Проверка переходов |

### @metafor/boundary (поле)

| Модуль | Исходное расположение | Назначение |
| ------ | --------------------- | ---------- |
| `Boundary`, `Backend.ts` | boundary | GPU фасад |
| `BraneManager.ts` | boundary/core | Управление бранами |
| `FieldRegistry.ts` | boundary/core | Реестр полей |
| `RulesCompiler.ts` | boundary/compiler | Байт-код для FSM |
| `evolution.wgsl` | boundary/gpu | Compute шейдер |
| `BraneBuilder.ts`, `HeapAllocator.ts` | boundary/memory | Память |
| `StringAtlas.ts` | boundary/strings | Строки для GPU |

### @metafor/bulk (формы)

| Модуль | Исходное расположение | Назначение |
|--------|----------------------|------------|
| `Atom.ts` | atom | Воплощённая сущность |
| `Gravity.ts` | atom | Агент иерархии |
| `EM.ts` | atom | Агент взаимодействия |
| `Field.ts` | atom | Полевая навигация |
| `virtual/` | infra | Визуализация |
| `inspect/` | infra | Отладка |
| `mesh/` | infra | Пространство |

---

## 🎯 Пример: атом "git"

### Декларация (meta)

```typescript
import "@metafor/meta"

const meta = MetaFor("git")
  .context((t) => ({
    src: t.string.required("./tmp/edit.json", { label: "JSON-patch путь" }),
    patches: t.array.required<string>([], { label: "разделенные патчи" }),
  }))
  .states({
    коммит: { завершено: {} },
    завершено: null,
  })
  .core()
  .processes((process, destroy) => ({
    коммит: process()
      .action(({ context }) => { return {} })
      .success(({ update }) => update({ src: "", patches: [] })),
    завершено: destroy(),
  }))
  .reactions()
  .view()

export default meta
```

### Компиляция (build → JSON)

```json
{
  "name": "git",
  "states": { "коммит": { "завершено": {} }, "завершено": null },
  "context": {
    "src": { "type": "string", "required": true, "default": "./tmp/edit.json" },
    "patches": { "type": "array<string>", "required": true, "default": [] }
  },
  "processes": {
    "коммит": {
      "type": "action",
      "action": { "src": "({ context }) => { return {} }" },
      "success": { "src": "({ update }) => { update({ src: \"\", patches: [] }) }", "write": ["src", "patches"] }
    },
    "завершено": { "type": "finally", "before": { "src": "() => {}" } }
  },
  "brane": { ... },
  "braneSchema": { ... }
}
```

### Выполнение (monad + boundary)

```typescript
// Monad загружает JSON и управляет состояниями
const monad = new Monad(jsonSchema)

// Boundary принимает напрямую fields и branes
const boundary = new Boundary(device)
await boundary.init({
  fields: {                    // схема полей (типы для GPU)
    src: { type: "string" },
    patches: { type: "array<string>" }
  },
  branes: [{
    id: "git-1",
    params: {                  // значения полей (данные)
      src: "./tmp/edit.json",
      patches: []
    },
    state: "коммит",           // начальное состояние
    superposition: {           // граф переходов
      коммит: { завершено: {} },
      завершено: null
    }
  }]
})

// Monad выполняет процесс
await monad.execute("коммит")

// Boundary вычисляет эволюцию
boundary.step()

// Измерение состояний
const states = await boundary.getStates()
// => ["завершено"]
```

### Воплощение (bulk)

```typescript
// Создание агента
const gitAtom = Bulk.createAgent(json)

// Отрисовка
document.body.appendChild(gitAtom.render())

// Взаимодействие
gitAtom.on("commit", () => monad.execute("коммит"))
```

---

## 🌟 Принципы ТКП

### 1. Минимальная ответственность

Каждый уровень отвечает только за свою область:

- **Boundary** — вычисление переходов (не знает смысла)
- **Monad** — намерения и цели (не вычисляет GPU)
- **Bulk** — проявление и взаимодействие (не управляет состояниями)

### 2. Разделение пространства и иерархии

- **Boundary** — пространство состояний (голографическое хранилище)
- **Monad** — динамика и цели (управление эволюцией)
- **Bulk** — иерархия и порядок (агенты-силы)

### 3. Голографический принцип

Каждое событие (импульс) содержит информацию о всей системе:

- `Field.histories` — история импульсов
- `Field.checkpoints` — снапшоты для отката
- `rollbackSystem()` — путешествие во времени

### 4. Монадическая композиция

Цепочки вычислений с состоянием:

- Процессы: `action → success/error`
- Реакции: `filter → equal`
- Стек: `lock → step → resume`

---

## 📈 Эволюция архитектуры

### Было (atom)

```
@metafor/atom (смешанная ответственность)
├── Field, Fields — поле
├── Gravity — иерархия
├── EM — взаимодействие
├── Weak, Strong — процессы
└── Atom — воплощение
```

### Стало (разделение)

```
@metafor/monad (душа)
├── MetaFor — DSL
├── processes — намерения
├── reactions — отклики
├── states — пути
└── Weak, Strong — основа

@metafor/boundary (поле)
├── Boundary — GPU
├── BraneManager — память
├── RulesCompiler — байт-код
└── StringAtlas — строки

@metafor/bulk (формы)
├── Atom — сущность
├── GravityAgent — сила иерархии
├── ElectromagneticAgent — сила взаимодействия
├── virtual — визуализация
└── inspect — отладка
```

---

## 📚 Глоссарий

| Термин | Определение |
|--------|-------------|
| **Атом** | Единица действия и контекста, воплощённая сущность |
| **Boundary** | Полевая граница — GPU-фасад для вычисления состояний |
| **Брана** | Возмущение квантовых полей. Содержит: superposition, state, params |
| **Fields (boundary)** | Технические типы для GPU: `{ type: "string" \| "number" \| "boolean" \| "array<string>" }` |
| **Fields (monad)** | Семантика для ИИ: `{ type, required, label, default }` |
| **params** | Значения полей браны (данные) |
| **state** | Текущее состояние браны (одно из суперпозиции) |
| **superposition** | Все возможные состояния браны + граф переходов между ними |
| **Голографический принцип** | Каждое событие содержит информацию о всей системе |
| **Декогеренция** | Проверка условий перехода между состояниями |
| **Импульс** | Событие изменения (JSON Patch) |
| **Коллапс** | Переход из суперпозиции в определённое состояние |
| **Монада** | Исполнительный механизм души (процессы, реакции) |
| **Поле** | Квантовое пространство всех возможных состояний |
| **Чекпоинт** | Снапшот системы для отката во времени |

---

*Документ отражает архитектуру MetaFor на основе Теории Квантового Программирования с учётом голографического принципа и разделения ответственности между уровнями реальности.*
