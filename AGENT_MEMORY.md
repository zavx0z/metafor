# Живая память MetaFor для стартового доведения

Дата фиксации: 2026-06-11.

Этот файл хранит не стенограмму разговора, а устойчивое рабочее понимание, с которым нужно вести дальнейшую разработку MetaFor.
Он существует потому, что контекстное окно модели ограничено и может быть сжато.

## Главный вектор

MetaFor доводится не как очередной AI-инструмент, IDE-агент, движок рабочих процессов или VR-приложение.
MetaFor доводится как цифровая среда, в которой человек, агент, голос, зрение, речь, приложения, процессы, память, патчи, снапшоты, 3D-пространство и логика находятся в одной живой системе.

Практическая цель текущего стартового прохода: привести MetaFor к состоянию, в котором дальнейшая разработка уже идет из этой среды, через ее контекст, процессы, интерпретатор, наблюдаемую историю и пространственную модель, а не как внешняя работа над набором разрозненных инструментов.

## Авторская позиция

Автор MetaFor - Vladimir Filipenko (zavx0z).
Проект растет из длинного инженерного и исследовательского пути, а не из краткого рыночного цикла вокруг текущих AI-моделей.

Зафиксированный в README профессиональный фон: МФТИ, Инжиниринговый центр, старший инженер, участие в разработке автопилота автомобилей, архитектура беспилотных поездов, системы наблюдения от матрицы до видео-стены.

Это важно для разработки: читать MetaFor нужно не как фантазию поверх чата, а как инженерную попытку собрать среду из наблюдения, управления, пространства, процессов, физической аналогии и программной формализации.

## Рабочая модель системы

Первичная модель не командная.
Система живет от значений полей:

```text
значения полей -> контекст -> состояние -> переход -> процесс -> новые значения полей
```

Команда является поверхностным случаем.
Более точная инженерная форма: создать условия в контексте, при которых нужный процесс может протекать.

Эта модель должна влиять на API, UI, рантайм, тесты и документацию.
Если новая реализация начинает выглядеть как обычный командный бот, внешний оркестратор или набор ручных вызовов инструментов, нужно вернуться к полям, контексту, состояниям и процессам.

## Текущий порядок стартового доведения

Стартовое доведение разбито на два этапа.

Первый этап - довести `Dark` как самодостаточный слой скрытой связности: `Wimp`, `Fuzzy`, `Macho`, `Axion`, нити связности, bindings, историю/структурные изменения и Force-выход.

Второй этап - `Boundary`: ловить частицы из `Dark` через Force и фиксировать их как граничный отпечаток. `Boundary` не должен придумывать частицы сам и не должен подменять `Dark`; он принимает и канонически фиксирует то, что приходит из скрытой связности.

## Boundary, Коммит И Частицы Force

Фиксация 2026-06-14: прежний `Store` как смысл переименован в `Boundary`.
`Boundary` хранит каноническую персистентную форму мира. `Dark` может работать с
`Boundary` напрямую, потому что скрытая связность должна материализоваться в
фиксируемую границу.

`Energy` и `Bulk` не являются репликами `Boundary`, не читают SQLite и не
используют Boundary ORM как скрытый загрузчик. Это рантайм-слои: они получают
самодостаточные Force/WebSocket-данные в реальном времени и ведут собственное
состояние и проекцию в рантайме.

Легкий Force `Particle` с UUID допустим только внутри пары `Dark`/`Boundary`, где
получатель имеет право прочитать зафиксированную часть мира из `Boundary`.
Для `Energy` и `Bulk` частица должна нести данные, необходимые для рантайм-исполнения,
потому что у них нет чтения БД.

В распределенной системе нельзя возвращаться к независимому `db-sync` и
отдельному domain `Particle`: это создает гонку сигнала против данных и делает
рантайм второй базой. Правильная модель: один причинный путь коммита и данных.
`Boundary` фиксирует персистентное изменение, затем Force переносит рантайм-данные
тем доменам, которым нужно реагировать в рантайме.

Следствие для агента и любого внешнего участника среды: изменение значения поля не должно требовать двух ручных действий - отдельно писать в БД и отдельно слать Force part.
У участника должен быть один смысловой вход: изменить значение поля / изменить контекст.
Внутри этого входа среда сама должна сделать boundary transaction, сформировать
коммит и рантайм-данные, а затем разослать частицы Force.
Если API вынуждает агента синхронизировать БД и Force part вручную, это не пользовательская обязанность, а признак незавершенного рантайм-контракта.

Фиксация 2026-06-13 была сделана в терминах `store/force`; после переименования
читать это нужно как `boundary/force`.
Envelope доменного сигнала называется `{ parts: Particle[] }`; не `patches` и не `particles`.
Подписки и низкоуровневые каналы должны использовать `force.onmessage` / `force.postMessage` или ORM `boundary.onmessage` / `boundary.postMessage`, но доменные зоны не должны сами создавать `BroadcastChannel`.
Отправка доменных сигналов после материализации встроена в ORM-методы Boundary: `actor.create`, `topology.create`, `wimp.states.add`, `wimp.processes.add`, `wimp.matter.*` и другие write-методы сами рождают Force part после SQL-записи.
Dark/Weak/Gravity не должны отдельно отправлять Force part или создавать локальный `BroadcastChannel` после ORM-записи.
Файл `store/server.ts` удален как неверная граница; SQLite-open/update адаптер
должен читаться как часть `Boundary`.
Следующий шаг этого же направления - схлопнуть ORM-сигналы в полноценный транзакционный конверт коммита.

## Интерпретатор

## Рабочее правило: редактирование через API интерпретатора

Когда работа идет внутри текущей среды интерпретатора, агент не должен чувствовать
себя обычным внешним coding-agent и править активный исходный код напрямую через
локальный filesystem patch.

Для файлов, которые относятся к активному процессу, исходному коду или контексту интерпретатора,
рабочий путь правки: API интерпретатора, прежде всего
`POST /processes/:id/apply_patch` или `POST /processes/:id/source`.

Локальные filesystem-правки допустимы только для чтения/диагностики, для файлов
вне активного графа процесса, для долговечной памяти/документации вне контекста интерпретатора или
когда пользователь явно разрешил прямую локальную правку.

Если есть сомнение, активен ли файл в интерпретаторе, сначала проверить это через
API интерпретатора (`/processes`, `/processes/:id/modules`, `/context`) и только
потом выбирать способ правки. Это правило зафиксировано после ошибки, когда
часть изменений была сделана как обычным агентом через локальный patch, хотя
нужно было идти через API среды. Никогда не возвращаться к этой ошибке.

Текущий интерпретатор - первый рабочий мир интерпретатора.
Он нужен не только для удобной отладки.
Он показывает будущий способ жизни MetaFor: система не останавливается глобально, а локально открывает внимание к процессу, исходному коду, кадру, области видимости, терминалу, патчу, точке останова, снимку и действию.

В дальнейшем интерпретаторная способность должна жить внутри MetaFor-процессов.
Поэтому любые изменения интерпретатора нужно оценивать не как изменения IDE, а как изменения органа самонаблюдения и самодоработки среды.

## Агент внутри среды

Агент здесь не внешний исполнитель.
Агент является участником одной среды с человеком.

Функциональные соответствия:

- голосовой ввод - слух агента;
- речевой синтез - будущая речь агента;
- скриншоты, камеры, 3D-пространство и визуальные поверхности - зрение агента;
- `@meta/*` API, браузер, окно, ввод и процессы - органы действия;
- контекст интерпретатора, патчи, снимки, терминал, события - нервная система разработки;
- история, Boundary и линия времени - память;
- повторяемые процессы и навыки - функциональное подсознание.

Подсознание в этой системе означает не скрытую магию, а наблюдаемые, повторяемые действия, которые превращаются в устойчивые процессы и могут выполняться машинально через контекст.

## Социальное измерение

MetaFor в социальном измерении нельзя сводить к обычной социальной сети с лентой,
профилями и реакциями как главным продуктовым ядром.
Более точное чтение: социальность здесь является общей мысленностью среды, где
люди, агенты, память, процессы, действия и контекст связаны в одном пространстве.

Социальный слой должен проектироваться не как внешний community-модуль поверх
системы, а как способ совместного мышления и совместного присутствия внутри
процессов MetaFor.
Связь между участниками важна постольку, поскольку она меняет контекст,
состояние, переходы, память и дальнейшее протекание процессов.

## Самоулучшение

MetaFor должен позволять агенту улучшать свои процессы не через изменение весов модели, а через развитие функциональной среды:

- анализ прошлых патчей, снапшотов и событий;
- выделение повторяемых действий;
- превращение их в процессы, навыки и поля контекста;
- проверку будущих вариантов через повторное воспроизведение и симуляцию;
- обновление правил и компонентов среды при появлении новых требований.

UI-инвариант paint-панелей: кнопка свернуть всегда стоит крайней левой в заголовке панели в app-web и interpreter; у Codex message заголовок панели центрируется.

Рабочее понимание Web UI dev-контура: человек и агент должны видеть один и тот же браузерный state без обязательной опоры на Mac display пользователя. Для этого будущий браузер WebApp нужно развивать как `browser-display` внутри interpreter `Space`: frame/snapshot stream, input proxy, health/restart и source maps для TypeScript-отладки. Это не HUD-панель и не постоянная Playwright-зависимость. Host restart должен быть отдельным lifecycle-действием (`POST /restart` в поддерживаемом tmux-контуре), а клиенты после restart должны ждать `/health`, чтобы не оставаться на белом экране.

Фиксация 2026-06-27 по remote desktop server-dev: без перезагрузки машины и без
физического монитора рабочий быстрый visual/audio path строится через один
Wayland/Mutter virtual monitor, Google Chrome и sender
`webrtc:chrome:monitor` на `127.0.0.1:32133`. Ожидаемый `rtc/state`:
`transport: "chrome-webrtc"`, `capture.frameSource:
"chrome-get-display-media:monitor"`, `1920x1080`, `capture.frameRate: 60`,
`audio.transport: "pipewire-pcm-track-generator-stream"`, `audio.trackCount: 1`,
peer `connected`, data channel `open`, `lastError: null`. Старый
`32123`/`metafor-remote-desktop-host` нельзя оставлять параллельно с `32133`:
он создает второй `MetaVendor` monitor, и Chrome может открыться на одном
виртуальном экране, а WebRTC capture - на другом. `webrtc:chrome:browser`,
Xwayland и Electron/PipeWire/MJPEG остаются fallback/diagnostics, не основной
server-dev realtime path.

Cold restart этого контура: сначала должен быть живой virtual display `Meta-0`.
На текущем сервере его держит headless GNOME RDP session через локальный
FreeRDP trigger (`xfreerdp` к `127.0.0.1:3390` внутри Xvfb `:101`). Если
`gdbus ... org.gnome.Mutter.DisplayConfig.GetCurrentState` не показывает
`Meta-0` 1920x1080, не запускай Xwayland/current-tab fallback; восстанови RDP
trigger и только потом перезапускай `metafor-chrome-wayland-monitor-main` /
`pkg/interpreter/remote-desktop/chrome-webrtc-monitor.sh`.

С 2026-06-28 основной способ для агента управлять этим контуром -
interpreter endpoint `GET|POST /remote-desktop/lifecycle`. `GET` возвращает
schema/userStories и state (`hostReady`, `captureReady`, `audioReady`,
`controlReady`, `ready`). `POST` принимает `action` (`status`, `start`,
`restart`, `recover`, `stop`), `scope` (`sender`, `display`, `all`), `wait`,
`timeoutMs`, `cleanProfile`, `stopXvfb`, `config`. Для обычного restart default
scope - `sender`, чтобы не гасить `Meta-0`. Ручные tmux/grdctl/xfreerdp команды
остаются только fallback diagnostics, если lifecycle endpoint недоступен.

Фиксация 2026-06-28: Chrome WebRTC sender больше не размещается в видимом
`https://meta.proizvodstvo1.ru/` target. Sender живет в отдельной служебной
странице `http://127.0.0.1:32133/desktop/rtc/sender`, а видимая вкладка продукта
остается только рабочим браузером/DevTools. Поэтому reload
`meta.proizvodstvo1.ru` не должен рвать remote desktop. Sender signaling -
`ws://10.66.0.10:6500/webrtc/signaling`, input/audio - локальные routes host-а
`127.0.0.1:32133`.

Перенос из `app/electron` выполнен для живого server-dev path: Chrome WebRTC
monitor sender, host API `/desktop/health|rtc|input|audio` и default dev-layout
живут в `pkg/interpreter/remote-desktop`. Старый
`app/electron/scripts/chrome-webrtc-monitor.sh` - compatibility wrapper. Не
переносить мертвые ветки и fallback-и: старый `32123`, Xwayland/current-tab,
MJPEG/snapshot как основной frame loop, Playwright-клиенты,
Electron UI-specific shell behavior.

Это рекурсивный контур:

```text
мы строим MetaFor -> MetaFor дает агенту органы и процессы -> агент помогает строить MetaFor -> среда дает более сильные органы и процессы
```

## Что должно сохраняться после компрессии

После любой компрессии контекста будущий агент должен восстановить минимум:

1. MetaFor - цифровая среда для совместного развития человека и AGI, а не инструмент вокруг модели.
2. Основная логика - поля, контекст, состояния, переходы и процессы.
3. Интерпретатор - первый мир интерпретатора и будущий орган самонаблюдения среды.
4. Человек и агент работают вместе в одном live-контексте.
5. Голос, зрение, речь, память и навыки должны становиться частями среды.
6. Новые фундаментальные понимания нужно фиксировать патчами в долговечной памяти, а не оставлять только в чате.
7. В формулировках не снижать уже реализованное до "система пытается"; говорить "строится", "уже реализовано", "доводится", "связывается", "проходит стартовый/финальный проход", когда это соответствует состоянию репозитория.

## Правило доработки этой памяти

Если во время разработки становится понятнее, как на самом деле устроен MetaFor, агент обязан добавить маленькое точное обновление в этот файл или в более подходящий durable слой.

Хорошее обновление:

- меняет будущие инженерные решения;
- короткое;
- проверяемое по коду, документации, API или словам автора;
- не превращает файл в эмоциональную стенограмму;
- не дублирует уже существующую публичную документацию без необходимости.
